---
phase: 02-recipe-collection-makeable-engine
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - apps/barback/src/api/useRecipes.ts
  - apps/barback/src/components/RecipeList.tsx
  - apps/barback/src/components/RecipeRow.tsx
  - apps/barback/src/components/MakeableStatusBadge.tsx
  - apps/barback/src/components/IngredientListForm.tsx
  - apps/barback/src/components/MethodStepList.tsx
  - apps/barback/src/components/UnitDropdown.tsx
  - apps/barback/src/components/GlasswareSelector.tsx
autonomous: true
requirements: [RECIPE-01, RECIPE-02, MATCH-01, MATCH-02, MATCH-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Owner can see every recipe in a scrollable list, each row showing its name and a server-computed makeable/not-makeable badge (D-15)"
    - "Owner can search the recipe list by name"
    - "Owner can delete a recipe directly from its row, with a confirmation prompt naming the recipe ('Delete [recipe name]?' / 'This can't be undone.') before the delete fires"
    - "Zero recipes renders the empty state 'No recipes yet' / 'Add your first recipe to build your menu.'"
    - "A recipe row's makeable badge reads 'Ready to make' when makeable, 'Missing ingredients' when not — exact 02-UI-SPEC.md Component Inventory copy"
    - "Recipe names longer than the row width truncate with an ellipsis rather than wrapping or overflowing (02-UI-SPEC.md overflow consideration)"
    - "The recipe ingredient sub-form enforces at least one ingredient line and cannot be submitted with zero (mirrors the server's RECIPE-01 min(1) constraint at the UI layer too)"
    - "The method step sub-form numbers steps sequentially (1., 2., 3., ...) and renumbers correctly after a step is removed from the middle of the list"
    - "The unit dropdown offers exactly the D-19 fixed set (oz, dash, splash, barspoon, muddled, part) with no free-text entry possible"
    - statement: "Long individual method-step text wraps or truncates appropriately depending on context (multi-line in the edit textarea, single-line ellipsis in any list-style display)"
      verification: backstop
  artifacts:
    - path: "apps/barback/src/api/useRecipes.ts"
      provides: "useRecipes, useCreateRecipe, useUpdateRecipe, useDeleteRecipe, DeleteRecipeError"
    - path: "apps/barback/src/components/RecipeList.tsx"
      provides: "Searchable, scrollable recipe list with loading/error/empty/filtered-empty states"
    - path: "apps/barback/src/components/IngredientListForm.tsx"
      provides: "antd Form.List sub-component for recipe ingredient lines (category + quantity + unit)"
    - path: "apps/barback/src/components/MethodStepList.tsx"
      provides: "antd Form.List sub-component for ordered method steps"
  key_links:
    - from: "apps/barback/src/components/RecipeList.tsx"
      to: "apps/barback/src/api/useRecipes.ts"
      via: "useRecipes() query, in-memory name filter (same pattern as IngredientList's search)"
      pattern: "useRecipes\\("
    - from: "apps/barback/src/components/RecipeRow.tsx"
      to: "apps/barback/src/api/useRecipes.ts"
      via: "useDeleteRecipe() called directly from the row after a Modal.confirm"
      pattern: "useDeleteRecipe\\("
  prohibitions:
    - "RecipeList/RecipeRow must never compute makeable status themselves — they only render the makeable/missingCategoryNames fields already present on the fetched Recipe object (server is the single source of truth, per this phase's core trust guarantee)"
    - "IngredientListForm's unit field must never be a free-text Input — always the fixed UnitDropdown enum (D-19)"
---

<objective>
Build the recipe list (with search, makeable badges, and delete) and the reusable form sub-components (ingredient lines, method steps, unit dropdown, glassware selector) that plan 02-06's RecipeForm will assemble into create/edit. This plan and 02-04 (glassware frontend) both depend only on wave 2's backend and touch disjoint files, so they run in parallel.

Purpose: D-15 requires the makeable badge visible per-row across the whole collection, not just on a detail view; D-16/D-19 require the method-step and unit sub-forms to be genuinely reusable, structured components (not ad-hoc free text) so the create and edit paths in 02-06 share identical validation and rendering.

Output: a working, searchable RecipeList with real delete; the four form sub-components RecipeForm will consume.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-recipe-collection-makeable-engine/02-CONTEXT.md
@.planning/phases/02-recipe-collection-makeable-engine/02-UI-SPEC.md
@.planning/phases/02-recipe-collection-makeable-engine/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useRecipes.ts, RecipeList.tsx, RecipeRow.tsx, MakeableStatusBadge.tsx</name>
  <files>apps/barback/src/api/useRecipes.ts, apps/barback/src/components/RecipeList.tsx, apps/barback/src/components/RecipeRow.tsx, apps/barback/src/components/MakeableStatusBadge.tsx</files>
  <read_first>
    apps/barback/src/api/useIngredients.ts
    apps/barback/src/api/useCategories.ts (DeleteCategoryError pattern)
    apps/barback/src/components/IngredientList.tsx
    apps/barback/src/components/IngredientRow.tsx
    packages/shared/src/recipe.ts
    .planning/phases/02-recipe-collection-makeable-engine/02-UI-SPEC.md (Copywriting Contract, Color)
  </read_first>
  <action>
    Create `apps/barback/src/api/useRecipes.ts`: `useRecipes()` (`useQuery({ queryKey: ['recipes'], queryFn: () => apiFetch<Recipe[]>('/recipes') })`); `useCreateRecipe()` (POST `RecipeInput`, `onSettled` invalidates `['recipes']` — needed now even though RecipeForm arrives in 02-06, so the hook contract exists in one place per Interface-First ordering); `useUpdateRecipe()` (PATCH `{ id, patch: RecipePatch }`, `onSettled` invalidates `['recipes']`); `useDeleteRecipe()` (raw `fetch` DELETE mirroring `useDeleteCategory`'s body-reading pattern, throwing a `DeleteRecipeError extends Error` on non-204, `onSettled` invalidates `['recipes']`).

    Create `apps/barback/src/components/MakeableStatusBadge.tsx`: props `{ makeable: boolean }` — no `missingCategoryNames` prop needed here (the exact missing-category text lives in 02-06's RecipeDetailView, per the Copywriting Contract's detail-view placement). Renders `<Tag icon={<CheckCircleOutlined />} color="success">Ready to make</Tag>` when `makeable`, else `<Tag icon={<CloseCircleOutlined />} color="error">Missing ingredients</Tag>` — these two exact strings come from 02-UI-SPEC.md's Component Inventory, not the longer "Can't make this right now. Missing: [...]" sentence (that sentence is reserved for the detail view where the full category-name list fits).

    Create `apps/barback/src/components/RecipeRow.tsx`: props `{ recipe: Recipe; onEdit?: (recipe: Recipe) => void; onView?: (recipe: Recipe) => void }` (no `onDelete` prop — delete is self-contained, matching 02-PATTERNS.md's RecipeRow sketch). Renders recipe name (truncate via `className="truncate"`, matching `IngredientRow`'s `<span className="text-base truncate ...">` pattern for the overflow consideration) plus `<MakeableStatusBadge makeable={recipe.makeable} />`, and conditionally-rendered Edit/View/Delete icon buttons (`{onEdit && (...)}`, `{onView && (...)}`, matching `IngredientList`'s optional-prop-gated button pattern — View is unconditionally absent in THIS plan since `RecipeDetailView` doesn't exist until 02-06; only render the Edit button when `onEdit` is supplied, same gating, and omit any `onView`-driven button entirely for now, it's added in 02-06). Delete: call `useDeleteRecipe()` directly inside `RecipeRow`, and wrap the mutate call in antd's `Modal.confirm({ title: \`Delete ${recipe.name}?\`, content: "This can't be undone.", okText: 'Delete', okButtonProps: { danger: true }, onOk: () => deleteRecipe.mutate(recipe.id) })` — the exact confirmation copy from 02-UI-SPEC.md's Copywriting Contract, triggered from a `danger` `DeleteOutlined` icon button with `style={{ minHeight: 48, minWidth: 48 }}`.

    Create `apps/barback/src/components/RecipeList.tsx` mirroring `IngredientList.tsx`'s structure exactly: `useRecipes()` for data, `isPending`/`isError`/`refetch` states (same Spin/Alert-with-Retry rendering, copy "Couldn't load recipes — check your connection and try again."), a `query` search state filtering in-memory by `recipe.name.toLowerCase().includes(...)` only (no category filter — recipes don't have a single category the way ingredients do), a sticky search `Input` (placeholder "Search recipes…"), the true-empty vs filtered-empty distinction `IngredientList` already establishes (empty state "No recipes yet" / "Add your first recipe to build your menu." vs "No matches for '{query}'"), and a scrollable `<ul>` of `<RecipeRow>` per recipe. Props: `{ onEdit?: (recipe: Recipe) => void; onView?: (recipe: Recipe) => void }`, threaded straight through to each `RecipeRow`.
  </action>
  <verify>
    <automated>pnpm -F @my-bar/barback build</automated>
  </verify>
  <acceptance_criteria>
    - `useRecipes.ts` exports all four hooks plus `DeleteRecipeError`
    - `MakeableStatusBadge` renders exactly "Ready to make" / "Missing ingredients" per makeable boolean
    - `RecipeRow`'s delete button opens an antd `Modal.confirm` with the exact contracted title/content before calling `useDeleteRecipe().mutate`
    - `RecipeList` distinguishes true-empty from filtered-empty exactly as `IngredientList` does
    - `pnpm -F @my-bar/barback build` succeeds
  </acceptance_criteria>
  <done>A real, searchable recipe list renders with correct makeable badges and working delete-with-confirmation, ready for 02-06 to wire Add/Edit into.</done>
</task>

<task type="auto">
  <name>Task 2: IngredientListForm.tsx, MethodStepList.tsx, UnitDropdown.tsx, GlasswareSelector.tsx</name>
  <files>apps/barback/src/components/IngredientListForm.tsx, apps/barback/src/components/MethodStepList.tsx, apps/barback/src/components/UnitDropdown.tsx, apps/barback/src/components/GlasswareSelector.tsx</files>
  <read_first>
    apps/barback/src/api/useCategories.ts
    apps/barback/src/components/CategoryManager.tsx (Form.List/array-management idiom, though CategoryManager itself doesn't use Form.List — reference antd's Form.List API directly for the array-field pattern)
    packages/shared/src/recipe.ts
    packages/shared/src/glassware.ts
    .planning/phases/02-recipe-collection-makeable-engine/02-PATTERNS.md (IngredientListForm.tsx, MethodStepList.tsx, GlasswareSelector.tsx, UnitDropdown.tsx sections)
  </read_first>
  <action>
    Create `apps/barback/src/components/UnitDropdown.tsx`: a plain antd `<Select placeholder="Unit" options={[...]} />` where `options` is built from the fixed D-19 array `['oz', 'dash', 'splash', 'barspoon', 'muddled', 'part']` mapped to `{ value: unit, label: unit }` — no props, no free-text fallback, since it's used inside an antd `Form.Item` that supplies `value`/`onChange` automatically via `antd Form`'s `name` binding.

    Create `apps/barback/src/components/IngredientListForm.tsx`: an antd `Form.List name="ingredients"` (the field name matching `recipeInput.ingredients` exactly, so it slots directly into `RecipeForm`'s form instance in 02-06). Each field row renders three `Form.Item`s sharing the field's `name` tuple (`[field.name, 'categoryId']`, `[field.name, 'quantity']`, `[field.name, 'unit']`): a `Select` populated from `useCategories()` (existing Phase 1 hook — import from `../api/useCategories.js`) for categoryId, an `Input maxLength={20}` for quantity, and `<UnitDropdown />` for unit, plus a `danger` `MinusOutlined` remove button per row. A trailing `dashed` `PlusOutlined` button reads "Add Ingredient". Because `recipeInput.ingredients` requires `.min(1)`, do NOT auto-seed one empty row on mount (that would silently satisfy the count without real data) — instead rely on the surrounding `Form.Item`'s `rules={[{ required: true }]}` (set by `RecipeForm` in 02-06) plus antd's own empty-array-fails-`Form.List`-validation behavior; this component's job is only to render whatever rows exist and let `add`/`remove` mutate them.

    Create `apps/barback/src/components/MethodStepList.tsx`: an antd `Form.List name="method"` (matching `recipeInput.method`). Each field renders a numbered prefix (`{index + 1}.`) beside an `Input.TextArea maxLength={500} rows={2}` bound to the field's own name (no nested tuple here — `method` is `z.array(z.string())`, a flat string array, so each `Form.List` field IS the string itself, not an object — bind the `Form.Item` directly to `field.name` with no further nesting) and a `danger` `MinusOutlined` remove button; removing a step from the middle re-renders the remaining steps' numbers correctly because the numbering is derived from `index`, not stored per-step. A trailing `dashed` `PlusOutlined` button reads "Add Step" (exact 02-UI-SPEC.md copy).

    Create `apps/barback/src/components/GlasswareSelector.tsx`: a purely presentational component, props `{ glassware?: Glassware[] }` (imported type from `@my-bar/shared`) — it does NOT call `useGlassware()` itself; the caller (`RecipeForm`, in 02-06) fetches and passes the array down, keeping this component's only job "render options from what I was given." Renders `<Select placeholder="Select a glassware type (optional)" options={(glassware ?? []).map(g => ({ value: g.id, label: g.name }))} allowClear />`.
  </action>
  <verify>
    <automated>pnpm -F @my-bar/barback build</automated>
  </verify>
  <acceptance_criteria>
    - `UnitDropdown` offers exactly the 6 D-19 unit values, no others, no free-text `Input` fallback anywhere in the component
    - `IngredientListForm` does not auto-seed a default empty row
    - `MethodStepList`'s step numbers are derived from array index, verified by reading the render logic (no per-step stored index field)
    - `GlasswareSelector` takes `glassware` as a prop and contains no `useGlassware` import
    - `pnpm -F @my-bar/barback build` succeeds
  </acceptance_criteria>
  <done>All four recipe-form sub-components exist, correctly typed against the shared Zod contracts, ready for 02-06's RecipeForm to compose.</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `apps/barback/src/api/useRecipes.ts` — new exports: `DeleteRecipeError`, `useRecipes`, `useCreateRecipe`, `useUpdateRecipe`, `useDeleteRecipe`
- `apps/barback/src/components/RecipeList.tsx` — new export: `RecipeList` (props `{ onEdit?, onView? }`)
- `apps/barback/src/components/RecipeRow.tsx` — new export: `RecipeRow` (props `{ recipe, onEdit?, onView? }`)
- `apps/barback/src/components/MakeableStatusBadge.tsx` — new export: `MakeableStatusBadge` (props `{ makeable: boolean }`)
- `apps/barback/src/components/IngredientListForm.tsx` — new export: `IngredientListForm` (antd `Form.List` field named `ingredients`)
- `apps/barback/src/components/MethodStepList.tsx` — new export: `MethodStepList` (antd `Form.List` field named `method`)
- `apps/barback/src/components/UnitDropdown.tsx` — new export: `UnitDropdown`
- `apps/barback/src/components/GlasswareSelector.tsx` — new export: `GlasswareSelector` (props `{ glassware?: Glassware[] }`)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| RecipeList search input | Client-side, in-memory filter only — never sent as a query parameter, no new server surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Tampering | RecipeRow delete confirmation | low | mitigate | Delete requires an explicit `Modal.confirm` acknowledgement naming the recipe before `useDeleteRecipe.mutate` fires — no accidental single-tap delete |
| T-02-15 | Spoofing | Client-rendered makeable badge | high | mitigate | `MakeableStatusBadge` renders only the `makeable` boolean already computed server-side on the fetched `Recipe` — no client-side recomputation exists anywhere in this plan's components |
</threat_model>

<verification>
- `pnpm -F @my-bar/barback build` succeeds
- `pnpm -F @my-bar/server test` (full server suite) still green — this plan touches no server files, regression check only
</verification>

<success_criteria>
The recipe list is real, searchable, correctly badge-annotated, and deletable with confirmation; all four form sub-components are ready for the next plan's RecipeForm.
</success_criteria>

<output>
Create `.planning/phases/02-recipe-collection-makeable-engine/02-05-SUMMARY.md` when done
</output>
