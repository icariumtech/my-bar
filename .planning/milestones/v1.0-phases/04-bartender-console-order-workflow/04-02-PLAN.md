---
phase: 04-bartender-console-order-workflow
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/bartender/src/api/useRecipes.ts
  - apps/bartender/src/api/useTags.ts
  - apps/bartender/src/components/BottomTabBar.tsx
  - apps/bartender/src/components/BottomTabBar.test.tsx
  - apps/bartender/src/components/RecipesTab.tsx
  - apps/bartender/src/components/RecipesTab.test.tsx
  - apps/bartender/src/components/RecipeSearchFilter.tsx
  - apps/bartender/src/components/RecipeSearchFilter.test.tsx
  - apps/bartender/src/components/MakeableStatusBadge.tsx
  - apps/bartender/src/components/MakeableStatusBadge.test.tsx
  - apps/bartender/src/components/RecipeOrOrderDetail.tsx
  - apps/bartender/src/components/RecipeOrOrderDetail.test.tsx
  - apps/bartender/src/App.tsx
autonomous: true
requirements: [BART-01, BART-05, BART-06]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Bartender's MakeableStatusBadge renders the FULL tri-state (green 'Ready to make' / yellow 'Substitution needed' / red 'Missing ingredients') — unlike Patron's 2-state collapse, all three strings are reachable, matching D-63 exactly and reusing the same three copy strings Barback's own MakeableStatusBadge already established"
    - "RecipeOrOrderDetail renders recipe.overallStatus and each ingredient line's status exactly as the server returns them — never re-computed or re-derived client-side; the recipe-level badge and the per-line indicators always agree because both read the same server response field (BART-06 adjacency/ordering edge — no independent client-side rollup logic exists to disagree with the server)"
    - "A recipe with a null glassware/garnish/description renders the existing 'None specified'/omitted-section convention already proven in Barback's RecipeDetailView — not a new gap this phase introduces (BART-01 unclassified edge, resolved)"
    - "RecipeSearchFilter's combined filter is: (no name query OR case-insensitive substring match on the trimmed query) AND, for every tag group with at least one tag selected, the recipe carries at least one selected tag in that group — tags within the same group combine as OR, different groups combine as AND; an empty query with zero tags selected returns the full unfiltered list (BART-05 empty edge)"
    - "RecipesTab's search entry point opens RecipeSearchFilter as a full-screen overlay (not inline chips) per D-62 — the Orders tab (built in Plan 04-04) has no equivalent search affordance, since D-62 explicitly scopes search/filter to Recipes only"
    - "The Recipes list renders the loading spinner, the 'Failed to load recipes. Check your connection.' + Retry error state, and 'No recipes match your search' for a filtered-to-zero result — matching 04-UI-SPEC.md's Copywriting Contract exactly"
    - statement: "Bartender Recipes Tab's true-zero-recipes empty state (no recipes exist system-wide, not just filtered-to-zero) is UI-SPEC's own flagged assumption: the system always has >=1 recipe by this phase (Phase 2 already seeded/created real recipes) — this plan reuses the identical 'No recipes match your search' copy for both the true-empty and filtered-empty cases rather than authoring a second, never-yet-observed empty state"
      verification: backstop
    - statement: "Recipe list rendering at >100 items on the Echo Show 8's ~8\" screen (overflow edge) relies on antd List's own virtualization/scroll behavior rather than a bespoke windowing implementation — no automated test proves smooth scroll performance at that scale; a real-device pass is the only way to confirm no UI cutoff or lag"
      verification: backstop
  artifacts:
    - path: "apps/bartender/src/components/BottomTabBar.tsx"
      provides: "Two-tab (Recipes/Orders) navigation with a live open-order-count badge (D-55)"
      exports: ["BottomTabBar"]
    - path: "apps/bartender/src/components/RecipeOrOrderDetail.tsx"
      provides: "Shared full detail view (recipe always required; optional order + onMarkDone props for the conditional Done button Plan 04-04 wires) — D-56"
      exports: ["RecipeOrOrderDetail"]
    - path: "apps/bartender/src/components/MakeableStatusBadge.tsx"
      provides: "Full tri-state badge, Bartender's own copy of Barback's component (D-63)"
      exports: ["MakeableStatusBadge"]
  key_links:
    - from: "apps/bartender/src/App.tsx"
      to: "apps/bartender/src/components/BottomTabBar.tsx"
      via: "activeTab state ('recipes' | 'orders'), replacing 04-01's single-view tracer body"
      pattern: "BottomTabBar"
    - from: "apps/bartender/src/components/RecipesTab.tsx"
      to: "apps/bartender/src/components/RecipeOrOrderDetail.tsx"
      via: "tap a recipe row -> setViewing(recipe) -> <RecipeOrOrderDetail recipe={viewing} onBack={...} /> (no order prop from this entry point)"
      pattern: "RecipeOrOrderDetail"
  prohibitions:
    - statement: "The Bartender console MUST NOT expose any recipe/ingredient/tag create, edit, or delete action — BART-01/BART-05/BART-06 are read-only lookup surfaces; all catalog management (BARBACK-01/02/03) stays exclusively on Barback, never duplicated or partially rebuilt here"
      status: resolved
      verification: judgment
---

<objective>
Replace Plan 04-01's minimal single-view Bartender shell with the real two-tab console: a `BottomTabBar` (Recipes/Orders, with a live open-order-count badge), a `RecipesTab` with a full-screen name+tag search/filter (D-62), and the shared `RecipeOrOrderDetail` view showing the FULL tri-state makeable status (D-63) plus every measurement/method/glassware/garnish/tag field a bartender needs to actually make the drink (BART-01). The Orders tab itself (batching, Done button, elapsed-time polish) is Plan 04-04's job — this plan wires the tab shell and the Recipes side completely, with `OrdersTab` (already real and live from 04-01) simply plugged into the second tab slot.

Purpose: BART-01/BART-05/BART-06 are the console's primary daily-use surface — a bartender reaching for a recipe mid-service needs this to work correctly before the order-queue polish matters.
Output: `BottomTabBar`, `RecipesTab`, `RecipeSearchFilter`, `MakeableStatusBadge`, `RecipeOrOrderDetail` — all new; `App.tsx` rewired to the full two-tab shell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-bartender-console-order-workflow/04-CONTEXT.md
@.planning/phases/04-bartender-console-order-workflow/04-UI-SPEC.md
@.planning/phases/04-bartender-console-order-workflow/04-01-PLAN.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plan 04-04 implements against these — no codebase exploration needed. -->

apps/bartender/src/components additions:

  BottomTabBar({ activeTab: 'recipes'|'orders', onChange, openOrderCount: number })
  RecipeOrOrderDetail({ recipe: Recipe, order?: { patronName: string|null; status: OrderStatus; elapsedSeconds: number }, onBack: () => void, onMarkDone?: () => void })
  MakeableStatusBadge({ status: TriStateStatus })

apps/bartender/src/api additions:

  useRecipes()  : UseQueryResult<Recipe[]>   — queryKey ['recipes'], mirrors Patron/Barback's shape
  useTags()     : UseQueryResult<Tag[]>       — queryKey ['tags'], mirrors Barback's read-only useTags.ts
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BottomTabBar + RecipesTab + App.tsx rewire — real two-tab shell with a live badge count</name>
  <files>apps/bartender/src/api/useRecipes.ts, apps/bartender/src/api/useTags.ts, apps/bartender/src/components/BottomTabBar.tsx, apps/bartender/src/components/BottomTabBar.test.tsx, apps/bartender/src/components/RecipesTab.tsx, apps/bartender/src/components/RecipesTab.test.tsx, apps/bartender/src/App.tsx</files>
  <read_first>
    apps/barback/src/components/BottomTabBar.tsx
    apps/barback/src/components/RecipesTab.tsx
    apps/barback/src/components/RecipeList.tsx
    apps/patron/src/api/useRecipes.ts
    apps/barback/src/api/useTags.ts
    apps/bartender/src/App.tsx (04-01's tracer-minimal version — this task replaces its body)
    apps/bartender/src/api/useOrders.ts (04-01 — the badge count's data source)
    .planning/phases/04-bartender-console-order-workflow/04-UI-SPEC.md (Component Inventory: Bartender Page-Level/Tab-Level Components; Copywriting Contract: Bartender new elements; Appendix: Orders badge count)
  </read_first>
  <behavior>
    RED then GREEN:
    - `BottomTabBar.test.tsx`: renders two tabs labeled "Recipes" and "Orders"; the Orders tab shows a badge with `openOrderCount` when it's greater than 0, and shows no badge (or a zero badge, per antd `Badge`'s own default `showZero={false}` behavior) when it's 0; clicking a tab that isn't already active calls `onChange` with that tab's value; clicking the already-active tab does NOT call `onChange` (mirrors Barback's existing no-op-on-active-tap convention).
    - `RecipesTab.test.tsx` (mock `useRecipes`): renders a loading spinner while `isPending`; renders "Failed to load recipes. Check your connection." plus a working Retry button on `isError`; renders every recipe's name in a list when populated; tapping a recipe row transitions to the detail view (verify by asserting the row's name is still present but a "back" affordance now also renders, OR mock `RecipeOrOrderDetail` and assert it received the tapped recipe as a prop); tapping the search/filter button transitions to the `RecipeSearchFilter` view (mock it, assert it renders).
  </behavior>
  <action>
    `apps/bartender/src/api/useRecipes.ts` (new) — identical shape to `apps/patron/src/api/useRecipes.ts`: `export function useRecipes() { return useQuery({ queryKey: ['recipes'], queryFn: () => apiFetch<Recipe[]>('/recipes'), staleTime: Infinity, gcTime: 1000 * 60 * 10 }) }` (import `Recipe` from `@my-bar/shared`, `apiFetch` from `./client.js`). `staleTime: Infinity` is correct here too — Bartender trusts the same Socket.IO `inventory:changed`/`recipe:updated` invalidation pattern Patron already proves, once Plan 04-04 or a future plan wires those two additional socket handlers (not this plan's scope — this plan's `socket.ts` from 04-01 only listens for `orders:*` events; a recipe/inventory change during this phase's scope still requires a manual Bartender refresh until that gap is closed, which is out of THIS plan's scope since BART-06 only requires the DISPLAY to be correct, not full live sync of recipe-catalog changes — the makeable status the bartender sees on each fetch is always correct, just not push-updated the instant an ingredient toggles).

    `apps/bartender/src/api/useTags.ts` (new) — identical to `apps/barback/src/api/useTags.ts`: `export function useTags() { return useQuery({ queryKey: ['tags'], queryFn: () => apiFetch<Tag[]>('/tags') }) }`.

    `apps/bartender/src/components/BottomTabBar.tsx` (new) — same structural shape as `apps/barback/src/components/BottomTabBar.tsx` (fixed-position bar, icon+label buttons, no-op-on-active-tap guard), but with TWO tabs instead of three: `{ value: 'recipes', label: 'Recipes', icon: <CoffeeOutlined /> }`, `{ value: 'orders', label: 'Orders', icon: <InboxOutlined /> }` (both from `@ant-design/icons`, already used by Barback). Props: `{ activeTab: 'recipes' | 'orders'; onChange: (tab: 'recipes' | 'orders') => void; openOrderCount: number }`. Wrap the Orders tab's icon in an antd `Badge`: `<Badge count={openOrderCount} color="#22c55e" offset={[-8, 8]}>{icon}</Badge>` — D-55's live count of open (not-yet-done) orders.

    `apps/bartender/src/components/RecipesTab.tsx` (new) — same structural shape as `apps/barback/src/components/RecipesTab.tsx`'s own `view` state machine (`'list' | 'filter' | 'detail'`), but simpler: no add/edit view exists on Bartender (read-only, per this plan's own prohibition). `const [view, setView] = useState<'list' | 'filter' | 'detail'>('list')`, `const [viewing, setViewing] = useState<Recipe>()`, `const [filteredIds, setFilteredIds] = useState<Set<string> | undefined>(undefined)` (undefined means "no filter applied" — `RecipeSearchFilter` sets this on Apply, Task 2's job to define the filtering logic; this task just holds the state and renders whatever `RecipeSearchFilter` reports back). `const { data: recipes, isPending, isError, refetch } = useRecipes()`. If `view === 'detail' && viewing`, return `<RecipeOrOrderDetail recipe={viewing} onBack={() => setView('list')} />` (no `order`/`onMarkDone` props from this entry point — Task 2 creates `RecipeOrOrderDetail`, this task just calls it). If `view === 'filter'`, return `<RecipeSearchFilter recipes={recipes ?? []} onApply={(ids) => { setFilteredIds(ids); setView('list') }} onBack={() => setView('list')} />` (Task 2 creates `RecipeSearchFilter`). Otherwise render: a sticky header with a search/filter icon button (antd `Button` with `SearchOutlined`, `aria-label="Search & Filter"`, `onClick={() => setView('filter')}`, same `48x48` square-icon-button convention `SearchFilterBar.tsx`/`RecipesTab.tsx` (Barback) already use); loading spinner on `isPending`; error alert + Retry (`refetch()`) on `isError`; otherwise the recipe list — `const visibleRecipes = filteredIds === undefined ? recipes : recipes.filter((r) => filteredIds.has(r.id))`; if `visibleRecipes.length === 0`, render "No recipes match your search" (covers both the true-system-empty case and the filtered-to-zero case, matching Barback's own `RecipeList.tsx` empty-copy-reuse precedent); otherwise an antd `List` of recipe rows (name + `<MakeableStatusBadge status={r.overallStatus} />`, Task 2 provides that component), each `onClick={() => { setViewing(r); setView('detail') }}`.

    `apps/bartender/src/App.tsx` (rewrite the tracer-minimal body from 04-01). `const [activeTab, setActiveTab] = useState<'recipes' | 'orders'>('recipes')`. `const { data: orders } = useOrders()` (from 04-01's `api/useOrders.js`). `const openOrderCount = (orders ?? []).filter((o) => o.status === 'new' || o.status === 'in_progress').length` — D-55's badge counts "new + in_progress" only, per `04-UI-SPEC.md`'s Appendix, never `'done'`. Keep the same `ConfigProvider`/`theme.darkAlgorithm`/token block from 04-01 unchanged. Render `<div className="h-dvh overflow-hidden flex flex-col bg-bar-bg"><div className="flex-1 min-h-0 overflow-y-auto">{activeTab === 'recipes' && <RecipesTab />}{activeTab === 'orders' && <OrdersTab />}</div><BottomTabBar activeTab={activeTab} onChange={setActiveTab} openOrderCount={openOrderCount} /></div>` — mirrors Barback's own `App.tsx` scroll-container/fixed-bottom-bar structure exactly (the `min-h-0`/`overflow-y-auto` wrapper is what makes any future sticky headers inside `RecipesTab`/`OrdersTab` pin correctly).
  </action>
  <verify>
    <automated>pnpm --filter bartender test -- BottomTabBar RecipesTab</automated>
  </verify>
  <acceptance_criteria>
    - `BottomTabBar.test.tsx` and `RecipesTab.test.tsx` pass, including the badge-count and no-op-on-active-tap cases
    - `grep -c "'new' || o.status === 'in_progress'" apps/bartender/src/App.tsx` is at least 1 — the badge count excludes 'done'
    - `grep -c "<BottomTabBar" apps/bartender/src/App.tsx` is at least 1
    - `pnpm --filter bartender test -- BottomTabBar RecipesTab` passes
    - `git log` shows a `test(04-02): ...` commit preceding a `feat(04-02): ...` commit for this task
  </acceptance_criteria>
  <done>Opening /bartender/ shows a real two-tab shell; the Orders tab's badge reflects the live open-order count; the Recipes tab lists every recipe and can navigate toward detail/filter views (both still stubs from this task's perspective, wired for real in Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MakeableStatusBadge + RecipeSearchFilter (name+tag AND/OR logic) + RecipeOrOrderDetail</name>
  <files>apps/bartender/src/components/MakeableStatusBadge.tsx, apps/bartender/src/components/MakeableStatusBadge.test.tsx, apps/bartender/src/components/RecipeSearchFilter.tsx, apps/bartender/src/components/RecipeSearchFilter.test.tsx, apps/bartender/src/components/RecipeOrOrderDetail.tsx, apps/bartender/src/components/RecipeOrOrderDetail.test.tsx</files>
  <read_first>
    apps/barback/src/components/MakeableStatusBadge.tsx
    apps/barback/src/components/views/RecipeDetailView.tsx
    apps/barback/src/components/FullScreenHeader.tsx
    apps/patron/src/components/TagRail.tsx (getActiveTagIds/filterRecipesByTag pure-function-ahead-of-component convention this task mirrors)
    packages/shared/src/tag.ts
    .planning/phases/04-bartender-console-order-workflow/04-CONTEXT.md (D-62, D-63)
    .planning/phases/04-bartender-console-order-workflow/04-UI-SPEC.md (Component Inventory: RecipeSearchFilter/RecipeOrOrderDetail; Copywriting Contract)
  </read_first>
  <behavior>
    RED then GREEN:
    - `MakeableStatusBadge.test.tsx`: `status="green"` renders "Ready to make"; `status="yellow"` renders "Substitution needed"; `status="red"` renders "Missing ingredients" — all three strings reachable (unlike Patron's collapse), matching Barback's own three copy strings exactly.
    - `RecipeSearchFilter.test.tsx` — pure-logic cases first (an exported `filterRecipes(recipes, nameQuery, selectedTagIdsByGroup)` function, tested directly, no render): empty query + no tags selected returns every recipe; a name query matches case-insensitively and ignores leading/trailing whitespace (`'  MARTINI  '` matches a recipe named `'Martini'`); selecting two tags in the SAME group (e.g. two `spirit` tags) returns recipes carrying EITHER one (OR within a group); selecting one tag in `spirit` AND one tag in `flavor` returns only recipes carrying tags in BOTH groups (AND across groups); a name query AND a tag selection combine restrictively (a recipe must satisfy both). Interaction cases: rendering the component shows a name search input and one expandable section per tag group (Spirit/Type/Season/Flavor, in `TAG_GROUP_ORDER`); tapping "Apply" calls `onApply` with the filtered set of matching recipe ids; tapping "Back" calls `onBack` without calling `onApply`.
    - `RecipeOrOrderDetail.test.tsx`: renders the recipe name, the `MakeableStatusBadge` for `recipe.overallStatus`, every ingredient line's quantity/unit/categoryName (or ingredientName when locked), every method step in order, the glassware name (or "None specified" when null), and the garnish text ONLY when non-null (omitted entirely when null, never "None specified" — mirrors Barback's own garnish-vs-glassware asymmetry). When no `order` prop is passed, no Done button renders. When an `order` prop IS passed with `status: 'new'` or `'in_progress'`, a "Done" button renders and clicking it calls the `onMarkDone` prop. When `order.status === 'done'`, no Done button renders (already complete). When `order` is passed, the caption "Ordered {elapsedSeconds formatted} ago" (or "...for {patronName}" when non-null) renders.
  </behavior>
  <action>
    `apps/bartender/src/components/MakeableStatusBadge.tsx` (new) — a direct copy of `apps/barback/src/components/MakeableStatusBadge.tsx`'s three-branch `Tag` rendering (green "Ready to make" / yellow "Substitution needed" / red "Missing ingredients", same `CheckCircleOutlined`/`WarningOutlined`/`CloseCircleOutlined` icons and `success`/`warning`/`error` antd `Tag` colors) — a second, independent copy rather than importing from `apps/barback` (the two apps share no runtime code beyond `@my-bar/shared`, matching this monorepo's existing structural convention of small UI components being duplicated per-app rather than extracted into a shared React component library).

    `apps/bartender/src/components/RecipeSearchFilter.tsx` (new). Pure logic ahead of the component (Interface-First, mirrors `TagRail.tsx`'s convention): export `filterRecipes(recipes: Recipe[], nameQuery: string, selectedTagIdsByGroup: Record<TagGroup, Set<string>>): Recipe[]` — `const normalizedQuery = nameQuery.trim().toLowerCase()`; a recipe passes the name check if `normalizedQuery === '' || recipe.name.toLowerCase().includes(normalizedQuery)`; a recipe passes the tag check if, for EVERY group in `TAG_GROUP_ORDER` where `selectedTagIdsByGroup[group].size > 0`, `recipe.tags.some((t) => t.group === group && selectedTagIdsByGroup[group].has(t.id))` is true (a group with zero selections imposes no constraint — vacuously true); return `recipes.filter((r) => passesNameCheck(r) && passesTagCheck(r))`. Component `RecipeSearchFilter({ recipes, onApply, onBack }: { recipes: Recipe[]; onApply: (matchingIds: Set<string>) => void; onBack: () => void })`: fetches `useTags()`; local state `nameQuery` (string) and `selectedTagIdsByGroup` (one `Set<string>` per `TagGroup`, all initially empty); a full-screen overlay (mirrors `apps/barback/src/components/SearchFilterBar.tsx`'s sticky-header shape, adapted to full-screen per D-62) with a back button, heading "Search & Filter", a name `Input` (placeholder "Recipe name…", `autoFocus`) bound to `nameQuery`; below it, one expandable section per `TAG_GROUP_ORDER` group heading "Filter by tags" with the group's tags (from `useTags()`, filtered by `group`) rendered as toggleable chips that add/remove from that group's `Set`; a bottom bar with "Clear" (resets both state values to their initial empty values, does not call `onApply`/`onBack`) and "Apply" (`onClick={() => onApply(new Set(filterRecipes(recipes, nameQuery, selectedTagIdsByGroup).map((r) => r.id)))}`).

    `apps/bartender/src/components/RecipeOrOrderDetail.tsx` (new). `RecipeOrOrderDetail({ recipe, order, onBack, onMarkDone }: { recipe: Recipe; order?: { patronName: string | null; status: OrderStatus; elapsedSeconds: number }; onBack: () => void; onMarkDone?: () => void })`. Mirrors `apps/barback/src/components/views/RecipeDetailView.tsx`'s exact structure (uses the shared `FullScreenHeader` component from Barback? — NO: Bartender does not import from `apps/barback`, so re-implement the same simple flex header inline, matching `FullScreenHeader.tsx`'s visual shape — back button left, centered title, empty right spacer): `<MakeableStatusBadge status={recipe.overallStatus} />`; the red "Can't make this right now. Missing: {names}." sentence when `overallStatus === 'red'`; the yellow per-line substitution-available sentence for every line with `status === 'yellow'` (identical copy pattern to Barback's `RecipeDetailView`: "The {ingredientName} you selected is out of stock, but {alternativeIngredientName} is available."); an `antd List` of ingredient lines showing `{quantity} {unit} {categoryName}` with a small colored dot per line's own `status` (green/yellow/red, same `statusColor()` helper pattern as Barback's); an ordered list of `method` steps; glassware (`glassware.name` or "None specified" fallback); garnish (only when non-null, no fallback text). When `order` is provided AND `order.status !== 'done'`: a caption `` `Ordered ${formatElapsed(order.elapsedSeconds)} ago` `` (import `formatElapsed` from `./OrdersTab.js`, the sibling component file where Plan 04-01 defined and exported it — a cross-component named-export reuse, not a new util module) plus, when `order.patronName` is non-null, appended `` ` for ${order.patronName}` ``; and a full-width "Done" button (`onClick={onMarkDone}`) below it. When `order` is undefined, or `order.status === 'done'`, no caption and no Done button render at all — this task wires the FULL conditional-render contract even though no caller yet supplies a real `order`/`onMarkDone` (Plan 04-04's `OrdersTab` is the first real caller); `RecipesTab`'s own call site (Task 1) never passes those two props, which is the correct, non-stub absence of applicability for a recipe-only lookup, not an unfinished feature.
  </action>
  <verify>
    <automated>pnpm --filter bartender test -- MakeableStatusBadge RecipeSearchFilter RecipeOrOrderDetail</automated>
  </verify>
  <acceptance_criteria>
    - All three RED-phase test files pass, including every `filterRecipes` AND/OR combination case and every `RecipeOrOrderDetail` conditional-Done-button case
    - `grep -c "'Substitution needed'" apps/bartender/src/components/MakeableStatusBadge.tsx` is at least 1 — the yellow branch is genuinely reachable, unlike Patron's 2-state collapse
    - `grep -c "export function filterRecipes" apps/bartender/src/components/RecipeSearchFilter.tsx` is 1
    - `pnpm --filter bartender test -- MakeableStatusBadge RecipeSearchFilter RecipeOrOrderDetail` passes
    - `git log` shows a `test(04-02): ...` commit preceding a `feat(04-02): ...` commit for this task
  </acceptance_criteria>
  <done>A bartender can search/filter recipes by name and any combination of the four tag groups, tap into a recipe (from RecipesTab) or an order (once Plan 04-04 wires that entry point) and see the full tri-state makeable status plus every field needed to make the drink.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Bartender client → GET /api/recipes, GET /api/tags | Read-only; this plan introduces zero new mutation endpoints |
| RecipeSearchFilter's client-side filtering | Purely local array filtering over already-fetched, already-trusted server data — no new request surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Elevation of Privilege | Bartender console scope creep (accidentally wiring a mutation control) | medium | mitigate | This plan's own prohibition (see `must_haves.prohibitions`) — no create/edit/delete affordance exists anywhere in `RecipesTab`/`RecipeOrOrderDetail`/`RecipeSearchFilter`; code review at commit time is the enforcement mechanism, since there is no automated negative test that can prove the absence of a button that was never written |
| T-04-08 | Information Disclosure | Full tri-state (including yellow/substitution wording) now visible on a second, unauthenticated screen | low | accept | Matches the project's existing no-auth posture (T-01-04/T-03-12) — Bartender and Barback are both already unauthenticated LAN devices; D-63 is a deliberate decision to show bartenders more detail than guests, not a new exposure beyond what Barback already displays |
| T-04-09 | Denial of Service | Unbounded recipe/tag list rendering on an 8" device | low | accept | antd `List`'s own rendering is the existing mitigation; at this project's real scale (a personal recipe collection, not a seeded generic database — `REQUIREMENTS.md`'s Out of Scope) hundreds of recipes is not a realistic near-term volume |

</threat_model>

## Artifacts This Plan Produces

**New files** — `apps/bartender/src/api/{useRecipes.ts,useTags.ts}`, `apps/bartender/src/components/{BottomTabBar.tsx,BottomTabBar.test.tsx,RecipesTab.tsx,RecipesTab.test.tsx,RecipeSearchFilter.tsx,RecipeSearchFilter.test.tsx,MakeableStatusBadge.tsx,MakeableStatusBadge.test.tsx,RecipeOrOrderDetail.tsx,RecipeOrOrderDetail.test.tsx}`

**Exported symbols** — `apps/bartender`: `useRecipes`, `useTags`, `BottomTabBar`, `RecipesTab`, `filterRecipes`, `RecipeSearchFilter`, `MakeableStatusBadge`, `RecipeOrOrderDetail`

**Modified** — `apps/bartender/src/App.tsx` (full two-tab shell, live badge count, replaces 04-01's single-view body)

<verification>
- `pnpm --filter bartender test` passes (full suite, including this plan's five new test files)
- `pnpm --filter bartender build` passes
- Manual: opening /bartender/ on the target 8" viewport shows both tabs usable with no cut-off buttons or overlapping text
</verification>

<success_criteria>
BART-01, BART-05, and BART-06 are closed: a bartender can look up any recipe's full detail (with the real tri-state makeable status) and can search/filter the catalog by name and any combination of the four tag groups.
</success_criteria>

<output>
Create `.planning/phases/04-bartender-console-order-workflow/04-02-SUMMARY.md` when done
</output>
