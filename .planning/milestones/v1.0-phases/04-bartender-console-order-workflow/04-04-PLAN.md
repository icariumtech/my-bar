---
phase: 04-bartender-console-order-workflow
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - apps/bartender/src/components/OrdersTab.tsx
  - apps/bartender/src/components/OrdersTab.test.tsx
  - apps/bartender/src/api/useOpenOrder.ts
  - apps/bartender/src/api/useOpenOrder.test.ts
  - apps/bartender/src/api/useMarkOrderDone.ts
  - apps/bartender/src/api/useMarkOrderDone.test.ts
autonomous: true
requirements: [BART-02, BART-03, BART-04]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "batchOrders() groups the flat order list by (recipe.id, status) — identical pending orders for the same recipe in the same status collapse into one row with a count, aggregated patronNames, and the MAXIMUM elapsedSeconds across the group (the oldest member sets the row's displayed age); a recipe with 2 'new' orders and 1 'in_progress' order renders as TWO separate batch rows, never merged across statuses (D-58, BART-04 adjacency edge)"
    - "batchOrders() on an empty input returns an empty array; a single unbatched order still renders as a count-of-1 row with no visible '×1' badge (only count>1 shows the '×N' suffix)"
    - "Batch rows are sorted by elapsedSeconds descending (oldest first), matching D-62's stated Orders-tab sort order; ties (equal elapsedSeconds) fall back to a stable secondary sort by the batch's first orderId, matching the ordering guarantee GET /api/orders itself already provides"
    - "Tapping a batch row with status 'new' calls useOpenOrder for every individual order id in that batch (auto-advancing all of them to 'in_progress' at once, D-57) before navigating to RecipeOrOrderDetail; tapping an already-'in_progress' batch does NOT re-call useOpenOrder (idempotent-safe either way, but this avoids redundant network calls)"
    - "The Done button inside RecipeOrOrderDetail, when reached via an Orders-tab batch, marks EVERY individual order id in that batch Done in one action (D-58's 'one Done button... clears all of them at once') — proven by asserting useMarkOrderDone's mutation is invoked once per orderId in the batch, not once for the whole batch"
    - "The batch's patronNames render as a comma-joined list inside the detail caption (D-58: 'the individual who's-this-for names are listed inside'); when patronNames is empty (nobody named), no 'for ...' suffix renders at all"
    - statement: "A very long patronNames join (many guests ordering the same drink) is truncated via CSS (max-width + text-overflow) rather than a fixed name-count cutoff, with the full untruncated list available via a native title attribute — no automated test proves the visual truncation itself, only that the full comma-joined string is present in the DOM/title (UI-SPEC long-text backstop)"
      verification: backstop
    - "After a Done action, the ['orders'] query is invalidated (onSettled, not onSuccess — matching this codebase's established invalidate-on-settle convention) so the Orders list reflects the real server state even if one of the batch's N mutation calls failed partway through"
  artifacts:
    - path: "apps/bartender/src/components/OrdersTab.tsx"
      provides: "Full Orders tab: batching, elapsed-time display (reusing 04-01's formatElapsed), tap-to-detail with auto-advance, Done wiring"
    - path: "apps/bartender/src/api/useOpenOrder.ts"
      provides: "PATCH /orders/:id/start mutation, invalidates ['orders'] onSettled"
      exports: ["useOpenOrder"]
    - path: "apps/bartender/src/api/useMarkOrderDone.ts"
      provides: "PATCH /orders/:id/done mutation, invalidates ['orders'] onSettled"
      exports: ["useMarkOrderDone"]
  key_links:
    - from: "apps/bartender/src/components/OrdersTab.tsx"
      to: "apps/bartender/src/components/RecipeOrOrderDetail.tsx"
      via: "tapping a batch row navigates to <RecipeOrOrderDetail recipe={batch.recipe} order={...} onMarkDone={...} onBack={...} /> — the first real caller supplying order/onMarkDone (04-02 only wired the conditional-render contract)"
      pattern: "RecipeOrOrderDetail"
    - from: "apps/bartender/src/api/useMarkOrderDone.ts"
      to: "apps/server/src/routes/orders.ts"
      via: "PATCH /orders/:id/done, one call per individual order id in the tapped batch"
      pattern: "useMarkOrderDone"
  prohibitions: []
---

<objective>
Complete the Orders tab (Plan 04-01 built a real but flat, non-interactive list): batch identical pending orders per D-58, wire tap-to-detail navigation into `RecipeOrOrderDetail` (built in Plan 04-02) with a real `order`/`onMarkDone` pair for the first time, auto-advance a batch to `in_progress` on open (D-57), and let one Done tap clear an entire batch at once using Plan 04-03's PATCH endpoints.

Purpose: this is the last piece of the bartender's actual daily workflow — receive, open, fulfill, clear — closing BART-02/03/04's remaining UI surface on top of the already-proven backend.
Output: `OrdersTab.tsx` rewritten with `batchOrders()` + full interaction wiring; `useOpenOrder`/`useMarkOrderDone` hooks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-bartender-console-order-workflow/04-CONTEXT.md
@.planning/phases/04-bartender-console-order-workflow/04-UI-SPEC.md
@.planning/phases/04-bartender-console-order-workflow/04-01-PLAN.md
@.planning/phases/04-bartender-console-order-workflow/04-02-PLAN.md
@.planning/phases/04-bartender-console-order-workflow/04-03-PLAN.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Nothing else in this phase depends on them (this is the phase's last plan). -->

apps/bartender/src/api additions:

  useOpenOrder()     : UseMutationResult<Order, Error, string>   — PATCH /orders/:id/start, invalidates ['orders'] onSettled
  useMarkOrderDone()  : UseMutationResult<Order, Error, string>   — PATCH /orders/:id/done, invalidates ['orders'] onSettled

apps/bartender/src/components/OrdersTab.tsx additions:

  interface BatchedOrder { recipe: Recipe; status: OrderStatus; count: number; patronNames: string[]; elapsedSeconds: number; orderIds: string[] }
  batchOrders(orders: Order[]): BatchedOrder[]
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: batchOrders() + Orders tab batching UI + auto-advance-on-open</name>
  <files>apps/bartender/src/api/useOpenOrder.ts, apps/bartender/src/api/useOpenOrder.test.ts, apps/bartender/src/components/OrdersTab.tsx, apps/bartender/src/components/OrdersTab.test.tsx</files>
  <read_first>
    apps/bartender/src/components/OrdersTab.tsx (04-01's flat-list version this task rewrites)
    apps/bartender/src/components/RecipeOrOrderDetail.tsx (04-02 — the order/onMarkDone prop contract this task's caller supplies)
    apps/barback/src/api/useIngredients.ts (useUpdateIngredient — the onSettled invalidation convention)
    .planning/phases/04-bartender-console-order-workflow/04-CONTEXT.md (D-57, D-58, D-59)
    .planning/phases/04-bartender-console-order-workflow/04-RESEARCH.md ("Bartender Orders Tab with Batching" code example, Pitfall 6)
  </read_first>
  <behavior>
    RED (write first, must fail):
    - `batchOrders([])` returns `[]`.
    - `batchOrders` given 3 orders for the SAME recipe id, all `status: 'new'`, with distinct `patronName`s (`'Alice'`, `'Bob'`, `null`) and distinct `elapsedSeconds` (10, 300, 45): returns ONE batch with `count: 3`, `patronNames: ['Alice', 'Bob']` (the `null` is excluded, never rendered as a literal `"null"` string), `elapsedSeconds: 300` (the maximum), and `orderIds` containing all 3 original ids.
    - `batchOrders` given 2 orders for the same recipe id where one is `status: 'new'` and the other is `status: 'in_progress'`: returns TWO separate batches (never merged across statuses), each with `count: 1`.
    - `batchOrders` given orders for two DIFFERENT recipes: returns batches sorted by `elapsedSeconds` descending (the batch containing the oldest order first).
    - `useOpenOrder.test.ts` (mock `apiFetch`): calling the mutation with an order id issues a PATCH to `` `/orders/${id}/start` ``; on settle (success OR failure), `queryClient.invalidateQueries` is called with `{ queryKey: ['orders'] }`.
    - `OrdersTab.test.tsx` (mock `useOrders`, `useOpenOrder`, and `RecipeOrOrderDetail` itself): a batch with `count: 3` renders its recipe name followed by `'×3'`; a batch with `count: 1` renders the recipe name with NO `'×N'` suffix; tapping a `'new'`-status batch row calls the mocked `useOpenOrder`'s mutate function once per id in that batch's `orderIds`, THEN renders `RecipeOrOrderDetail` (assert it received `recipe` matching the batch's recipe and an `order` prop derived from the batch); tapping an `'in_progress'`-status batch row does NOT call `useOpenOrder`'s mutate function at all, and still renders `RecipeOrOrderDetail`.
    GREEN: implement `batchOrders()` and the rewired `OrdersTab` until every test above passes.
  </behavior>
  <action>
    `apps/bartender/src/api/useOpenOrder.ts` (new). `export function useOpenOrder() { const queryClient = useQueryClient(); return useMutation({ mutationFn: (orderId: string) => apiFetch<Order>(\`/orders/${orderId}/start\`, { method: 'PATCH' }), onSettled: () => { queryClient.invalidateQueries({ queryKey: ['orders'] }) } }) }` (import `useMutation, useQueryClient` from `@tanstack/react-query`, `Order` from `@my-bar/shared`, `apiFetch` from `./client.js`) — `onSettled` (not `onSuccess`) matches this codebase's established convention (`apps/barback/src/api/useIngredients.ts`'s mutations, `apps/bartender/src/api/useMarkOrderDone.ts` from Task 2 below) so the list re-syncs to server truth even on a failed transition.

    `apps/bartender/src/components/OrdersTab.tsx` (rewrite, keep `formatElapsed` unchanged and exported exactly as Plan 04-01 defined it — nothing about elapsed-time formatting changes in this plan). Add `interface BatchedOrder { recipe: Recipe; status: OrderStatus; count: number; patronNames: string[]; elapsedSeconds: number; orderIds: string[] }` and export `function batchOrders(orders: Order[]): BatchedOrder[]`: reduce into a `Record<string, BatchedOrder>` keyed by `` `${order.recipe.id}:${order.status}` ``; on first sight of a key, seed `{ recipe: order.recipe, status: order.status, count: 0, patronNames: [], elapsedSeconds: 0, orderIds: [] }`; for every order, `count += 1`, `if (order.patronName) patronNames.push(order.patronName)`, `elapsedSeconds = Math.max(elapsedSeconds, order.elapsedSeconds)`, `orderIds.push(order.id)`; return `Object.values(grouped).sort((a, b) => b.elapsedSeconds - a.elapsedSeconds || a.orderIds[0].localeCompare(b.orderIds[0]))` (the `localeCompare` tiebreaker on the first orderId is the stable secondary sort for equal `elapsedSeconds`, mirroring `GET /api/orders`'s own `asc(id)` tiebreaker at the API layer).

    `OrdersTab()` component: `const { data: rawOrders = [], isLoading, isError, refetch } = useOrders()`; `const openOrder = useOpenOrder()`; `const [view, setView] = useState<'list' | 'detail'>('list')`; `const [viewingBatch, setViewingBatch] = useState<BatchedOrder>()`; `const batches = useMemo(() => batchOrders(rawOrders), [rawOrders])`. Keep the existing loading/error/empty renders from Plan 04-01 unchanged (same copy, same conditions), now driven off `batches.length === 0` instead of `rawOrders.length === 0` (equivalent when the underlying array is empty). Define `function openBatch(batch: BatchedOrder) { if (batch.status === 'new') { batch.orderIds.forEach((id) => openOrder.mutate(id)) } setViewingBatch(batch); setView('detail') }`. If `view === 'detail' && viewingBatch`, return `<RecipeOrOrderDetail recipe={viewingBatch.recipe} order={{ patronName: viewingBatch.patronNames.length > 0 ? viewingBatch.patronNames.join(', ') : null, status: viewingBatch.status, elapsedSeconds: viewingBatch.elapsedSeconds }} onBack={() => setView('list')} onMarkDone={() => setView('list')} />` — this task's own RED/GREEN cycle covers batching and navigation only, so `onMarkDone` here just returns to the list; Task 2 (same plan, next commit) replaces this exact line with the real `useMarkOrderDone`-backed handler that also clears every order in the batch, per its own separate RED/GREEN cycle. Otherwise (list view): render each batch as a `List.Item` with `onClick={() => openBatch(batch)}`, the recipe name, a `'×{count}'` suffix ONLY when `count > 1`, the joined `patronNames` (when non-empty) inside a `title={patronNames.join(', ')}` attribute on a `max-width` truncating `<span>` (the long-text backstop truth), and `formatElapsed(elapsedSeconds)`.
  </action>
  <verify>
    <automated>pnpm --filter bartender test -- useOpenOrder OrdersTab</automated>
  </verify>
  <acceptance_criteria>
    - All RED-phase test cases above pass, including the cross-status non-merge case and the sort-by-elapsedSeconds-descending case
    - `grep -c "export function batchOrders" apps/bartender/src/components/OrdersTab.tsx` is 1
    - `grep -c "batch.status === 'new'" apps/bartender/src/components/OrdersTab.tsx` is at least 1 — auto-advance is gated correctly
    - `pnpm --filter bartender test -- useOpenOrder OrdersTab` passes
    - `git log` shows a `test(04-04): ...` commit preceding a `feat(04-04): ...` commit for this task
  </acceptance_criteria>
  <done>Identical pending orders for the same recipe collapse into one row; opening a new batch auto-advances every order in it to in_progress; the detail view receives real batch-derived order data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useMarkOrderDone + wire the batch-clearing Done button</name>
  <files>apps/bartender/src/api/useMarkOrderDone.ts, apps/bartender/src/api/useMarkOrderDone.test.ts, apps/bartender/src/components/OrdersTab.tsx, apps/bartender/src/components/OrdersTab.test.tsx</files>
  <precondition>Task 1's batchOrders/OrdersTab rewrite is committed — this task wires the onMarkDone callback Task 1 left for it.</precondition>
  <read_first>
    apps/bartender/src/components/OrdersTab.tsx (Task 1's version, including the openBatch/viewingBatch state this task extends)
    apps/bartender/src/api/useOpenOrder.ts (this plan's own Task 1 — identical mutation shape to mirror)
  </read_first>
  <behavior>
    RED then GREEN:
    - `useMarkOrderDone.test.ts` (mock `apiFetch`): calling the mutation with an order id issues a PATCH to `` `/orders/${id}/done` ``; on settle, `queryClient.invalidateQueries` is called with `{ queryKey: ['orders'] }`.
    - `OrdersTab.test.tsx` addition: opening a batch of 3 orders (from Task 1's setup) and clicking the rendered "Done" button (inside the mocked `RecipeOrOrderDetail`'s `onMarkDone` invocation, or by directly invoking the prop the component captured) calls the mocked `useMarkOrderDone`'s mutate function exactly 3 times — once per `orderId` in that batch, never once for the whole batch as a single call.
  </behavior>
  <action>
    `apps/bartender/src/api/useMarkOrderDone.ts` (new) — identical shape to `useOpenOrder.ts`: `export function useMarkOrderDone() { const queryClient = useQueryClient(); return useMutation({ mutationFn: (orderId: string) => apiFetch<Order>(\`/orders/${orderId}/done\`, { method: 'PATCH' }), onSettled: () => { queryClient.invalidateQueries({ queryKey: ['orders'] }) } }) }`.

    `apps/bartender/src/components/OrdersTab.tsx` (extend Task 1's version). `const markDone = useMarkOrderDone()`. Replace Task 1's `onMarkDone={() => setView('list')}` on `<RecipeOrOrderDetail>` with the real handler: `onMarkDone={() => { viewingBatch.orderIds.forEach((id) => markDone.mutate(id)); setView('list') }}` — every individual order id in the currently-viewed batch is marked done (D-58's "one Done button... clears all of them at once"), and the view returns to the list immediately (does not wait for all N mutations to settle before navigating back — each mutation's own `onSettled` independently triggers the `['orders']` refetch that will reflect the change).
  </action>
  <verify>
    <automated>pnpm --filter bartender test -- useMarkOrderDone OrdersTab</automated>
  </verify>
  <acceptance_criteria>
    - Both RED-phase test cases pass, including the exactly-3-calls-for-a-3-order-batch assertion
    - `grep -c "viewingBatch.orderIds.forEach" apps/bartender/src/components/OrdersTab.tsx` is at least 1
    - `pnpm --filter bartender test` (full Bartender suite) passes with zero regressions across every prior plan's test file
    - `git log` shows a `test(04-04): ...` commit preceding a `feat(04-04): ...` commit for this task
  </acceptance_criteria>
  <done>Tapping Done on a batched order entry clears every individual order in that batch in one action, and the Orders tab reflects the change without a manual refresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Bartender client → PATCH /api/orders/:id/start, /done (N calls per batch action) | Same unauthenticated write path Plan 04-03 already threat-modeled; this plan is a client-side UI wiring change, not a new server-side surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Denial of Service | A large batch (many identical pending orders) triggering N simultaneous PATCH calls on open/Done | low | accept | Bounded by realistic party size at a home bar (per-order patronName is a free-text guest count, not a mass-order system) — N is realistically single digits; both endpoints are cheap, idempotent, single-row updates |
| T-04-14 | Information Disclosure | Multiple guest names joined and displayed together on a shared batch row | low | accept | Matches the existing no-auth, single-shared-screen posture already established for `patronName` in Plan 04-01 (T-04-04) — this plan only changes HOW multiple already-visible names are grouped for display, not what is exposed |

</threat_model>

## Artifacts This Plan Produces

**New files** — `apps/bartender/src/api/{useOpenOrder.ts,useOpenOrder.test.ts,useMarkOrderDone.ts,useMarkOrderDone.test.ts}`

**Exported symbols** — `apps/bartender`: `useOpenOrder`, `useMarkOrderDone`, `batchOrders` (from `OrdersTab.tsx`)

**Modified** — `apps/bartender/src/components/OrdersTab.tsx` (full rewrite: batching, tap-to-detail, auto-advance, Done wiring)

<verification>
- `pnpm --filter bartender test` passes (full suite across all four Bartender-touching plans in this phase, zero regressions)
- `pnpm --filter bartender build` passes
- Manual: submit 3 identical orders from Patron, confirm they collapse into one "×3" row on Bartender, confirm opening it and tapping Done clears all 3
</verification>

<success_criteria>
BART-02, BART-03, and BART-04 are fully closed on the Bartender UI side: the Orders tab batches identical pending orders, opening one auto-advances it, and one Done action clears an entire batch — completing the phase's core "receive, fulfill, clear" bartender workflow.
</success_criteria>

<output>
Create `.planning/phases/04-bartender-console-order-workflow/04-04-SUMMARY.md` when done
</output>
