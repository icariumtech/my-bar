---
phase: 02-recipe-collection-makeable-engine
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/shared/src/glassware.ts
  - packages/shared/src/index.ts
  - apps/server/src/routes/glassware.ts
  - apps/server/src/routes/glassware.test.ts
  - apps/server/src/index.ts
autonomous: true
requirements: [RECIPE-01]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Owner can list, create, and rename glassware entries via GET/POST/PATCH /api/glassware, mirroring the categories CRUD pattern exactly (D-17)"
    - "Glassware names are unique — creating or renaming onto an existing name is refused with 409, never a duplicate row"
    - "Deleting a glassware entry still referenced by any recipe is refused with 409 and an accurate recipe count, using the exact 'This glassware is used by N recipe(s) — remove or reassign them first.' copy from 02-UI-SPEC.md (D-22)"
    - "Deleting an unreferenced glassware entry succeeds with 204"
    - "A race between a glassware delete and a concurrent recipe insert referencing it is caught and re-counted, never allowed to silently delete a now-referenced glassware entry"
  artifacts:
    - path: "packages/shared/src/glassware.ts"
      provides: "glasswareInput, glassware Zod schemas + inferred types"
    - path: "apps/server/src/routes/glassware.ts"
      provides: "GET/POST/PATCH/DELETE /api/glassware, registered at /api/glassware"
  key_links:
    - from: "apps/server/src/routes/glassware.ts"
      to: "apps/server/src/db/schema.ts"
      via: "sql`count(*)` against recipes filtered by glasswareId, gating the DELETE handler"
      pattern: "eq\\(recipes\\.glasswareId"
    - from: "apps/server/src/index.ts"
      to: "apps/server/src/routes/glassware.ts"
      via: "app.register(glasswareRoutes, { prefix: '/api/glassware' })"
      pattern: "glasswareRoutes"
  prohibitions:
    - "Glassware deletion must never succeed while any recipe references it via glasswareId (D-22) — the route's pre-count is the real UX-facing block; the schema's onDelete: 'set null' on recipes.glasswareId is a defensive fallback only, never the primary mechanism"
    - "Glassware names must never be validated as free text without the unique constraint — the same typo-drift risk D-01 already ruled out for categories applies here"
---

<objective>
Give the owner a curated, owner-managed glassware list — add/rename/delete, with the same usage-guard-on-delete discipline Phase 1 established for categories — as the backend half of D-17/D-22. This runs in parallel with plan 02-02 (both depend only on 02-01's schema/shared foundation, touch disjoint files).

Purpose: D-17 requires glassware to be a curated list (not free text) so recipe glassware values stay consistent across the collection; D-22 requires the identical "refuse if in use" discipline D-03 already established for categories, applied to glassware's one referencing table (`recipes.glasswareId`).

Output: full glassware CRUD API, delete-guarded exactly like categories.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-recipe-collection-makeable-engine/02-CONTEXT.md
@.planning/phases/02-recipe-collection-makeable-engine/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Glassware shared contract + GET/POST/PATCH /api/glassware</name>
  <files>packages/shared/src/glassware.ts, packages/shared/src/index.ts, apps/server/src/routes/glassware.ts, apps/server/src/routes/glassware.test.ts, apps/server/src/index.ts</files>
  <read_first>
    apps/server/src/routes/categories.ts
    packages/shared/src/category.ts
    apps/server/src/db/schema.ts (glassware table, added in 02-01)
    apps/server/src/index.ts
  </read_first>
  <behavior>
    - GET /api/glassware: `[]` on empty DB; else glassware rows ordered by name asc, shape `{ id, name }`.
    - POST /api/glassware: `{ name: 'Coupe' }` → 201, `{ id, name: 'Coupe' }`.
    - POST /api/glassware with a duplicate name: 409, `error: 'Glassware already exists'`; GET afterward still shows exactly one row (no duplicate created).
    - PATCH /api/glassware/:id: renames, 200; renaming onto another existing glassware's name: 409; unknown id: 404.
  </behavior>
  <action>
    Create `packages/shared/src/glassware.ts` mirroring `packages/shared/src/category.ts` exactly: `glasswareInput = z.object({ name: z.string().trim().min(1).max(60) })`, `glassware = glasswareInput.extend({ id: z.string().uuid() })`, both types exported. Add `export * from './glassware.js'` to `packages/shared/src/index.ts`.

    Create `apps/server/src/routes/glassware.ts` as a `FastifyPluginAsync<{ db?: typeof defaultDb }>` structurally identical to `categoriesRoutes` in `categories.ts`: the same `db = opts.db ?? defaultDb` injection, the same `GET '/'` (select all, orderBy asc name), the same `POST '/'` (insert with generated uuid, catch `UNIQUE constraint failed` → `409 { error: 'Glassware already exists' }`), the same `PATCH '/:id'` (update name, catch UNIQUE → 409, empty SELECT after UPDATE → 404). Import `glassware` (table) from `../db/schema.js` and `glassware`/`glasswareInput` (Zod) from `@my-bar/shared` — these share the same identifier name across the two import sources exactly as `categories`/`category` do in `categories.ts` today; disambiguate the same way that file already does (Drizzle table import vs. Zod schema import coexist because Zod schemas are imported from `@my-bar/shared` and the Drizzle table from `../db/schema.js`, so name collision is avoided by only ever referencing one or the other per statement — follow `categories.ts`'s existing import structure exactly, it already solves this).

    Register in `apps/server/src/index.ts`: `import { glasswareRoutes } from './routes/glassware.js'` and `app.register(glasswareRoutes, { prefix: '/api/glassware' })`, alongside the existing route registrations.
  </action>
  <verify>
    <automated>pnpm --filter @my-bar/shared build && pnpm -F @my-bar/server test -- glassware.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared/src/glassware.ts` exports `glasswareInput`, `glassware`, `GlasswareInput`, `Glassware`
    - GET/POST/PATCH behaviors above pass as automated tests, mirroring categories.test.ts's structure
    - `git log` shows `test(02-03): ...` preceding `feat(02-03): ...`
  </acceptance_criteria>
  <done>Owner can list, create, and rename glassware entries via a real API, with duplicate names refused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DELETE /api/glassware/:id with recipe-usage guard (D-22)</name>
  <files>apps/server/src/routes/glassware.ts, apps/server/src/routes/glassware.test.ts</files>
  <read_first>
    apps/server/src/routes/glassware.ts (from Task 1)
    apps/server/src/routes/categories.ts (DELETE 127-176, pre-count + race-condition fallback pattern)
    apps/server/src/db/schema.ts (recipes.glasswareId FK)
  </read_first>
  <behavior>
    - DELETE /api/glassware/:id unreferenced by any recipe: 204; GET afterward shows it gone.
    - DELETE /api/glassware/:id referenced by 2 recipes: 409, `error: 'This glassware is used by 2 recipe(s) — remove or reassign them first.'`, body includes `recipeCount: 2`; both recipes still exist and still reference the glassware afterward.
    - DELETE on an unknown id: 404.
    - The FK-catch race-condition branch (a concurrent recipe insert lands between the pre-count and the delete) re-counts and returns the same 409 shape rather than trusting the stale pre-count.
  </behavior>
  <action>
    Add `DELETE '/:id'` to `apps/server/src/routes/glassware.ts`, mirroring `categories.ts`'s DELETE handler structure exactly: schema `params: z.object({ id: z.string().uuid() })`, response `{ 204: z.void(), 404: errorObj, 409: z.object({ error: z.string(), recipeCount: z.number() }) }`. Define `function inUseMessage(count: number) { return \`This glassware is used by ${count} recipe(s) — remove or reassign them first.\` }` (the exact D-22 copy from 02-UI-SPEC.md's Copywriting Contract). Pre-count: `sql<number>`count(*)`` against `recipes` (imported from `../db/schema.js`) filtered by `eq(recipes.glasswareId, id)`. If `recipeCount > 0`, return 409 before attempting the delete. Otherwise delete, with the same `try/catch` re-count-on-FK-failure fallback `categories.ts` uses for its own race condition (recipes' `glasswareId` FK is `onDelete: 'set null'`, not `restrict`, so a genuine FK failure here is unlikely in practice — but the pre-count-then-delete pair can still be raced by a concurrent recipe insert landing between the two statements, so the same defensive re-count-and-409 branch applies for consistency with the established pattern, even though the specific SQLite error text it would catch differs).
  </action>
  <verify>
    <automated>pnpm -F @my-bar/server test -- glassware.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All DELETE behaviors above pass as automated tests
    - The 409 refusal message matches 02-UI-SPEC.md's Copywriting Contract exactly, verified by an exact-string test assertion
    - `git log` shows `test(02-03): ...` / `feat(02-03): ...` commits for this task distinct from Task 1's
  </acceptance_criteria>
  <done>Glassware entries referenced by any recipe cannot be deleted; the refusal message states an accurate count using the exact contracted copy.</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `packages/shared/src/glassware.ts` — new exports: `glasswareInput`, `GlasswareInput`, `glassware`, `Glassware`
- `apps/server/src/routes/glassware.ts` — new export: `glasswareRoutes` (FastifyPluginAsync), registered at `/api/glassware` with `GET /`, `POST /`, `PATCH /:id`, `DELETE /:id`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Barback client → POST/PATCH/DELETE /api/glassware | Untrusted request body/params cross into the glassware curated list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | POST/PATCH /api/glassware name uniqueness | medium | mitigate | DB `UNIQUE` constraint on `glassware.name`, translated to fixed 409, mirroring D-01's category pattern |
| T-02-11 | Tampering | DELETE /api/glassware/:id vs. recipe references | high | mitigate | Pre-count against `recipes.glasswareId` before delete, with a race-condition re-count fallback on any concurrent-insert race |
| T-02-12 | Information Disclosure | glassware.ts error handling | medium | mitigate | UNIQUE/FK errors translated to fixed 409/404 bodies, never a raw 500 with SQLite's own error text |
</threat_model>

<verification>
- `pnpm -F @my-bar/server test -- glassware.test.ts` passes
- `git log --oneline --grep="^test(02-03)"` and `--grep="^feat(02-03)"` both return commits
</verification>

<success_criteria>
Owner can manage a curated glassware list end-to-end via the API, with deletion blocked whenever a recipe still references the entry.
</success_criteria>

<output>
Create `.planning/phases/02-recipe-collection-makeable-engine/02-03-SUMMARY.md` when done
</output>
