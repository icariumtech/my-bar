---
phase: 02-recipe-collection-makeable-engine
plan: 04
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - apps/barback/src/api/useGlassware.ts
  - apps/barback/src/components/GlasswareManager.tsx
  - apps/barback/src/App.tsx
autonomous: true
requirements: [RECIPE-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Owner can open a Glassware manager from the Barback header, add a new glassware type, rename one, and delete an unused one, entirely from the phone/iPad UI (D-17)"
    - "Attempting to delete a glassware entry still used by a recipe shows the exact refusal message from the server, including the real recipe count"
    - "Zero glassware types renders the empty state 'No glassware types yet' / 'Add glassware options for your recipes.' (02-UI-SPEC.md)"
    - "Renaming a glassware entry invalidates both the glassware list and the recipes list, so any recipe's displayed glassware name updates without a manual page refresh — mirrors D-03's category-rename-propagates-to-ingredients precedent"
  artifacts:
    - path: "apps/barback/src/api/useGlassware.ts"
      provides: "useGlassware, useCreateGlassware, useUpdateGlassware, useDeleteGlassware, DeleteGlasswareError"
    - path: "apps/barback/src/components/GlasswareManager.tsx"
      provides: "Add/rename/delete UI for the curated glassware list, mirroring CategoryManager.tsx"
  key_links:
    - from: "apps/barback/src/components/GlasswareManager.tsx"
      to: "apps/barback/src/api/useGlassware.ts"
      via: "useGlassware()/useCreateGlassware()/useUpdateGlassware()/useDeleteGlassware() hook calls"
      pattern: "useGlassware|useCreateGlassware|useDeleteGlassware"
    - from: "apps/barback/src/App.tsx"
      to: "apps/barback/src/components/GlasswareManager.tsx"
      via: "a header button toggling glasswareManagerOpen state, rendering <GlasswareManager open onClose>"
      pattern: "GlasswareManager"
  prohibitions:
    - "The Glassware button must never use the accent color (#22c55e) — 02-UI-SPEC.md reserves accent exclusively for primary CTAs (Add Recipe/Save Recipe/Add Step/Add Glassware-in-create-mode), the makeable badge, and active filter/tab state, not general navigation entry points"
---

<objective>
Give the owner a working Glassware management screen — add, rename, delete with a usage-guard refusal — reachable from the Barback header, backed by plan 02-03's API. This is the frontend half of D-17/D-22, built in parallel with plan 02-05 (recipe list + form sub-components) since both depend only on wave 2's backend and touch disjoint files.

Purpose: D-17 requires the glassware list to be owner-editable from the UI, not just the API; the delete-guard refusal (D-22) must be visible to the owner with the real count, not just enforced silently server-side.

Output: a fully functional Glassware manager, wired into the existing Barback header.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-recipe-collection-makeable-engine/02-CONTEXT.md
@.planning/phases/02-recipe-collection-makeable-engine/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useGlassware.ts — query + CRUD mutation hooks</name>
  <files>apps/barback/src/api/useGlassware.ts</files>
  <read_first>
    apps/barback/src/api/useCategories.ts
    packages/shared/src/glassware.ts
  </read_first>
  <action>
    Create `apps/barback/src/api/useGlassware.ts` mirroring `useCategories.ts` exactly: a `DeleteGlasswareError extends Error` class carrying an optional `recipeCount?: number` (named for glassware's field, not `ingredientCount`); `useGlassware()` (`useQuery({ queryKey: ['glassware'], queryFn: () => apiFetch<Glassware[]>('/glassware') })`); `useCreateGlassware()` (`useMutation` POSTing `GlasswareInput`, `onSettled` invalidates `['glassware']`); `useUpdateGlassware()` (PATCH rename, `onSettled` invalidates BOTH `['glassware']` AND `['recipes']` — a glassware rename changes the `glasswareName` label joined onto every recipe response, exactly as `useRenameCategory` invalidates both `['categories']` and `['ingredients']` for the identical reason); `useDeleteGlassware()` (raw `fetch` DELETE, not `apiFetch`, so the 409 body's `recipeCount` is readable — same reasoning `useDeleteCategory` documents for its own `ingredientCount`, `onSettled` invalidates `['glassware']`).
  </action>
  <verify>
    <automated>pnpm -F @my-bar/barback build</automated>
  </verify>
  <acceptance_criteria>
    - `useUpdateGlassware`'s `onSettled` invalidates both `['glassware']` and `['recipes']` query keys
    - `DeleteGlasswareError` carries `recipeCount` read from the DELETE response body, not a generic error message
    - `pnpm -F @my-bar/barback build` (typecheck + bundle) succeeds
  </acceptance_criteria>
  <done>useGlassware.ts exports a complete CRUD hook set, ready for GlasswareManager to consume.</done>
</task>

<task type="auto">
  <name>Task 2: GlasswareManager.tsx + Barback header entry point</name>
  <files>apps/barback/src/components/GlasswareManager.tsx, apps/barback/src/App.tsx</files>
  <read_first>
    apps/barback/src/components/CategoryManager.tsx
    apps/barback/src/App.tsx
    .planning/phases/02-recipe-collection-makeable-engine/02-UI-SPEC.md (Copywriting Contract, Color, Spacing)
  </read_first>
  <action>
    Create `apps/barback/src/components/GlasswareManager.tsx` as a structural copy of `CategoryManager.tsx` with every category-specific identifier and string replaced for glassware: `useCategories`/`useCreateCategory`/`useRenameCategory`/`useDeleteCategory`/`DeleteCategoryError` → `useGlassware`/`useCreateGlassware`/`useUpdateGlassware`/`useDeleteGlassware`/`DeleteGlasswareError`; modal title `"Manage Categories"` → `"Manage Glassware"`; empty state → `<Empty description="No glassware types yet — add glassware options for your recipes." />` (combining both empty-state lines from 02-UI-SPEC.md into the single `Empty` description, matching how `CategoryManager` already renders its own one-line empty description); the create button label `"Add Category"` → `"Add Glassware"` (exact 02-UI-SPEC.md Copywriting Contract copy); the delete refusal message construction reads `err.recipeCount` (not `err.ingredientCount`) and renders exactly `` `This glassware is used by ${err.recipeCount} recipe(s) — remove or reassign them first.` `` per the Copywriting Contract; the `maxLength` on the name `Input` changes from 60 (matches `glasswareInput`'s bound — same 60 as categories, no change needed there). Keep every `style={{ minHeight: 48 }}` tap-target and the exact same layout structure (List + Divider + add-row).

    Wire a "Glassware" entry point into `apps/barback/src/App.tsx`: add `const [glasswareManagerOpen, setGlasswareManagerOpen] = useState(false)` alongside the existing `categoryManagerOpen` state, add a header `<Button icon={<SettingOutlined />} style={{ minHeight: 48 }} onClick={() => setGlasswareManagerOpen(true)}>Glassware</Button>` next to the existing "Categories" button (secondary/default styling, NOT `type="primary"` — accent is reserved for primary CTAs per 02-UI-SPEC.md, and "Glassware" is a navigation/management entry point exactly like "Categories" already is), and render `<GlasswareManager open={glasswareManagerOpen} onClose={() => setGlasswareManagerOpen(false)} />` alongside the existing `<CategoryManager />` render.
  </action>
  <verify>
    <automated>pnpm -F @my-bar/barback build</automated>
  </verify>
  <acceptance_criteria>
    - GlasswareManager renders the empty state, add/rename/delete flows, and the exact refusal copy from 02-UI-SPEC.md
    - The "Glassware" header button uses default (not `type="primary"`) styling
    - `pnpm -F @my-bar/barback build` succeeds with no type errors
  </acceptance_criteria>
  <done>Owner can manage the glassware list end-to-end from the Barback header, with the exact contracted copy for empty state, create, and delete-refusal.</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `apps/barback/src/api/useGlassware.ts` — new exports: `DeleteGlasswareError`, `useGlassware`, `useCreateGlassware`, `useUpdateGlassware`, `useDeleteGlassware`
- `apps/barback/src/components/GlasswareManager.tsx` — new export: `GlasswareManager` (React component, props `{ open: boolean; onClose: () => void }`)
- `apps/barback/src/App.tsx` — adds `glasswareManagerOpen` state, a "Glassware" header button, and a `<GlasswareManager>` render

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GlasswareManager form inputs → POST/PATCH /api/glassware | Client-entered text crosses to the server; server-side Zod validation (plan 02-03) is the actual boundary, this UI only provides the affordance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Information Disclosure | Delete refusal rendering | low | accept | The refusal message surfaces a recipe count, which is non-sensitive operational data already visible to the same trusted-LAN owner via the recipe list itself |
</threat_model>

<verification>
- `pnpm -F @my-bar/barback build` succeeds
- Manual: open Barback, click "Glassware", add/rename/delete a test entry, confirm the empty state and refusal copy (covered by this phase's end-of-phase human-verify checkpoint)
</verification>

<success_criteria>
The owner can fully manage the glassware list from the Barback UI, matching 02-UI-SPEC.md's copy and color contract exactly.
</success_criteria>

<output>
Create `.planning/phases/02-recipe-collection-makeable-engine/02-04-SUMMARY.md` when done
</output>
