---
phase: 02.1-recipe-ui-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["02.1-02"]
files_modified:
  - apps/barback/src/components/pickers/CategoryPicker.tsx
  - apps/barback/src/components/pickers/CategoryPicker.test.tsx
  - apps/barback/src/components/views/AddEditIngredientView.tsx
  - apps/barback/src/components/views/AddEditIngredientView.test.tsx
  - apps/barback/src/components/IngredientsTab.tsx
  - apps/barback/src/components/AddEditIngredientForm.tsx
autonomous: true
requirements: [BARBACK-02, BARBACK-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tapping 'Add Ingredient' on the Ingredients tab opens a full-screen view (not a Modal) with a back button that returns to the ingredient list without saving"
    - "The ingredient's category field is a single autocomplete-with-inline-create text field — typing filters existing categories, and a '+ Add new' option appears only when the typed text matches no existing category name"
    - "Editing an existing ingredient opens the same full-screen view pre-filled with that ingredient's current name/category/note, submitted through the update path instead of create"
    - "Selecting '+ Add new \"X\"' in the category picker creates category X via the existing POST /api/categories endpoint and immediately selects it in the form — no separate screen or extra tap is required"
    - statement: "The category autocomplete picker's search is synchronous over the already-fetched in-memory category list, with no loading spinner of its own — only the form's own Save button shows a loading state on submit (UI-SPEC backstop: AutoComplete picker has no async loading state)"
      verification: backstop
  assumptions:
    - "BARBACK-03 unclassified edge item: the deterministic edge-probe could not classify a shape (adjacency/empty/ordering) for 'a single autocomplete field searching existing entries, with an inline add new option' — the requirement text doesn't map cleanly onto that taxonomy. Resolved by treating D-27/D-28/D-29 and the UI-SPEC's picker rows (search-as-you-type match behavior, inline-create-on-no-match, case-insensitive duplicate prevention per this plan's own prohibition) as the complete edge surface for this requirement — no further edge dimension is manufactured beyond what those already-authored specs cover."
  artifacts:
    - path: "apps/barback/src/components/pickers/CategoryPicker.tsx"
      provides: "Reusable autocomplete-with-inline-create picker for selecting or creating a category"
    - path: "apps/barback/src/components/views/AddEditIngredientView.tsx"
      provides: "Full-screen ingredient add/edit view with a back button, replacing the AddEditIngredientForm Modal"
  key_links:
    - from: "apps/barback/src/components/views/AddEditIngredientView.tsx"
      to: "apps/barback/src/components/pickers/CategoryPicker.tsx"
      via: "renders <CategoryPicker> as the categoryId Form.Item's child, replacing the old static <Select>"
      pattern: "<CategoryPicker"
    - from: "apps/barback/src/components/IngredientsTab.tsx"
      to: "apps/barback/src/components/views/AddEditIngredientView.tsx"
      via: "view === 'add' renders AddEditIngredientView with an onBack callback instead of the old Modal's open/onClose"
      pattern: "AddEditIngredientView"
  prohibitions:
    - "MUST NOT offer '+ Add new \"X\"' when a category named X already exists under a case-insensitive comparison — the picker's match check must be case-insensitive so a near-identical duplicate (e.g. 'Rum' vs 'rum') is surfaced as the existing option, never silently offered as a brand-new one; the categories table's UNIQUE constraint is case-SENSITIVE (SQLite's default TEXT collation), so this check must happen in the picker's own filtering logic, not left to the database to reject"
---

<objective>
Deliver the first complete full-screen add/edit flow of the phase: the owner can tap "Add Ingredient" on the Ingredients tab, get a real full-screen form (not a Modal) with a back button, and pick or create the ingredient's category through a single autocomplete field instead of a static dropdown (D-26/D-27, for the Ingredient entity). This is the template every later full-screen-view plan in this phase (Category/Glassware management, Recipe add/edit, Recipe detail) follows exactly.

Purpose: BARBACK-02/BARBACK-03 for the Ingredient entity — the simplest of the four entities being converted (name + category + note, no nested ingredient lines), making it the right place to prove the full-screen-view-with-back-button pattern and the autocomplete-picker pattern before the more complex Recipe form (a later plan) builds on both.

Output: `CategoryPicker` (new, reusable autocomplete-with-inline-create component); `AddEditIngredientView` (new, full-screen replacement for the `AddEditIngredientForm` Modal); `IngredientsTab` wired to the new view instead of the old Modal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-CONTEXT.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CategoryPicker — autocomplete-with-inline-create</name>
  <files>apps/barback/src/components/pickers/CategoryPicker.tsx, apps/barback/src/components/pickers/CategoryPicker.test.tsx</files>
  <read_first>
    apps/barback/src/components/AddEditIngredientForm.tsx
    apps/barback/src/api/useCategories.ts
    .planning/phases/02.1-recipe-ui-cleanup/02.1-RESEARCH.md (Pattern 2: Autocomplete-with-Inline-Create Picker)
  </read_first>
  <action>
    Create `apps/barback/src/components/pickers/CategoryPicker.tsx` exporting `interface CategoryPickerProps { value?: string; onChange?: (categoryId: string | undefined) => void }` and `export function CategoryPicker({ value, onChange }: CategoryPickerProps)` — this exact `value`/`onChange` prop shape is required so antd `Form.Item` can clone them onto this component directly, mirroring the fix already applied to `UnitDropdown`/`GlasswareSelector` in Phase 2 (G-02-6). Internally call `useCategories()` and `useCreateCategory()` (both already exist, unchanged). Use antd's `AutoComplete` component (not `Select` with `showSearch` — RESEARCH.md's explicit recommendation for inline-create workflows). Track local search text via `useState`. Build `options` from the categories list, each `{ value: category.id, label: category.name }`. When the trimmed search text is non-empty AND does not case-insensitively match any existing category name (`categories.some(c => c.name.toLowerCase() === trimmed.toLowerCase())` — this exact case-insensitive check is what the plan's prohibition requires), append one more option: `{ value: '__create__', label: \`+ Add "${trimmed}"\` }`. On select: if the selected value is `'__create__'`, call `createCategory.mutateAsync({ name: trimmed })`, then call `onChange?.(created.id)` and clear the search text — do not call `onChange` with the literal string `'__create__'`. Otherwise call `onChange?.(selectedValue)` directly. Render the currently-selected category's name (not its id) as the field's display value — look up `categories.find(c => c.id === value)?.name` for the `AutoComplete`'s own `value` display prop, distinct from the `onChange` payload (which is always the category id). Show a small inline error (mirroring `AddEditIngredientForm`'s existing `categoryError` pattern) if `createCategory` rejects.
  </action>
  <verify>
    <automated>pnpm --filter barback test -- CategoryPicker</automated>
  </verify>
  <acceptance_criteria>
    - Typing a substring of an existing category's name filters the option list to matches (case-insensitive)
    - Typing a name that exactly matches an existing category (any case) does NOT show a "+ Add" option — the existing option is offered instead
    - Typing a name matching no existing category shows exactly one "+ Add \"typed text\"" option; selecting it calls `useCreateCategory`'s mutation with that exact name and then calls `onChange` with the newly-created category's real id (never the literal `'__create__'`)
    - `pnpm --filter barback test -- CategoryPicker` passes
  </acceptance_criteria>
  <done>CategoryPicker is a working, tested, reusable field: search-filters existing categories and creates new ones inline without leaving the parent form.</done>
</task>

<task type="auto">
  <name>Task 2: AddEditIngredientView (full-screen) + wire into IngredientsTab</name>
  <files>apps/barback/src/components/views/AddEditIngredientView.tsx, apps/barback/src/components/views/AddEditIngredientView.test.tsx, apps/barback/src/components/IngredientsTab.tsx, apps/barback/src/components/AddEditIngredientForm.tsx</files>
  <reversibility rating="costly">Converts the ingredient add/edit shell from Modal to full-screen (D-26); reverting means restoring the Modal wrapper this plan removes.</reversibility>
  <read_first>
    apps/barback/src/components/AddEditIngredientForm.tsx
    apps/barback/src/components/IngredientsTab.tsx
    apps/barback/src/components/RecipeDetailView.tsx
    .planning/phases/02.1-recipe-ui-cleanup/02.1-UI-SPEC.md (Component Inventory: AddEditIngredientView)
    .planning/phases/02.1-recipe-ui-cleanup/02.1-PATTERNS.md (AddEditIngredientView section: Modal to full-screen conversion pattern)
  </read_first>
  <action>
    Create `apps/barback/src/components/views/AddEditIngredientView.tsx`, adapted from the current `AddEditIngredientForm.tsx`: same `interface AddEditIngredientViewProps { ingredient?: Ingredient; onBack: () => void }` (no `open`/`onClose` — full-screen views are always rendered when their parent's view-state says so, and there is no `open` prop; `onBack` replaces `onClose`). Keep the exact same `Form`, validation rules (`nameRules`, `noteRules`), `useCreateIngredient`/`useUpdateIngredient` submit logic, and the re-populate-on-ingredient-change `useEffect` — none of that changes. Remove the `<Modal>` wrapper; replace with: `<div style={{ display: 'flex', flexDirection: 'column', minHeight: '100vh' }}>` containing a header `<div style={{ display: 'flex', alignItems: 'center', gap: 8, padding: 16, borderBottom: '1px solid rgba(255,255,255,0.1)' }}>` with a `<Button type="text" onClick={onBack}>← Back</Button>` and `<h2 className="text-white">{isEditing ? 'Edit Ingredient' : 'Add Ingredient'}</h2>`, then a `<main style={{ flex: 1, padding: 16, overflow: 'auto' }}>` wrapping the existing `<Form>`. On successful submit, call `onBack()` instead of the old `onClose()`. Replace the `categoryId` `Form.Item`'s child from `<Select showSearch ... popupRender={renderCategoryPopup} />` to `<CategoryPicker />` (import from `../pickers/CategoryPicker.js`) — this REMOVES `renderCategoryPopup`, `handleCategorySearch`, `handleAddCategory`, `categorySearch`/`categoryError` local state, and the `useCreateCategory` import (all now owned inside `CategoryPicker` itself). Delete `apps/barback/src/components/AddEditIngredientForm.tsx` entirely — it is fully superseded by this new file, and leaving it in place would be dead code with a stale Modal-based, static-Select shell.

    In `IngredientsTab.tsx`, replace the import of `AddEditIngredientForm` with `AddEditIngredientView` (from `./views/AddEditIngredientView.js`), and change the render from `<AddEditIngredientForm ingredient={editing} open={view === 'add'} onClose={() => setView('list')} />` to conditional full-screen rendering: `{view === 'add' && <AddEditIngredientView ingredient={editing} onBack={() => setView('list')} />}`. When `view === 'add'`, do NOT also render the list body underneath — the full-screen view replaces the tab's entire content area (no double-rendering).
  </action>
  <verify>
    <automated>pnpm --filter barback test -- AddEditIngredientView</automated>
  </verify>
  <acceptance_criteria>
    - `AddEditIngredientView.tsx` renders no `role="dialog"` element (confirms it is not Modal-wrapped) and does render a "← Back" button
    - Clicking "← Back" without submitting calls `onBack` and does not call either mutation
    - Submitting a valid new ingredient calls `useCreateIngredient`'s mutation and then `onBack`
    - `apps/barback/src/components/AddEditIngredientForm.tsx` no longer exists (`test -f apps/barback/src/components/AddEditIngredientForm.tsx` exits non-zero)
    - `IngredientsTab.tsx`'s "Add Ingredient" button, when clicked, renders `AddEditIngredientView` full-screen with no ingredient list visible underneath it
    - `pnpm --filter barback test -- AddEditIngredientView` passes and `pnpm --filter barback build` succeeds with zero type errors
  </acceptance_criteria>
  <done>Ingredient add/edit is a genuine full-screen view with a working back button, using the new CategoryPicker for category selection — the first complete BARBACK-02/BARBACK-03 vertical slice in this phase.</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `apps/barback/src/components/pickers/CategoryPicker.tsx` — new export: `CategoryPicker({ value, onChange })`
- `apps/barback/src/components/views/AddEditIngredientView.tsx` — new export: `AddEditIngredientView({ ingredient, onBack })`
- `apps/barback/src/components/AddEditIngredientForm.tsx` — deleted (superseded by AddEditIngredientView)
- `apps/barback/src/components/IngredientsTab.tsx` — wired to the new full-screen view

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Barback client → POST /api/categories (via CategoryPicker's inline-create) | Untrusted category name now creatable from within the ingredient-add flow, not just CategoryManager |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-07 | Tampering | CategoryPicker inline-create bypassing the curated category list | medium | mitigate | Inline-create calls the existing `POST /api/categories` endpoint unchanged — the same server-side `.min(1).max(60)` validation and UNIQUE constraint that already govern CategoryManager's add flow apply identically here; no new server-side attack surface is introduced |
| T-02.1-08 | Tampering | Case-variant duplicate category creation via the picker (e.g. "Rum" vs "rum") | low | mitigate | The picker's own case-insensitive existing-match check (see prohibitions) prevents the UI from ever offering "+ Add" for a name that already exists under any casing — the DB's case-sensitive UNIQUE constraint remains the last-resort backstop for any bypass of the client-side check |
</threat_model>

<verification>
- `pnpm --filter barback test -- CategoryPicker AddEditIngredientView` passes
- `pnpm --filter barback build` succeeds with zero type errors
- Manual smoke: `pnpm dev`, Ingredients tab → Add Ingredient → full-screen form → type a new category name → "+ Add" appears → select it → category is created and selected → save → back to list
</verification>

<success_criteria>
The owner can add or edit an ingredient through a genuine full-screen view with a working back button, selecting or creating its category through a single searchable field — no modal, no static dropdown.
</success_criteria>

<output>
Create `.planning/phases/02.1-recipe-ui-cleanup/02.1-03-SUMMARY.md` when done
</output>
