---
phase: 02.1-recipe-ui-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["02.1-02"]
files_modified:
  - apps/barback/src/components/CategoryManager.tsx
  - apps/barback/src/components/CategoryManager.test.tsx
  - apps/barback/src/components/GlasswareManager.tsx
  - apps/barback/src/components/GlasswareManager.test.tsx
  - apps/barback/src/components/SettingsTab.tsx
autonomous: true
requirements: [BARBACK-02]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tapping 'Categories' from the Settings menu opens category management as a full-screen view (not a Modal) with a back button that returns to the Settings menu"
    - "Tapping 'Glassware' from the Settings menu opens glassware management as a full-screen view (not a Modal) with a back button that returns to the Settings menu"
    - "Category and glassware add/rename/delete behavior (inline add input, inline rename, refuse-only delete with the exact Phase 2 refusal copy) is unchanged by this conversion — only the Modal wrapper is removed, the internal management UX is identical"
  artifacts:
    - path: "apps/barback/src/components/CategoryManager.tsx"
      provides: "Category management as a full-screen view with a back button, replacing the Modal shell"
    - path: "apps/barback/src/components/GlasswareManager.tsx"
      provides: "Glassware management as a full-screen view with a back button, replacing the Modal shell"
---

<objective>
Convert the Settings tab's two management sub-pages — Categories and Glassware — from Modal-wrapped to full-screen views with a back button (D-24/D-26). This is the phase's simplest full-screen conversion: both `CategoryManager` and `GlasswareManager` already present a single flat, non-nested list-with-inline-add/rename UI (never modal-on-modal to begin with), so this plan is a mechanical shell swap with zero change to their internal add/rename/delete logic.

Per Claude's discretion (CONTEXT.md D-24 only locks that Categories/Glassware are each their own full-screen sub-page under Settings — it does not require add/edit to further split into a separate screen the way Ingredient/Recipe do): this plan converts `CategoryManager`/`GlasswareManager` to full-screen IN PLACE, keeping their existing inline-add-at-the-bottom-of-the-list and inline-rename-in-row interactions, rather than introducing separate `AddEditCategoryView`/`AddEditGlasswareView` files. That inline UX is not modal-on-modal today and satisfies D-26's actual concern (no nested modal) once un-wrapped from its `Modal` container.

Purpose: BARBACK-02 for the two Settings sub-pages — the lowest-risk conversion in the phase, since no picker or business-logic change is involved, only shell restructuring.

Output: `CategoryManager`/`GlasswareManager` converted from Modal to full-screen; `SettingsTab` wired to their new `onBack`-based prop contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CategoryManager — Modal to full-screen</name>
  <files>apps/barback/src/components/CategoryManager.tsx, apps/barback/src/components/CategoryManager.test.tsx</files>
  <reversibility rating="costly">Removes CategoryManager's Modal wrapper (D-26); reverting means restoring it.</reversibility>
  <read_first>
    apps/barback/src/components/CategoryManager.tsx
    apps/barback/src/components/views/AddEditIngredientView.tsx
  </read_first>
  <action>
    In `CategoryManager.tsx`, change `interface CategoryManagerProps` from `{ open: boolean; onClose: () => void }` to `{ onBack: () => void }`. Remove the `<Modal title="Manage Categories" open={open} onCancel={handleClose} footer={null} destroyOnHidden>` wrapper; replace with the same full-screen shell pattern established in `AddEditIngredientView.tsx`: an outer flex column `div` (`minHeight: '100vh'`), a header with a `<Button type="text" onClick={onBack}>← Back</Button>` + `<h2 className="text-white">Manage Categories</h2>`, and a `<main style={{ flex: 1, padding: 16, overflow: 'auto' }}>` wrapping everything the `Modal`'s children used to hold (the delete-refusal `Alert`, the category `List`/`Empty`, the `Divider`, the add-category `Input`+`Button` row). Rename the existing `handleClose` function's body to call `onBack()` instead of `onClose()` — keep its existing cleanup (clearing `editingId`/`newName`/`createError`/`deleteRefusal`) unchanged, just swap what it calls at the end. No other logic in this file changes — `useCategories`, `useCreateCategory`, `useRenameCategory`, `useDeleteCategory`, and every handler function are untouched.

    Create `CategoryManager.test.tsx` (this component currently has no test): render with a stubbed `onBack` and a mocked `useCategories()` returning one category; assert the "← Back" button is present and calling it invokes `onBack`; assert no element with `role="dialog"` is present (confirms the Modal wrapper is gone).
  </action>
  <verify>
    <automated>pnpm --filter barback test -- CategoryManager</automated>
  </verify>
  <acceptance_criteria>
    - `CategoryManager.tsx` renders no `role="dialog"` element
    - Clicking "← Back" calls the `onBack` prop
    - Existing add/rename/delete button click handlers and copywriting are byte-for-byte unchanged (verified by reading the diff — no text strings altered)
    - `pnpm --filter barback test -- CategoryManager` passes
  </acceptance_criteria>
  <done>Category management is a full-screen view with a working back button; all existing add/rename/delete behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: GlasswareManager — Modal to full-screen + wire SettingsTab</name>
  <files>apps/barback/src/components/GlasswareManager.tsx, apps/barback/src/components/GlasswareManager.test.tsx, apps/barback/src/components/SettingsTab.tsx</files>
  <reversibility rating="costly">Removes GlasswareManager's Modal wrapper (D-26); reverting means restoring it.</reversibility>
  <read_first>
    apps/barback/src/components/GlasswareManager.tsx
    apps/barback/src/components/CategoryManager.tsx
    apps/barback/src/components/SettingsTab.tsx
  </read_first>
  <action>
    Apply the exact same conversion to `GlasswareManager.tsx` as task 1 applied to `CategoryManager.tsx`: `interface GlasswareManagerProps` becomes `{ onBack: () => void }`, remove the `Modal` wrapper, add the identical header (back button + "Manage Glassware" title) + `<main>` shell, `handleClose` calls `onBack()`. No other logic changes. Create `GlasswareManager.test.tsx` mirroring `CategoryManager.test.tsx`'s assertions (back button present and wired, no `role="dialog"`).

    In `SettingsTab.tsx`, update both renders: `<CategoryManager open={view === 'categories'} onClose={() => setView('menu')} />` becomes `{view === 'categories' && <CategoryManager onBack={() => setView('menu')} />}`, and the equivalent for `GlasswareManager`. When `view` is `'categories'` or `'glassware'`, do NOT also render the Settings menu underneath (the full-screen view replaces the tab's entire content area, matching `IngredientsTab`'s established pattern from plan 02.1-02).
  </action>
  <verify>
    <automated>pnpm --filter barback test -- GlasswareManager SettingsTab</automated>
  </verify>
  <acceptance_criteria>
    - `GlasswareManager.tsx` renders no `role="dialog"` element; clicking "← Back" calls `onBack`
    - `SettingsTab.tsx` renders `CategoryManager`/`GlasswareManager` full-screen (no Settings menu visible underneath) when their respective view is active
    - `pnpm --filter barback test -- GlasswareManager SettingsTab` passes and `pnpm --filter barback build` succeeds with zero type errors
  </acceptance_criteria>
  <done>Both Settings sub-pages (Categories, Glassware) are genuine full-screen views with working back buttons, reached from the Settings tab menu — BARBACK-02 fully satisfied for both entities.</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `apps/barback/src/components/CategoryManager.tsx` — new prop contract: `{ onBack: () => void }` (was `{ open, onClose }`)
- `apps/barback/src/components/GlasswareManager.tsx` — new prop contract: `{ onBack: () => void }` (was `{ open, onClose }`)
- `apps/barback/src/components/SettingsTab.tsx` — wired to the new full-screen prop contracts

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure shell restructuring — no change to any request/response boundary already covered by Phase 1/2's threat register for category/glassware CRUD |
</threat_model>

<verification>
- `pnpm --filter barback test -- CategoryManager GlasswareManager SettingsTab` passes
- `pnpm --filter barback build` succeeds with zero type errors
- Manual smoke: `pnpm dev`, Settings tab → Categories → full-screen list with working add/rename/delete → Back → Settings tab → Glassware → same
</verification>

<success_criteria>
Both Category and Glassware management are reachable as genuine full-screen views from the Settings tab, with all existing add/rename/delete behavior fully intact.
</success_criteria>

<output>
Create `.planning/phases/02.1-recipe-ui-cleanup/02.1-04-SUMMARY.md` when done
</output>
