---
phase: 02.1-recipe-ui-cleanup
plan: 06
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02", "02.1-05"]
files_modified:
  - apps/barback/src/components/pickers/GlasswarePicker.tsx
  - apps/barback/src/components/pickers/GlasswarePicker.test.tsx
  - apps/barback/src/components/IngredientListForm.tsx
  - apps/barback/src/components/IngredientListForm.test.tsx
  - apps/barback/src/components/views/AddEditRecipeView.tsx
  - apps/barback/src/components/views/AddEditRecipeView.test.tsx
  - apps/barback/src/components/RecipesTab.tsx
  - apps/barback/src/components/RecipeForm.tsx
  - apps/barback/src/components/RecipeForm.test.tsx
  - apps/barback/src/components/GlasswareSelector.tsx
autonomous: true
requirements: [MATCH-05, BARBACK-02, BARBACK-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tapping 'Add Recipe' on the Recipes tab opens a full-screen view (not a Modal) with a back button that returns to the recipe list without saving"
    - "Each recipe ingredient line's category-or-ingredient field is the combined IngredientPicker, replacing the old static category-only Select — a line can resolve to either a plain category or a locked specific ingredient, submitted with the correct categoryId/ingredientId/requiresSpecific triple"
    - "The recipe's glassware field is a single autocomplete-with-inline-create field, replacing the old static Select — typing filters existing glassware, and a '+ Add new' option appears only when the typed text matches no existing glassware name"
    - "Editing an existing recipe opens the same full-screen view pre-filled with every ingredient line's category, specific ingredient (if any), lock state, quantity, and unit, plus method/glassware/garnish"
    - statement: "The glassware autocomplete picker's search is synchronous over the already-fetched in-memory glassware list, with no loading spinner of its own — matches CategoryPicker's identical backstop behavior from plan 02.1-03"
      verification: backstop
  artifacts:
    - path: "apps/barback/src/components/pickers/GlasswarePicker.tsx"
      provides: "Reusable autocomplete-with-inline-create picker for selecting or creating glassware"
    - path: "apps/barback/src/components/views/AddEditRecipeView.tsx"
      provides: "Full-screen recipe add/edit view with a back button, replacing the RecipeForm Modal"
    - path: "apps/barback/src/components/IngredientListForm.tsx"
      provides: "Recipe ingredient-line rows now using IngredientPicker instead of a static category Select"
  key_links:
    - from: "apps/barback/src/components/IngredientListForm.tsx"
      to: "apps/barback/src/components/pickers/IngredientPicker.tsx"
      via: "a Form.Item noStyle shouldUpdate render-prop reads/writes each row's categoryId/ingredientId/requiresSpecific through the form instance, passed to IngredientPicker as a composite value/onChange"
      pattern: "IngredientPicker"
    - from: "apps/barback/src/components/views/AddEditRecipeView.tsx"
      to: "apps/barback/src/components/pickers/GlasswarePicker.tsx"
      via: "renders <GlasswarePicker> as the glasswareId Form.Item's child, replacing the old GlasswareSelector"
      pattern: "<GlasswarePicker"
  prohibitions:
    - "MUST NOT offer '+ Add new \"X\"' in GlasswarePicker for a glassware name that already exists under a case-insensitive comparison — mirrors CategoryPicker's identical duplicate-prevention rule from plan 02.1-03, applied to the glassware namespace"
---

<objective>
Deliver the phase's most complex full-screen flow: the recipe add/edit form, now full-screen (D-26) instead of a Modal, using the combined IngredientPicker (plan 02.1-05) for each ingredient line's category-or-specific-ingredient field and a new GlasswarePicker for the glassware field — completing D-27's "single autocomplete field" pattern across all three of its target locations (category, recipe-ingredient, glassware).

Purpose: MATCH-05's remaining UI wiring (the recipe form is the only place `requiresSpecific`/`ingredientId` are actually set) plus BARBACK-02/03 for the Recipe entity, the last and largest of the four entities being converted to full-screen.

Output: `GlasswarePicker` (new); `IngredientListForm` updated to use `IngredientPicker` per row; `AddEditRecipeView` (new, full-screen replacement for `RecipeForm`); `RecipesTab` wired to the new view.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-CONTEXT.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-UI-SPEC.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-01-SUMMARY.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GlasswarePicker — autocomplete-with-inline-create</name>
  <files>apps/barback/src/components/pickers/GlasswarePicker.tsx, apps/barback/src/components/pickers/GlasswarePicker.test.tsx</files>
  <read_first>
    apps/barback/src/components/pickers/CategoryPicker.tsx
    apps/barback/src/components/GlasswareSelector.tsx
    apps/barback/src/api/useGlassware.ts
  </read_first>
  <action>
    Create `apps/barback/src/components/pickers/GlasswarePicker.tsx`, structurally identical to `CategoryPicker.tsx` (same `value`/`onChange` prop shape, same case-insensitive-duplicate-prevention check for the "+ Add" trigger, same inline error-on-create-failure pattern), but backed by `useGlassware()`/`useCreateGlassware()` instead of the category hooks, and OPTIONAL — glassware is nullable per D-17, so preserve `allowClear` behavior: clearing the field calls `onChange?.(undefined)`. Prop shape: `interface GlasswarePickerProps { value?: string; onChange?: (glasswareId: string | undefined) => void }`.
  </action>
  <verify>
    <automated>pnpm --filter barback test -- GlasswarePicker</automated>
  </verify>
  <acceptance_criteria>
    - Typing a substring of an existing glassware name filters options (case-insensitive)
    - Typing a name matching no existing glassware shows exactly one "+ Add" option; selecting it creates the glassware and selects it
    - Typing a name matching an existing glassware (any case) does not offer "+ Add"
    - Clearing the field calls `onChange` with `undefined`
    - `pnpm --filter barback test -- GlasswarePicker` passes
  </acceptance_criteria>
  <done>GlasswarePicker works exactly like CategoryPicker, scoped to the glassware entity.</done>
</task>

<task type="auto">
  <name>Task 2: Wire IngredientPicker into IngredientListForm</name>
  <files>apps/barback/src/components/IngredientListForm.tsx, apps/barback/src/components/IngredientListForm.test.tsx</files>
  <read_first>
    apps/barback/src/components/IngredientListForm.tsx
    apps/barback/src/components/pickers/IngredientPicker.tsx
    apps/barback/src/components/UnitDropdown.tsx
  </read_first>
  <action>
    In `IngredientListForm.tsx`, obtain the surrounding form instance via `const form = Form.useFormInstance()` (this component is always rendered inside `RecipeForm`'s/`AddEditRecipeView`'s `<Form>`, so this hook always resolves). Remove the `categoryId` `Form.Item`'s `<Select placeholder="Category" options={categoryOptions} />` and the now-unused `useCategories`/`categoryOptions` — replace with a `<Form.Item noStyle shouldUpdate key={\`\${field.key}-picker\`}>` render-prop that reads the row's current composite value via `form.getFieldValue([field.name, 'categoryId'])` / `form.getFieldValue([field.name, 'ingredientId']) ?? null` / `form.getFieldValue([field.name, 'requiresSpecific']) ?? false`, and renders `<IngredientPicker value={{ categoryId, ingredientId, requiresSpecific }} onChange={(next) => { form.setFieldValue([field.name, 'categoryId'], next.categoryId); form.setFieldValue([field.name, 'ingredientId'], next.ingredientId); form.setFieldValue([field.name, 'requiresSpecific'], next.requiresSpecific) }} />` (import `IngredientPicker` from `./pickers/IngredientPicker.js`). Keep this render-prop `Form.Item` in the same flex position the old category `Form.Item` occupied (`style={{ flex: 1, margin: 0 }}` on the wrapping element, since `Form.Item noStyle` itself renders no layout box).

    Change `add()`'s call site (the "Add Ingredient" dashed button) from `onClick={() => add()}` to `onClick={() => add({ ingredientId: null, requiresSpecific: false })}` so every new row starts with explicit, well-typed defaults for the two new fields rather than `undefined` (which would make `computeMakeable`'s `!line.ingredientId` guard behave correctly anyway, but explicit defaults make the row's shape self-documenting and match what a freshly-added plain-category row should look like).
  </action>
  <verify>
    <automated>pnpm --filter barback test -- IngredientListForm</automated>
  </verify>
  <acceptance_criteria>
    - A newly-added ingredient row defaults to `ingredientId: null, requiresSpecific: false`
    - Selecting a category in the row's picker sets that row's `categoryId` and leaves `ingredientId`/`requiresSpecific` at their category-only defaults
    - Selecting a specific ingredient in the row's picker sets `categoryId`/`ingredientId`/`requiresSpecific: true` together, and the lock checkbox renders for that row
    - `pnpm --filter barback test -- IngredientListForm` passes
  </acceptance_criteria>
  <done>Every recipe ingredient-line row uses the combined IngredientPicker; the three related fields (categoryId/ingredientId/requiresSpecific) stay in sync as one unit per row.</done>
</task>

<task type="auto">
  <name>Task 3: AddEditRecipeView (full-screen) + wire RecipesTab, retire RecipeForm/GlasswareSelector</name>
  <files>apps/barback/src/components/views/AddEditRecipeView.tsx, apps/barback/src/components/views/AddEditRecipeView.test.tsx, apps/barback/src/components/RecipesTab.tsx, apps/barback/src/components/RecipeForm.tsx, apps/barback/src/components/RecipeForm.test.tsx, apps/barback/src/components/GlasswareSelector.tsx</files>
  <reversibility rating="costly">Converts the recipe add/edit shell from Modal to full-screen (D-26); reverting means restoring RecipeForm's Modal wrapper this task deletes.</reversibility>
  <read_first>
    apps/barback/src/components/RecipeForm.tsx
    apps/barback/src/components/RecipesTab.tsx
    apps/barback/src/components/views/AddEditIngredientView.tsx
    apps/barback/src/components/pickers/GlasswarePicker.tsx
    packages/shared/src/recipe.ts
  </read_first>
  <action>
    Create `apps/barback/src/components/views/AddEditRecipeView.tsx`, adapted from `RecipeForm.tsx` following the exact same Modal-to-full-screen conversion `AddEditIngredientView.tsx` already established in plan 02.1-03: same `interface AddEditRecipeViewProps { recipe?: Recipe; onBack: () => void }` (no `open`), remove the `<Modal>` wrapper for the identical header+back-button+`<main>` shell, `onBack()` replaces `onClose()` on both successful submit and the cancel/back path. Keep all existing validation rules (`nameRules`, `ingredientsRules`, `methodRules`) and the `useCreateRecipe`/`useUpdateRecipe` submit logic unchanged. Update the pre-fill `useEffect`'s `ingredients` mapping to also carry the two new fields: `ingredients: recipe.ingredients.map((ing) => ({ categoryId: ing.categoryId, ingredientId: ing.ingredientId, requiresSpecific: ing.requiresSpecific, quantity: ing.quantity, unit: ing.unit }))`. Replace the glassware `Form.Item`'s child from `<GlasswareSelector glassware={glasswareList} />` to `<GlasswarePicker />` (import from `../pickers/GlasswarePicker.js`) — remove the now-unused `useGlassware` call and `glasswareList` variable (GlasswarePicker self-fetches).

    Delete `apps/barback/src/components/RecipeForm.tsx`, `apps/barback/src/components/RecipeForm.test.tsx` (fully superseded — `RecipeForm.test.tsx`'s three existing test cases are re-created against the new component below), and `apps/barback/src/components/GlasswareSelector.tsx` (fully superseded by `GlasswarePicker.tsx`).

    Create `AddEditRecipeView.test.tsx` re-implementing `RecipeForm.test.tsx`'s three prior cases (unit submission reaches the real value, glasswareId submission reaches the real value, server error message surfaces verbatim) against `AddEditRecipeView`, updated for the new picker components: use the `IngredientPicker`'s combobox interaction pattern (select a category via the AutoComplete, matching the file's existing `fireEvent.mouseDown` + `findByTitle`-or-equivalent antd interaction helpers) instead of the old plain `Select`. Also add one new test: selecting a specific ingredient (not just a category) in the ingredient-line picker results in `capturedBody.ingredients[0].ingredientId`/`requiresSpecific: true` reaching the POST body correctly.

    In `RecipesTab.tsx`, replace the `RecipeForm` import with `AddEditRecipeView` (from `./views/AddEditRecipeView.js`), and change the `view === 'add'` render from the old Modal-style `<RecipeForm recipe={editing} open={view === 'add'} onClose={() => setView('list')} />` to `{view === 'add' && <AddEditRecipeView recipe={editing} onBack={() => setView('list')} />}`, ensuring the recipe list is not also rendered underneath when a full-screen view is active (matching the pattern already established for `IngredientsTab` in plan 02.1-03).
  </action>
  <verify>
    <automated>pnpm --filter barback test -- AddEditRecipeView RecipesTab</automated>
  </verify>
  <acceptance_criteria>
    - `AddEditRecipeView.tsx` renders no `role="dialog"` element and does render a "← Back" button
    - `apps/barback/src/components/RecipeForm.tsx`, `RecipeForm.test.tsx`, and `GlasswareSelector.tsx` no longer exist
    - Submitting a recipe with one plain-category ingredient line and one locked-specific ingredient line sends both `ingredientId`/`requiresSpecific` triples correctly in the POST body
    - Selecting a glassware via the new `GlasswarePicker` reaches the POST body's `glasswareId` correctly
    - `pnpm --filter barback test -- AddEditRecipeView RecipesTab` passes and `pnpm --filter barback build` succeeds with zero type errors
  </acceptance_criteria>
  <done>Recipe add/edit is a genuine full-screen view using IngredientPicker and GlasswarePicker throughout — D-27's autocomplete-with-inline-create pattern is now applied consistently across all three of its target locations (category, recipe-ingredient, glassware).</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `apps/barback/src/components/pickers/GlasswarePicker.tsx` — new export: `GlasswarePicker({ value, onChange })`
- `apps/barback/src/components/IngredientListForm.tsx` — recipe ingredient rows now use `IngredientPicker`
- `apps/barback/src/components/views/AddEditRecipeView.tsx` — new export: `AddEditRecipeView({ recipe, onBack })`
- `apps/barback/src/components/RecipeForm.tsx`, `RecipeForm.test.tsx`, `GlasswareSelector.tsx` — deleted (superseded)
- `apps/barback/src/components/RecipesTab.tsx` — wired to the new full-screen view

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Barback client → POST /api/glassware (via GlasswarePicker's inline-create) | Untrusted glassware name now creatable from within the recipe-add flow, not just GlasswareManager |
| Barback client → POST /api/recipes (ingredientId/requiresSpecific per line) | Already covered by plan 02.1-01's T-02.1-01/T-02.1-02/T-02.1-03 — this plan is the first UI path that actually populates those fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-11 | Tampering | GlasswarePicker inline-create bypassing the curated glassware list | medium | mitigate | Inline-create calls the existing `POST /api/glassware` endpoint unchanged — same server-side validation and UNIQUE constraint CategoryPicker already relies on |
| T-02.1-12 | Tampering | Case-variant duplicate glassware creation via the picker | low | mitigate | Case-insensitive existing-match check prevents "+ Add" from ever being offered for a name that already exists under any casing |
</threat_model>

<verification>
- `pnpm --filter barback test -- GlasswarePicker IngredientListForm AddEditRecipeView RecipesTab` passes
- `pnpm --filter barback build` succeeds with zero type errors
- Manual smoke: `pnpm dev`, Recipes tab → Add Recipe → full-screen form → add an ingredient line, pick a specific ingredient (checkbox appears, checked) → pick glassware via autocomplete → save → recipe appears in list
</verification>

<success_criteria>
The owner can create or edit a recipe through a genuine full-screen view, with every ingredient line able to lock to a specific ingredient via the combined picker, and glassware selected through the same autocomplete-with-inline-create pattern used for categories.
</success_criteria>

<output>
Create `.planning/phases/02.1-recipe-ui-cleanup/02.1-06-SUMMARY.md` when done
</output>
