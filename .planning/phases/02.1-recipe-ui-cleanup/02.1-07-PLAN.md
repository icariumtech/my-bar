---
phase: 02.1-recipe-ui-cleanup
plan: 07
type: execute
wave: 4
depends_on: ["02.1-01", "02.1-06"]
files_modified:
  - apps/barback/src/components/views/RecipeDetailView.tsx
  - apps/barback/src/components/views/RecipeDetailView.test.tsx
  - apps/barback/src/components/RecipeDetailView.tsx
  - apps/barback/src/components/RecipesTab.tsx
autonomous: true
requirements: [MATCH-05, BARBACK-02]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tapping a recipe row's view action opens recipe detail as a full-screen view (not a Modal) with a back button that returns to the recipe list"
    - "The recipe-level tri-state badge (green/yellow/red) is shown prominently at the top of the detail view, using the exact Copywriting Contract strings established in plan 02.1-01"
    - "Every ingredient line in the detail view shows its own per-line tri-state indicator (green/yellow/red), not just the recipe-level rollup — MATCH-05's per-line detail is visible, not collapsed into the single recipe-level badge"
    - "A red-status recipe shows the exact 'Can't make this right now. Missing: [category names].' sentence, listing only the categories behind red lines (never yellow lines, which have a substitute available)"
    - "Each yellow-status ingredient line shows the exact hint sentence naming both the selected (out-of-stock) ingredient and its available in-stock alternative, per the UI-SPEC Copywriting Contract"
  artifacts:
    - path: "apps/barback/src/components/views/RecipeDetailView.tsx"
      provides: "Full-screen recipe detail view with recipe-level badge, per-line tri-state indicators, and yellow/red hint text"
---

<objective>
Complete the phase's tri-state UI work: convert RecipeDetailView from a Modal to a full-screen view (D-26) and enrich it with the per-line tri-state indicators and yellow-state hint text that plan 02.1-01 deliberately deferred when it did the minimal atomic boolean-to-enum fix. This is the last plan in the phase — after it lands, every requirement (MATCH-05, BARBACK-01/02/03) is fully delivered end to end.

Purpose: MATCH-05's remaining UI surface (per-line status, not just the recipe-level rollup) and the last BARBACK-02 full-screen conversion — closing out the phase's goal that a not-makeable-as-intended recipe shows exactly why, at the line level, not just a single pass/fail badge.

Output: `views/RecipeDetailView.tsx` (new, full-screen, per-line tri-state); `RecipesTab` wired to it; old `components/RecipeDetailView.tsx` (Modal) deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-CONTEXT.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-UI-SPEC.md
@.planning/phases/02.1-recipe-ui-cleanup/02.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-screen RecipeDetailView with per-line tri-state indicators</name>
  <files>apps/barback/src/components/views/RecipeDetailView.tsx, apps/barback/src/components/views/RecipeDetailView.test.tsx, apps/barback/src/components/RecipeDetailView.tsx</files>
  <reversibility rating="costly">Converts the recipe detail shell from Modal to full-screen (D-26); reverting means restoring the Modal wrapper this task deletes.</reversibility>
  <read_first>
    apps/barback/src/components/RecipeDetailView.tsx
    apps/barback/src/components/MakeableStatusBadge.tsx
    apps/barback/src/components/views/AddEditIngredientView.tsx
    packages/shared/src/recipe.ts
    packages/shared/src/makeable.ts
    .planning/phases/02.1-recipe-ui-cleanup/02.1-UI-SPEC.md (Component Inventory: RecipeDetailView, IngredientStatusIndicator; Copywriting Contract: yellow detail hint)
  </read_first>
  <action>
    Create `apps/barback/src/components/views/RecipeDetailView.tsx`, adapted from the current `apps/barback/src/components/RecipeDetailView.tsx` (already updated to read `recipe.overallStatus` by plan 02.1-01's task 3) following the same Modal-to-full-screen conversion pattern as `AddEditIngredientView.tsx`: `interface RecipeDetailViewProps { recipe: Recipe; onBack: () => void }` (no `open`), header with back button + the recipe's name as the title (Display-role typography, matching the UI-SPEC), `<main>` wrapping the existing content.

    Keep the top `<MakeableStatusBadge status={recipe.overallStatus} />` and the red-only missing-ingredients sentence (`{recipe.overallStatus === 'red' && <p>...Missing: {recipe.missingCategoryNames.join(', ')}.</p>}`) exactly as plan 02.1-01 left them.

    Add, directly below the top badge, one hint paragraph per 'yellow' ingredient line: `recipe.ingredients.filter(ing => ing.status === 'yellow').map(ing => <p key={ing.id} style={{ color: '#facc15', margin: 0 }}>{`The ${ing.ingredientName} you selected is out of stock, but ${ing.alternativeIngredientName} is available.`}</p>)` — this exact sentence structure matches the UI-SPEC Copywriting Contract's "Recipe yellow detail hint" row.

    In the ingredients `<List>`, add a small per-line status indicator before each line's existing `quantity unit categoryName` text — a small colored dot is sufficient (no separate exported component needed; implement as an inline `<span>` styled via a local `statusColor(status)` helper returning `'#22c55e' | '#facc15' | '#ef4444'`), rendered as `<span style={{ display: 'inline-block', width: 8, height: 8, borderRadius: '50%', backgroundColor: statusColor(ing.status), marginRight: 8 }} aria-label={`${ing.status} status`} />` immediately before each `List.Item`'s text span.

    Delete `apps/barback/src/components/RecipeDetailView.tsx` (the Modal-wrapped version) — fully superseded.

    Create `RecipeDetailView.test.tsx`: renders with a fixture `Recipe` containing three ingredient lines (one green, one yellow with a named `alternativeIngredientName`, one red) and `overallStatus: 'red'`; asserts the "← Back" button is present and calls `onBack`; asserts the red missing-ingredients sentence is present; asserts the yellow hint sentence names both the missing and alternative ingredient; asserts no `role="dialog"` element is present.
  </action>
  <verify>
    <automated>pnpm --filter barback test -- views/RecipeDetailView</automated>
  </verify>
  <acceptance_criteria>
    - `views/RecipeDetailView.tsx` renders no `role="dialog"` element and does render a "← Back" button
    - A recipe with a 'yellow' line shows the exact hint sentence naming both ingredients; a recipe with a 'red' line shows the missing-categories sentence listing only red lines' categories
    - `apps/barback/src/components/RecipeDetailView.tsx` (the old Modal version) no longer exists
    - `pnpm --filter barback test -- views/RecipeDetailView` passes
  </acceptance_criteria>
  <done>Recipe detail is a full-screen view showing the recipe-level badge, per-line tri-state indicators, and the exact red/yellow hint copy from the UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RecipesTab to the full-screen detail view</name>
  <files>apps/barback/src/components/RecipesTab.tsx</files>
  <read_first>
    apps/barback/src/components/RecipesTab.tsx
    apps/barback/src/components/views/RecipeDetailView.tsx
  </read_first>
  <action>
    In `RecipesTab.tsx`, replace the `RecipeDetailView` import with the one from `./views/RecipeDetailView.js`, and change the `view === 'detail'` render from the old Modal-style `{view === 'detail' && viewing && <RecipeDetailView recipe={viewing} open onClose={() => setView('list')} />}` to `{view === 'detail' && viewing && <RecipeDetailView recipe={viewing} onBack={() => setView('list')} />}`, ensuring the recipe list is not also rendered underneath when the detail view is active (matching the pattern already established for `add`/`edit` in plan 02.1-06).
  </action>
  <verify>
    <automated>pnpm --filter barback test -- RecipesTab</automated>
    <human-check>Create a recipe with one ingredient line locked to a specific ingredient. Toggle that ingredient's stock off from the Ingredients tab while another ingredient exists in the same category (in stock) — open the recipe's detail view and confirm the line's dot turns yellow and the hint sentence names a real alternative. Toggle every ingredient in that category off — confirm the line's dot turns red and the hint sentence disappears, replaced by that category appearing in the top "Missing:" sentence.</human-check>
  </verify>
  <acceptance_criteria>
    - Tapping a recipe row's view action renders the full-screen `RecipeDetailView` with no recipe list visible underneath
    - `pnpm --filter barback test -- RecipesTab` passes and `pnpm --filter barback build` succeeds with zero type errors
  </acceptance_criteria>
  <done>Recipe detail is fully reachable through the tab's own view state — every full-screen view introduced across this phase (Ingredient, Category, Glassware, Recipe add/edit, Recipe detail) now shares the identical back-button navigation pattern. The manual human-check above confirms the end-to-end tri-state badge update flow (VALIDATION.md's one manual-only verification item).</done>
</task>

</tasks>

## Artifacts This Plan Produces

- `apps/barback/src/components/views/RecipeDetailView.tsx` — new export: `RecipeDetailView({ recipe, onBack })`, with per-line tri-state indicators and yellow/red hint text
- `apps/barback/src/components/RecipeDetailView.tsx` — deleted (superseded)
- `apps/barback/src/components/RecipesTab.tsx` — wired to the new full-screen detail view

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure read/render — this plan adds no new write path; it only renders fields plan 02.1-01's `GET /api/recipes` already returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-13 | Information Disclosure | RecipeDetailView rendering `alternativeIngredientName` | low | accept | The alternative ingredient's name is already visible elsewhere in the same trusted, unauthenticated home-LAN app (the Ingredients tab lists every ingredient by name); surfacing it here on a 'yellow' line discloses nothing not already visible one tab away |
</threat_model>

<verification>
- `pnpm --filter barback test -- views/RecipeDetailView RecipesTab` passes
- `pnpm --filter barback build` succeeds with zero type errors
- `pnpm test` (full monorepo suite) passes — this is the phase's final wave, closing out MATCH-05/BARBACK-01/02/03 together
</verification>

<success_criteria>
Recipe detail is a full-screen view showing both the recipe-level tri-state badge and per-line tri-state detail with real substitution hints — the phase's full MATCH-05/BARBACK-01/02/03 scope is complete and verifiable end to end.
</success_criteria>

<output>
Create `.planning/phases/02.1-recipe-ui-cleanup/02.1-07-SUMMARY.md` when done
</output>
