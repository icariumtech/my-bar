---
phase: 03-patron-browse-experience
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/barback/src/api/useTags.ts
  - apps/barback/src/components/pickers/TagPicker.tsx
  - apps/barback/src/components/pickers/TagPicker.test.tsx
  - apps/barback/src/components/views/AddEditRecipeView.tsx
  - apps/barback/src/components/views/AddEditRecipeView.test.tsx
autonomous: true
requirements: [PATR-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Barback recipe form (AddEditRecipeView) includes a description textarea and a multi-select tag picker grouped into the four fixed D-34 groups in TAG_GROUP_ORDER — the owner can assign zero or more tags across any combination of groups to a single recipe"
    - "Editing an existing recipe pre-fills both the description field and the currently-assigned tags from the server response — re-saving without changes round-trips the same tagIds/description back unchanged"
    - "A recipe with zero tags assigned saves successfully — tags are optional, not required, at both the create and edit form level (D-35)"
  artifacts:
    - path: "apps/barback/src/api/useTags.ts"
      provides: "Read-only TanStack Query hook for GET /api/tags, queryKey ['tags']"
      exports: ["useTags"]
    - path: "apps/barback/src/components/pickers/TagPicker.tsx"
      provides: "Multi-select tag assignment grouped by the four fixed D-34 groups, no inline-create (D-35)"
  key_links:
    - from: "apps/barback/src/components/views/AddEditRecipeView.tsx"
      to: "apps/barback/src/components/pickers/TagPicker.tsx"
      via: "Form.Item name=\"tagIds\""
      pattern: "TagPicker"
    - from: "apps/barback/src/components/pickers/TagPicker.tsx"
      to: "apps/barback/src/api/useTags.ts"
      via: "useTags() as the full-taxonomy data source"
      pattern: "useTags"
  prohibitions:
    - "TagPicker MUST NOT expose a create-new-tag affordance this phase (D-35's fixed-taxonomy boundary) — unlike CategoryPicker/GlasswarePicker, no '+ Add' option should ever appear, even if the owner types text matching no existing tag; silently re-opening that scope boundary here would contradict CONTEXT.md's explicit deferral"
---

<objective>
Give the owner the Barback-side half of D-33/D-40: a description textarea and a tag-assignment picker on the recipe form, so recipes created or edited from Barback can carry the tag/description data 03-01's API already accepts. This plan is implementation groundwork for PATR-01/PATR-02 (per `03-CONTEXT.md`'s domain framing, "not scope creep") — without it, 03-01's tags/description fields have no owner-facing way to ever be populated, and every Patron card/detail plan would only ever render empty state.

Purpose: closes the write-side gap left by 03-01 (which built the API but not the Barback UI to drive it), so 03-02/03-04's Patron UI has real tagged/described recipes to render during manual verification, not just structurally-valid-but-empty ones.
Output: `useTags()` hook; `TagPicker.tsx` (multi-select, grouped, no inline-create); `AddEditRecipeView.tsx` extended with description + tag fields.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-patron-browse-experience/03-CONTEXT.md
@.planning/phases/03-patron-browse-experience/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useTags() hook + TagPicker — grouped multi-select, no inline-create</name>
  <files>apps/barback/src/api/useTags.ts, apps/barback/src/components/pickers/TagPicker.tsx, apps/barback/src/components/pickers/TagPicker.test.tsx</files>
  <read_first>
    apps/barback/src/api/useCategories.ts
    apps/barback/src/components/pickers/CategoryPicker.tsx
    apps/barback/src/components/pickers/GlasswarePicker.tsx
    packages/shared/src/tag.ts
  </read_first>
  <behavior>
    RED then GREEN:
    - Given a flat `Tag[]` fixture spanning all four groups in scrambled order (e.g. a flavor tag listed before a spirit tag), `TagPicker` renders four labeled option-groups in exactly `TAG_GROUP_ORDER` sequence (Spirit, Type, Season, Flavor), each containing only that group's tags.
    - Selecting two tags from different groups and calling the form's `onChange` reports both ids as a `string[]`, order-independent.
    - No "+ Add" / create-new option is ever rendered, even when the picker's search text matches no existing tag name (unlike `CategoryPicker`/`GlasswarePicker` — D-35's fixed-taxonomy prohibition).
  </behavior>
  <action>
    Create `apps/barback/src/api/useTags.ts`: `useTags()` — `useQuery({ queryKey: ['tags'], queryFn: () => apiFetch<Tag[]>('/tags') })` (mirrors `useCategories()`'s exact shape; import `apiFetch` from `./client.js`, `Tag` from `@my-bar/shared`). Read-only — no create/rename/delete mutation exported, since D-35 gives Barback no tag-CRUD UI this phase.

    Create `TagPicker.tsx`: `TagPicker({ value, onChange }: { value?: string[]; onChange?: (tagIds: string[]) => void })` — the value/onChange shape antd's `Form.Item` clones directly onto the component (same contract convention as `CategoryPicker`/`GlasswarePicker`, but a `string[]` rather than a single `string | undefined`, since tag assignment is multi-select). Uses antd's `Select` with `mode="multiple"`, backed by `useTags()`. Group the flat `tags` array into four labeled sections by iterating `TAG_GROUP_ORDER` (imported from `@my-bar/shared`) and filtering `tags.filter(t => t.group === groupId)` per group — construct antd's grouped-`options` shape: an array of `{ label: <human label: 'Spirit'|'Type'|'Season'|'Flavor'>, options: [{ label: tag.name, value: tag.id }, ...] }`, one entry per group, in `TAG_GROUP_ORDER` sequence, SKIPPING a group entirely from the `options` array if it has zero tags (defensive — the 24-entry seed always has ≥4 per group, but this keeps the component correct if the taxonomy is ever pared down). Do NOT implement any search-driven "+ Add" branch — no `onSearch`/`CREATE_OPTION_VALUE` logic at all, unlike `CategoryPicker`/`GlasswarePicker`; a plain multi-select over the fixed, fully-known option list is the entire component. Render `<Select mode="multiple" value={value} onChange={onChange} options={groupedOptions} placeholder="Select tags (optional)" />`.
  </action>
  <verify>
    <automated>pnpm --filter barback test -- TagPicker</automated>
  </verify>
  <acceptance_criteria>
    - `TagPicker.test.tsx` asserts the four option-groups render in `TAG_GROUP_ORDER` sequence regardless of input array order
    - `grep -c "CREATE_OPTION_VALUE" apps/barback/src/components/pickers/TagPicker.tsx` is 0 — no inline-create path exists
    - `pnpm --filter barback test -- TagPicker` passes
  </acceptance_criteria>
  <done>TagPicker renders a grouped multi-select over the live `/api/tags` taxonomy with no create-new affordance, reporting selected tag ids as a plain string array.</done>
</task>

<task type="auto">
  <name>Task 2: Wire description + TagPicker into AddEditRecipeView</name>
  <files>apps/barback/src/components/views/AddEditRecipeView.tsx, apps/barback/src/components/views/AddEditRecipeView.test.tsx</files>
  <read_first>
    apps/barback/src/components/views/AddEditRecipeView.tsx
    apps/barback/src/components/views/AddEditRecipeView.test.tsx
    apps/barback/src/components/pickers/TagPicker.tsx
  </read_first>
  <action>
    In `AddEditRecipeView.tsx`, add two new `Form.Item`s after the existing garnish field (D-40's "after garnish" ordering, matching `03-CONTEXT.md`'s canonical-refs note): `<Form.Item name="description" label="Description / Story"><Input.TextArea placeholder="e.g. A smooth, sippable classic..." maxLength={2000} rows={4} /></Form.Item>` (no `rules` — optional, matches D-40), then `<Form.Item name="tagIds" label="Tags"><TagPicker /></Form.Item>` (also no `rules` — optional, matches D-35/must_haves' zero-tags-saves-successfully requirement). Import `TagPicker` from `../pickers/TagPicker.js`.

    Update the existing pre-fill `useEffect`'s `form.setFieldsValue({...})` call to add `description: recipe.description ?? undefined, tagIds: recipe.tags.map((t) => t.id)` alongside the existing fields (mirrors the effect's existing `garnish: recipe.garnish ?? undefined` null-to-undefined convention for the optional description field; `tagIds` defaults to an empty array via `.map()` on an empty `tags` array, never `undefined`, so a zero-tag recipe's picker renders as "nothing selected" rather than uncontrolled).

    Update `AddEditRecipeView.test.tsx`'s recipe fixture(s) (fixture-shape-only change, mirroring the `02.1-01` precedent's identical fixture-update pattern) to include `tags: [], description: null` on every mocked `Recipe` response object, so the file continues to type-check and its existing assertions keep passing unmodified.
  </action>
  <verify>
    <automated>pnpm --filter barback test -- AddEditRecipeView</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "name=\"description\"" apps/barback/src/components/views/AddEditRecipeView.tsx` is at least 1
    - `grep -c "name=\"tagIds\"" apps/barback/src/components/views/AddEditRecipeView.tsx` is at least 1
    - Editing a recipe fixture with 2 tags and a description pre-fills both fields, verified by an explicit test
    - Saving a recipe with zero tags and an empty description succeeds (no validation error), verified by an explicit test
    - `pnpm --filter barback test -- AddEditRecipeView` passes with all pre-existing assertions (unit submission, glasswareId submission, server-error-message surfacing) still passing unmodified
  </acceptance_criteria>
  <done>The owner can assign a description and any number of tags (across any groups) to a recipe from Barback, and both round-trip correctly through create and edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Barback form → PATCH/POST /api/recipes | Untrusted request body carrying `tagIds`/`description`, already validated server-side by 03-01's Zod boundary — this plan is client-only, no new server surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering / Injection | Description textarea free-text input | medium | mitigate | Server-side `.trim().max(2000)` Zod bound (03-01) is the real enforcement boundary; this plan's textarea has no client-side sanitization logic to bypass, and the value is rendered as plain text everywhere it's later displayed (never `dangerouslySetInnerHTML`) — inherits 03-01's T-03-03 mitigation |
| T-03-09 | Tampering | Client trusts `useTags()` GET response shape | low | accept | Read-only reference data validated by the shared `tag` Zod schema at the fetch boundary; a malformed/unexpected `group` value fails Zod parsing rather than silently rendering a broken option-group |

</threat_model>

## Artifacts This Plan Produces

**New files** — `apps/barback/src/api/useTags.ts`, `apps/barback/src/components/pickers/{TagPicker.tsx,TagPicker.test.tsx}`

**Exported symbols** — `useTags`, `TagPicker`

**Modified** — `apps/barback/src/components/views/AddEditRecipeView.tsx` (description + tagIds fields, pre-fill effect)

<verification>
- `pnpm --filter barback test -- TagPicker AddEditRecipeView` passes
- Manual: creating/editing a recipe in Barback with 2+ tags across different groups and a description round-trips correctly on re-open
</verification>

<success_criteria>
The owner can assign description + tags to any recipe from Barback; the write flows through unchanged into 03-01's API; zero-tag recipes remain valid.
</success_criteria>

<output>
Create `.planning/phases/03-patron-browse-experience/03-03-SUMMARY.md` when done
</output>
