---
phase: 04-bartender-console-order-workflow
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/server/src/routes/orders.ts
  - apps/server/src/routes/orders.test.ts
autonomous: true
requirements: [BART-03, BART-04, SYNC-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PATCH /api/orders/:id/start transitions status 'new' -> 'in_progress' and emits 'orders:updated'; calling it again on an already-'in_progress' or already-'done' order is a safe no-op that returns 200 with the CURRENT (unchanged) status — it never regresses a 'done' order back to 'in_progress' (D-57, BART-03 unclassified edge, resolved: repeat-PATCH is idempotent, not an error)"
    - "PATCH /api/orders/:id/done transitions any non-'done' status to 'done', sets updatedAt to the transition time, and emits 'orders:updated'; calling it again on an already-'done' order is a safe no-op that returns 200 WITHOUT touching updatedAt a second time — repeat-Done never restarts the retention-window clock"
    - "Both PATCH endpoints return 404 {error} for an id matching no row; neither endpoint accepts or requires a request body"
    - "Marking an order Done does not write to the ingredients table in any way — verified by asserting every ingredient row's inStock value is byte-identical before and after the PATCH (order fulfillment tracks ticket status only, never a proxy for inventory depletion)"
    - "GET /api/orders (extended from Plan 04-01's ne(status,'done') filter) now also includes a 'done' order when its updatedAt is within the last 5 minutes (DONE_RETENTION_MS = 5 * 60 * 1000, inclusive lower bound: now - updatedAt <= 5 minutes) — a 'done' order older than that is excluded entirely, giving the bartender a brief recent-history window without the queue growing unbounded (D-60, SYNC-02 boundary edge)"
    - "The retention boundary is exact and one-sided: an order marked done 4:59 ago still appears in GET /api/orders; the same order marked done 5:01 ago does not — proven with a directly-inserted 'done' row carrying a controlled updatedAt timestamp, not a real-time sleep-based test"
  artifacts:
    - path: "apps/server/src/routes/orders.ts"
      provides: "PATCH /:id/start, PATCH /:id/done, and a 5-minute done-order retention window in GET /"
  key_links:
    - from: "apps/bartender/src/api/socket.ts"
      to: "apps/server/src/routes/orders.ts"
      via: "Both PATCH handlers emit app.io?.emit('orders:updated', { orderId }) — already-registered handler from Plan 04-01 fires for real once this plan lands"
      pattern: "orders:updated"
  prohibitions:
    - statement: "Marking an order Done (BART-03/D-56) MUST NOT modify ingredient stock or any other inventory record — order fulfillment tracks ticket status only; REQUIREMENTS.md's Out of Scope explicitly excludes precise pour-depletion tracking, and a well-intentioned 'decrement stock on Done' feature would silently violate that boundary"
      status: resolved
      verification: test
---

<objective>
Close BART-03's status lifecycle and SYNC-02's live status propagation on the server side: `PATCH /api/orders/:id/start` (new → in_progress, idempotent), `PATCH /api/orders/:id/done` (any non-done → done, idempotent), both emitting `orders:updated`, plus the D-60 retention window that keeps a just-completed order briefly visible in `GET /api/orders` instead of vanishing instantly or accumulating forever.

Purpose: Plan 04-01's `GET /api/orders` and the Socket.IO `orders:updated` handler already exist but have nothing to emit them yet — this plan is the missing write path Plan 04-04's Done button and auto-advance-on-open UI calls into.
Output: two new PATCH routes on `apps/server/src/routes/orders.ts`; `GET /` extended with the 5-minute retention window.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-bartender-console-order-workflow/04-CONTEXT.md
@.planning/phases/04-bartender-console-order-workflow/04-RESEARCH.md
@.planning/phases/04-bartender-console-order-workflow/04-01-PLAN.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plan 04-04 implements against these — no codebase exploration needed. -->

apps/server additions to `ordersRoutes` (apps/server/src/routes/orders.ts):

  PATCH /:id/start : params {id: uuid} -> 200 Order | 404 {error}   — idempotent new->in_progress
  PATCH /:id/done  : params {id: uuid} -> 200 Order | 404 {error}   — idempotent *->done
  GET /  (extended): now also returns 'done' orders whose updatedAt is <= 5 minutes old
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: PATCH /:id/start + PATCH /:id/done — idempotent status transitions, RED then GREEN</name>
  <reversibility rating="reversible">The `/start`/`/done` endpoint shapes and the `orders:updated` event name are internal contracts consumed only by this monorepo's own Bartender frontend, never a published external API — changing them later only touches in-repo call sites (mirrors 03-05's identical reversibility rating for `inventory:changed`/`recipe:updated`).</reversibility>
  <files>apps/server/src/routes/orders.ts, apps/server/src/routes/orders.test.ts</files>
  <read_first>
    apps/server/src/routes/orders.ts (Plan 04-01's POST/GET handlers and the loadOrder helper this task reuses)
    apps/server/src/routes/ingredients.ts (PATCH /:id/stock — the closest existing analog for a narrow, single-purpose PATCH endpoint)
    apps/server/src/db/schema.ts (orders table, ingredients table — this task's stock-untouched assertion reads both)
  </read_first>
  <behavior>
    RED (write first, must fail) — extend `apps/server/src/routes/orders.test.ts`:
    - Seed a green recipe, `POST /api/orders` to create a `'new'` order. `PATCH /api/orders/:id/start` returns 200 with `status: 'in_progress'`; a follow-up `GET /api/orders` shows the same order now `'in_progress'`.
    - Calling `PATCH /api/orders/:id/start` a second time on the same (now `'in_progress'`) order returns 200 with `status: 'in_progress'` still (unchanged, no error).
    - `PATCH /api/orders/:id/done` on that order returns 200 with `status: 'done'`.
    - Calling `PATCH /api/orders/:id/start` on an already-`'done'` order returns 200 with `status: 'done'` STILL — it does not regress the order back to `'in_progress'`.
    - Calling `PATCH /api/orders/:id/done` a second time on an already-`'done'` order: capture the order's `updatedAt` from the first Done call's response, wait (or directly re-read the DB row), call `/done` again, and assert `updatedAt` is UNCHANGED (byte-identical timestamp) — the retention clock does not restart on a repeat call.
    - Both `PATCH /:id/start` and `PATCH /:id/done` on a well-formed but unknown order id return 404 `{ error: 'Order not found' }`.
    - Seed a recipe with an in-stock ingredient, create and complete an order (`POST` then `/start` then `/done`). Directly query the `ingredients` table before and after the `/done` call and assert the seeded ingredient's `inStock` value is unchanged — Done never touches inventory.
    - Real-listening-server integration test (mirrors the existing `orders:created` test in this same file and `hub.test.ts`'s pattern): after `PATCH /:id/done`, a connected client receives `orders:updated` with `{ orderId }` matching the completed order's id, within a 2-second timeout. A second such test for `PATCH /:id/start`.
    GREEN: implement both handlers until every test above passes.
  </behavior>
  <action>
    In `apps/server/src/routes/orders.ts`, add two new route handlers inside the existing `ordersRoutes` plugin function, after the existing `POST /` and `GET /` handlers.

    `PATCH /:id/start`: schema `{ params: z.object({ id: z.string().uuid() }), response: { 200: order, 404: z.object({ error: z.string() }) } }`. `const { id } = request.params`. `const [existing] = db.select({ id: orders.id, status: orders.status }).from(orders).where(eq(orders.id, id)).all()`. If `!existing`, `return reply.status(404).send({ error: 'Order not found' })`. Only when `existing.status === 'new'`: `db.update(orders).set({ status: 'in_progress', updatedAt: new Date() }).where(eq(orders.id, id)).run()` — the conditional write is what makes an already-`'in_progress'`/`'done'` order a true no-op (no `updatedAt` bump, no status regression). `app.io?.emit('orders:updated', { orderId: id })` (emit unconditionally — even a no-op call is harmless for a client to re-fetch on). Return `reply.status(200).send(loadOrder(db, id))`.

    `PATCH /:id/done`: same params/response schema shape. Same existence check (404 on miss). Only when `existing.status !== 'done'`: `db.update(orders).set({ status: 'done', updatedAt: new Date() }).where(eq(orders.id, id)).run()` — the conditional write is what makes a repeat call leave `updatedAt` untouched (the retention-window must-have depends on this). `app.io?.emit('orders:updated', { orderId: id })` unconditionally. Return `reply.status(200).send(loadOrder(db, id))`.

    Import `orders` was already imported from `../db/schema.js` in Task 1 of Plan 04-01; no new imports are needed beyond what that file already has (`eq` from `drizzle-orm` is also already imported there).
  </action>
  <verify>
    <automated>pnpm -F @my-bar/server test -- orders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All RED-phase test cases above pass, including both idempotency cases, both 404 cases, the stock-untouched assertion, and both real-socket `orders:updated` integration tests
    - `grep -c "existing.status === 'new'" apps/server/src/routes/orders.ts` is at least 1 — the conditional write exists for `/start`
    - `grep -c "existing.status !== 'done'" apps/server/src/routes/orders.ts` is at least 1 — the conditional write exists for `/done`
    - `pnpm -F @my-bar/server test -- orders.test.ts` passes (full file, including Plan 04-01's original cases — zero regressions)
    - `git log` shows a `test(04-03): ...` commit preceding a `feat(04-03): ...` commit for this task
  </acceptance_criteria>
  <done>A ticket can be opened (new -> in_progress) and completed (any -> done) via two idempotent, correctly-scoped PATCH endpoints, each broadcasting a real Socket.IO event, and Done never touches inventory.</done>
</task>

<task type="tdd">
  <name>Task 2: GET /api/orders — bounded 5-minute done-order retention window, RED then GREEN</name>
  <files>apps/server/src/routes/orders.ts, apps/server/src/routes/orders.test.ts</files>
  <read_first>
    apps/server/src/routes/orders.ts (the GET / handler's current ne(status,'done') filter, Task 1's PATCH handlers)
    .planning/phases/04-bartender-console-order-workflow/04-CONTEXT.md (D-60, and the Claude's Discretion note picking a concrete retention number)
    .planning/phases/04-bartender-console-order-workflow/04-RESEARCH.md (Pitfall 3: Done Orders Not Auto-Clearing Grows the Queue Unbounded)
  </read_first>
  <behavior>
    RED (write first, must fail) — extend `apps/server/src/routes/orders.test.ts`:
    - Directly insert (via `testDb.db.insert(orders)`, not through the API) a `'done'` order whose `updatedAt` is exactly `Date.now() - 4 * 60 * 1000` (4 minutes ago). `GET /api/orders` INCLUDES it.
    - Directly insert a `'done'` order whose `updatedAt` is exactly `Date.now() - 5 * 60 * 1000` (exactly 5 minutes ago, the inclusive boundary). `GET /api/orders` INCLUDES it.
    - Directly insert a `'done'` order whose `updatedAt` is `Date.now() - (5 * 60 * 1000 + 1000)` (5 minutes and 1 second ago, one step past the boundary). `GET /api/orders` EXCLUDES it.
    - A mix of `'new'`, `'in_progress'`, a recently-`'done'`, and a stale-`'done'` order: `GET /api/orders` returns exactly the first three, sorted per the existing createdAt-then-id ordering (the stale-done order is the only one excluded).
    GREEN: implement the retention window until every test above (and every pre-existing `GET /` test) passes.
  </behavior>
  <action>
    In `apps/server/src/routes/orders.ts`, add a module-level constant near the top of the file: `const DONE_RETENTION_MS = 5 * 60 * 1000` (D-60's concrete 5-minute window, per `04-CONTEXT.md`'s Claude's Discretion note and `04-UI-SPEC.md`'s Appendix, which already settled on this value). Import `and, gte, or` from `drizzle-orm` (alongside the existing `asc, eq, ne` import).

    Change the `GET /` handler's query from `.where(ne(orders.status, 'done'))` to `.where(or(ne(orders.status, 'done'), and(eq(orders.status, 'done'), gte(orders.updatedAt, new Date(Date.now() - DONE_RETENTION_MS)))))` — an order passes if it is NOT done (unconditionally included, matching Plan 04-01's original behavior), OR it IS done AND its `updatedAt` is within the retention window (inclusive: `gte`, not `gt`, so an order marked done exactly 5 minutes ago still shows). The existing `.orderBy(asc(orders.createdAt), asc(orders.id))` is unchanged.
  </action>
  <verify>
    <automated>pnpm -F @my-bar/server test -- orders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All RED-phase test cases above pass, including the exact-boundary (4:59 included, 5:00 included, 5:01 excluded) cases
    - `grep -c "DONE_RETENTION_MS" apps/server/src/routes/orders.ts` is at least 2 (the constant declaration plus its use in the query)
    - `pnpm -F @my-bar/server test -- orders.test.ts` passes (full file — every case from Plan 04-01 and this plan's Task 1, zero regressions)
    - `git log` shows a `test(04-03): ...` commit preceding a `feat(04-03): ...` commit for this task
  </acceptance_criteria>
  <done>A just-completed order stays visible in the Bartender queue for exactly 5 minutes after being marked Done, then disappears on its own — the queue never grows unbounded across a long event.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Bartender client → PATCH /api/orders/:id/start, /done | Unauthenticated write path affecting an existing order's status — same no-auth posture as every other route in this project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Elevation of Privilege | Marking any order Done writing to inventory | high | mitigate | This plan's own resolved test-tier prohibition — the RED-phase stock-untouched assertion directly proves no `ingredients` write occurs from either PATCH handler |
| T-04-11 | Tampering | Any device on the LAN can PATCH any order's status (no per-order ownership) | low | accept | Matches the project's existing no-auth, "no ownership" posture already established for orders in Plan 04-01's threat register (T-04-05) — there is exactly one Bartender console by design, so this is the intended single-operator model, not an unaddressed gap |
| T-04-12 | Denial of Service | Repeated `/start`/`/done` calls on the same order | low | accept | Both handlers are idempotent no-ops past the first real transition (this plan's own must-haves) — repeated calls cost a cheap SELECT + a harmless socket emit, not a resource-exhaustion vector at this project's scale |

</threat_model>

## Artifacts This Plan Produces

**Modified** — `apps/server/src/routes/orders.ts` (two new PATCH routes, `DONE_RETENTION_MS` constant, extended `GET /` filter), `apps/server/src/routes/orders.test.ts` (extended)

**Exported symbols** — none new (both PATCH routes are registered inside the existing `ordersRoutes` plugin from Plan 04-01)

<verification>
- `pnpm -F @my-bar/server test -- orders.test.ts` passes (full file: Plan 04-01's original cases + this plan's two tasks, zero regressions)
</verification>

<success_criteria>
BART-03's status lifecycle (new → in_progress → done) and SYNC-02's live status propagation are fully closed server-side: both transitions are idempotent, both broadcast in real time, Done never touches inventory, and completed orders auto-clear from the live queue after a bounded 5-minute window.
</success_criteria>

<output>
Create `.planning/phases/04-bartender-console-order-workflow/04-03-SUMMARY.md` when done
</output>
