---
phase: 05-docker-containerization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/index.ts
  - apps/server/src/index.test.ts
  - Dockerfile
  - compose.yml
  - .dockerignore
  - .env.example
  - .gitignore
autonomous: true
requirements: [DOCK-01, DOCK-02, DOCK-03, DOCK-04]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docker compose up -d starts exactly one container that serves the Patron, Bartender, and Barback screens plus the REST API and Socket.IO — no separate reverse proxy or second container (DOCK-01/DOCK-02)"
    - "GET /health returns 200 immediately, and this is the exact target compose.yml's healthcheck: block polls (D-08)"
    - "Data written before a full `docker compose down && docker compose up -d` cycle is still present afterward — SQLite persists via the whole ./data bind mount, not just a single .db file (DOCK-03, PITFALLS.md Pitfall 2)"
    - "On a fresh clone with no prior ./data directory, docker compose up -d still starts successfully — the ./data directory and my-bar.db file are created automatically (matching apps/server/src/db/client.ts's existing mkdirSync behavior)"
    - "The app container starts successfully even when ANTHROPIC_API_KEY in .env is left as the .env.example placeholder value — no Phase 5 startup check gates on this var (it is only consumed by AI features added in a later phase)"
    - "The bind-mounted ./data directory persists the .db, .db-wal, and .db-shm sidecar files together on the same host filesystem across a container restart, so WAL-mode coordination never breaks (PITFALLS.md Pitfall 2)"
    - ".env.example distinguishes the one required var with no safe default (ANTHROPIC_API_KEY) from optional vars that already have safe defaults (PORT, DB_PATH, NODE_ENV) via comments"
    - { statement: "Multi-stage COPY --from=builder does not silently merge/partial-overlay files from a stale layer — the runtime stage starts from a fresh node:22-slim base with no prior /app content", verification: backstop }
    - { statement: "Multi-stage build stage order (builder then runtime) is fixed and deterministic — the runtime stage never accidentally reuses builder-stage layers or build tools", verification: backstop }
  prohibitions:
    - "compose.yml must never grow into multi-container (separate DB/reverse-proxy/watchtower services) — single container only, per REQUIREMENTS.md's explicit Out-of-Scope decision and D-09"
    - "GET /health must never leak environment variables, stack traces, or internal file/DB paths in its response body — status + timestamp only, no diagnostic detail"
    - ".env.example must never contain a real secret value — every var uses an obviously-placeholder value (e.g. your_api_key_here), never a real key accidentally pasted in"
  artifacts:
    - path: "Dockerfile"
      provides: "multi-stage build producing the runtime image"
      contains: "FROM node:22-slim"
    - path: "compose.yml"
      provides: "single-container service definition with healthcheck + bind mount"
      contains: "healthcheck:"
    - path: ".dockerignore"
      provides: "excludes secrets/build artifacts from the Docker build context"
    - path: ".env.example"
      provides: "documents PORT/DB_PATH/NODE_ENV/ANTHROPIC_API_KEY"
      contains: "ANTHROPIC_API_KEY"
    - path: "apps/server/src/index.ts"
      provides: "GET /health endpoint used by compose.yml's healthcheck"
      contains: "/health"
    - path: "apps/server/src/index.test.ts"
      provides: "unit test proving the /health contract"
  key_links:
    - from: "compose.yml healthcheck"
      to: "apps/server/src/index.ts GET /health"
      via: "node -e fetch('http://127.0.0.1:3000/health') from inside the container"
      pattern: "/health"
    - from: "compose.yml volumes"
      to: "apps/server/src/db/client.ts DB_PATH resolution"
      via: "./data:/app/data bind mount + DB_PATH=./data/my-bar.db resolving relative to /app"
      pattern: "DB_PATH"
    - from: "Dockerfile runtime CMD"
      to: "apps/server/dist/index.js"
      via: "node apps/server/dist/index.js"
      pattern: "apps/server/dist/index.js"
---

<objective>
Package the full my-bar stack (Fastify server + Patron/Bartender/Barback built bundles) into a single Docker image and prove it end-to-end: one container reachable at all three screens plus the API and Socket.IO, with SQLite data surviving a full container recreate.

Purpose: This is the vertical slice the rest of Phase 5 builds on — README and CI (Plan 05-02) both document and automate what this plan proves works by hand first.
Output: Dockerfile, compose.yml, .dockerignore, .env.example, a GET /health endpoint, and a passing end-to-end smoke test (build → run → curl all four surfaces → recreate → data still present).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-docker-containerization/05-CONTEXT.md
@.planning/research/PITFALLS.md
@apps/server/src/index.ts
@apps/server/src/db/client.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub build context → image layers | The Docker build context is the whole repo tree; a missing `.dockerignore` entry could bake a secret file into a published layer |
| Public GHCR image → anyone with `docker pull` | Per D-01 the published image is public — anyone on the internet can pull and inspect its layers/binaries |
| Host filesystem `./data` ↔ container `/app/data` | The bind mount gives the container write access to a host directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Dockerfile / .dockerignore | high | mitigate | `.dockerignore` excludes `.env` (never `COPY`'d); `ANTHROPIC_API_KEY` reaches the container only via compose's `env_file: .env` at runtime, never as a Dockerfile `ENV`/`ARG` baked into a layer — Task 1's verify statically greps the Dockerfile for `ENV ANTHROPIC_API_KEY`/`ARG ANTHROPIC_API_KEY` (must be absent) and dynamically confirms `docker run --rm <image> printenv` never contains it |
| T-05-04 | Elevation of Privilege / Tampering | Host `./data` bind mount | medium | accept | Container runs as root (base image default) inside a single-container, no-multi-tenant, LAN-only deployment matching the project's existing no-auth trust model (CLAUDE.md) — same risk profile already accepted for the non-Docker deployment; adding a non-root `USER` directive would require chown coordination with the bind-mounted host directory's ownership, disproportionate complexity for this app's single-owner threat model |
| T-05-05 | Information Disclosure | Public GHCR image (D-01) | low | accept | Image contains only application code and compiled frontend bundles, no secrets or user data (see T-05-01's mitigation) — public visibility is an explicit, intentional decision (D-01) for a home-hobby project |
| T-05-SC | Tampering | npm/pnpm dependency installs | low | accept | This plan adds no new npm packages — Dockerfile/compose reuse only already-locked dependencies from `pnpm-lock.yaml`; `pnpm install --frozen-lockfile` prevents silent dependency substitution during the image build |
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end containerized stack — health check, Dockerfile, compose, one path proven</name>
  <precondition>docker and the Docker Compose v2 plugin are installed and the Docker daemon is running (checkable via: docker info)</precondition>
  <files>apps/server/src/index.ts, apps/server/src/index.test.ts, Dockerfile, compose.yml, .dockerignore</files>
  <read_first>
    apps/server/src/index.ts
    apps/server/src/db/client.ts
    pnpm-workspace.yaml
    package.json
    apps/server/package.json
    apps/server/src/routes/categories.test.ts
    .planning/research/PITFALLS.md
    .planning/phases/05-docker-containerization/05-CONTEXT.md
  </read_first>
  <action>
    RED first: create apps/server/src/index.test.ts importing buildApp from ./index.js, calling app.inject({ method: 'GET', url: '/health' }) and asserting statusCode 200, res.json() matches { status: 'ok' }, and res.json().timestamp is a string. Run pnpm --filter server test and confirm it fails (no /health route exists yet).

    GREEN: in apps/server/src/index.ts, inside buildApp(), add app.get('/health', async () => { return { status: 'ok', timestamp: new Date().toISOString() } }) — place it near the other route registrations, with a one-line comment noting this is the target compose.yml's healthcheck polls (D-08). Re-run pnpm --filter server test and confirm it passes.

    Create Dockerfile at repo root, two stages. Stage "builder": FROM node:22-slim AS builder; RUN apt-get update && apt-get install -y --no-install-recommends python3 build-essential && rm -rf /var/lib/apt/lists/* (fallback so better-sqlite3 can compile its native binding from source if no prebuilt matches this platform/Node ABI — pnpm-workspace.yaml's allowBuilds already permits its postinstall step to run, per PITFALLS.md Pitfall 1); RUN corepack enable && corepack prepare pnpm@11.17.0 --activate (pin the exact pnpm version this repo's pnpm-lock.yaml lockfileVersion 9.0 was generated with — keep this version string in sync with Plan 05-02's CI workflow); WORKDIR /app; COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./ plus each workspace package's package.json individually (apps/server/package.json, apps/barback/package.json, apps/patron/package.json, apps/bartender/package.json, packages/shared/package.json) into their matching subdirectories, copied before the rest of the source so this install layer stays cached across source-only rebuilds (PITFALLS.md Pitfall 7's layer-separation technique); RUN pnpm install --frozen-lockfile; COPY . . ; RUN pnpm build (root script: pnpm -r build — builds @my-bar/shared then server + all three Vite SPAs, preserving the apps/*/dist monorepo-relative layout); RUN pnpm prune --prod (drops devDependencies — vite, typescript, vitest — before the runtime copy, PITFALLS.md Pitfall 8). Stage "runtime": FROM node:22-slim AS runtime; ENV NODE_ENV=production; WORKDIR /app; COPY --from=builder /app /app as one wholesale copy — do not cherry-pick individual subdirectories, since pnpm's workspace node_modules is a tree of relative symlinks that breaks if only some directories are copied, and this also preserves the exact monorepo-relative layout apps/server/src/index.ts expects at runtime (path.join(__dirname, '../../barback/dist') etc.); EXPOSE 3000; CMD ["node", "apps/server/dist/index.js"]. Never add an ENV or ARG line for ANTHROPIC_API_KEY anywhere in this file — the key is supplied only at container-run time via compose's env_file.

    Create compose.yml at repo root: one service named app; image: ghcr.io/icariumtech/my-bar:latest; build: . (the Dockerfile lives in this same repo per CONTEXT.md's same-repo-layout decision, so this same file supports both a local `docker compose up -d` build-from-source and a later `docker compose pull` of the CI-published image); restart: unless-stopped; ports mapping "3000:3000" only, hardcoded, no ${PORT} substitution (D-07 — no port remapping); env_file: [.env]; volumes: ["./data:/app/data"] (the whole directory, never a single-file mount, so the .db-wal/.db-shm sidecars persist alongside the .db file — PITFALLS.md Pitfall 2, D-03); healthcheck block using the array form ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/health').then((r) => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))"] — using Node's built-in global fetch rather than curl/wget, since node:22-slim ships neither and adding either would grow the runtime image (PITFALLS.md Pitfall 8) — with interval: 30s, timeout: 5s, retries: 3, start_period: 10s (D-08).

    Create .dockerignore at repo root excluding: **/node_modules, **/dist, data, *.db, *.db-shm, *.db-wal, *.db-journal, .git, .env, .planning, .github, *.md, **/*.test.ts, **/*.test.tsx.
  </action>
  <verify>
    <automated>
! grep -qE '^(ENV|ARG) ANTHROPIC_API_KEY' Dockerfile
pnpm --filter server test -- index.test.ts
ENV_EXISTED=0; [ -f .env ] && ENV_EXISTED=1
if [ "$ENV_EXISTED" = "0" ]; then
  printf 'ANTHROPIC_API_KEY=test-key-for-smoke-test\nDB_PATH=./data/my-bar.db\nPORT=3000\nNODE_ENV=production\n' > .env
fi
DATA_EXISTED=0; [ -d ./data ] && DATA_EXISTED=1
docker compose build
docker compose up -d
CID=$(docker compose ps -q app)
timeout 60 sh -c "until [ \"\$(docker inspect -f '{{.State.Health.Status}}' $CID)\" = 'healthy' ]; do sleep 2; done"
curl -f http://localhost:3000/health
curl -f http://localhost:3000/patron/
curl -f http://localhost:3000/bartender/
curl -f http://localhost:3000/barback/
curl -f http://localhost:3000/api/ingredients
curl -sf -X POST http://localhost:3000/api/categories -H 'Content-Type: application/json' -d '{"name":"DockerSmokeTest"}' >/dev/null
! docker run --rm ghcr.io/icariumtech/my-bar:latest printenv | grep -q ANTHROPIC_API_KEY
docker compose down
docker compose up -d
CID2=$(docker compose ps -q app)
timeout 60 sh -c "until [ \"\$(docker inspect -f '{{.State.Health.Status}}' $CID2)\" = 'healthy' ]; do sleep 2; done"
curl -sf http://localhost:3000/api/categories | grep -q DockerSmokeTest
docker compose down
if [ "$ENV_EXISTED" = "0" ]; then rm -f .env; fi
if [ "$DATA_EXISTED" = "0" ]; then rm -rf ./data; fi
    </automated>
  </verify>
  <acceptance_criteria>
    - apps/server/src/index.test.ts asserts GET /health returns 200 with { status: 'ok', timestamp: string } and passes via pnpm --filter server test
    - Dockerfile contains no ENV or ARG line naming ANTHROPIC_API_KEY
    - docker compose build and docker compose up -d succeed, and the container's health status reaches "healthy" within 60s
    - curl to /health, /patron/, /bartender/, /barback/, and /api/ingredients on localhost:3000 all return success
    - a category created before a full docker compose down && docker compose up -d cycle is still returned by GET /api/categories afterward
    - the built image's baked environment (docker run --rm <image> printenv) never contains ANTHROPIC_API_KEY
  </acceptance_criteria>
  <done>Dockerfile, compose.yml, and .dockerignore are committed; docker compose up -d serves all three screens, the API, and Socket.IO from one container; data survives a full container recreate; the health endpoint has a passing unit test.</done>
</task>

<task type="auto">
  <name>Task 2: .env.example and .gitignore — document required/optional env vars, never commit a real one</name>
  <files>.env.example, .gitignore</files>
  <read_first>
    apps/server/src/db/client.ts
    apps/server/src/index.ts
    compose.yml
    .gitignore
    .planning/phases/05-docker-containerization/05-CONTEXT.md
  </read_first>
  <action>
    Create .env.example at repo root with four documented vars, each preceded by a one-to-two-line comment: PORT=3000 (comment: server listen port; compose.yml maps host 3000 to this exact value — don't change without updating compose.yml too, D-07); DB_PATH=./data/my-bar.db (comment: path relative to the app's working directory — /app in the container — combined with compose.yml's ./data:/app/data bind mount this persists the SQLite db plus its WAL sidecar files across restarts and updates, D-03); NODE_ENV=production (comment: disables the dev-only CORS grant in apps/server/src/index.ts); ANTHROPIC_API_KEY=your_api_key_here (comment: required, server-side only, never sent to the browser — get a key from https://console.anthropic.com/; the app starts fine without a real value filled in during this phase, it is consumed by AI features added later). Use an obviously-placeholder value for ANTHROPIC_API_KEY, never a real-looking key.

    Add a new line .env to .gitignore (the file currently has no .env entry at all — this is a real gap, since a filled-in .env holds a real ANTHROPIC_API_KEY and must never be committed). Do not add a wildcard pattern that would also match .env.example.
  </action>
  <verify>
    <automated>
[ "$(grep -v '^#' .env.example | grep -c '^PORT=')" = "1" ]
[ "$(grep -v '^#' .env.example | grep -c '^DB_PATH=')" = "1" ]
[ "$(grep -v '^#' .env.example | grep -c '^NODE_ENV=')" = "1" ]
[ "$(grep -v '^#' .env.example | grep -c '^ANTHROPIC_API_KEY=')" = "1" ]
! grep -qE '^ANTHROPIC_API_KEY=sk-' .env.example
grep -qx '\.env' .gitignore
cp .env.example .env
docker compose config >/dev/null
rm .env
    </automated>
  </verify>
  <acceptance_criteria>
    - .env.example exists at repo root with exactly one PORT=, DB_PATH=, NODE_ENV=, and ANTHROPIC_API_KEY= line each, every line preceded by an explanatory comment
    - ANTHROPIC_API_KEY's value is an obvious placeholder, not a real-looking secret
    - .gitignore contains a standalone .env line so a real .env is never committed
    - copying .env.example to .env lets `docker compose config` parse compose.yml without error
  </acceptance_criteria>
  <done>.env.example documents all four env vars with required/optional context; .gitignore prevents a real .env from ever being committed.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter server test` — apps/server/src/index.test.ts passes. Run `docker compose build && docker compose up -d` — container reaches "healthy". Curl all four surfaces (health, patron, bartender, barback) plus one API route from localhost:3000 — all succeed. Create data, recreate the container (`docker compose down && docker compose up -d`), confirm the data is still present. Confirm the built image's baked env never contains ANTHROPIC_API_KEY.
</verification>

<success_criteria>
- DOCK-01, DOCK-02, DOCK-03, DOCK-04 all satisfied
- One container serves Patron, Bartender, Barback, the REST API, and Socket.IO
- Data persists across a full container recreate via the ./data bind mount
- .env.example and updated .gitignore prevent secret leakage while documenting every required/optional var
</success_criteria>

## Artifacts this phase produces

- `Dockerfile` — multi-stage build (builder + runtime), both stages `FROM node:22-slim`
- `compose.yml` — single service `app`, port 3000:3000, `./data:/app/data` bind mount, `healthcheck:` block
- `.dockerignore` — excludes node_modules/dist/data/.env/.git/.planning/.github/*.md/test files from the build context
- `.env.example` — documents `PORT`, `DB_PATH`, `NODE_ENV`, `ANTHROPIC_API_KEY`
- `GET /health` route in `apps/server/src/index.ts` — returns `{ status: 'ok', timestamp }`
- `apps/server/src/index.test.ts` — unit test for the `/health` contract
- `.gitignore` gains a `.env` entry

<output>
Create `.planning/phases/05-docker-containerization/05-01-SUMMARY.md` when done
</output>
