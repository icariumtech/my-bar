---
phase: 05-docker-containerization
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - README.md
  - .github/workflows/docker-publish.yml
autonomous: true
requirements: [DOCK-05, DOCK-06]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A commit that touches only README.md or .planning/** files does not trigger docker-publish.yml at all (paths-ignore, D-05) — avoids rebuilding/republishing an identical image for doc-only commits"
    - "Back-to-back pushes to main never race on the latest GHCR tag — the workflow's concurrency group cancels any in-flight older run so only the newest commit's build ever publishes"
    - "Running docker compose pull && docker compose up -d a second time with no new image available is a safe no-op — ./data is never touched or reset — and README documents this exact command for both first-time setup and later updates"
    - "A push to main only publishes to GHCR after pnpm -r test (and the typecheck embedded in pnpm -r build) passes — a failing test blocks the image from ever reaching :latest (D-04)"
    - "Owner can complete first-time setup and later updates by following README.md alone, without reading source code (DOCK-05)"
    - { statement: "A merge to main triggers exactly one docker-publish.yml run via the push trigger — paths-ignore and branch scoping prevent duplicate/overlapping runs for the same commit from both pull_request and push events", verification: backstop }
    - { statement: "An interrupted docker compose pull (e.g. a network drop) leaves the currently-running container on the prior image untouched — docker compose up -d only swaps images after a successful pull completes", verification: backstop }
  prohibitions:
    - "CI must never push a non-main-branch build to GHCR (feature branches, pull requests, including from forks) — only pushes to main publish, per D-03"
  artifacts:
    - path: "README.md"
      provides: "Quickstart, Environment Variables, Accessing the App, Updating, Troubleshooting, Local Development sections"
      contains: "## Quickstart"
    - path: ".github/workflows/docker-publish.yml"
      provides: "test-gated CI pipeline that builds and publishes to GHCR on push to main"
      contains: "packages: write"
  key_links:
    - from: ".github/workflows/docker-publish.yml build-and-push job"
      to: "Dockerfile"
      via: "docker/build-push-action@v6 with context: ."
      pattern: "context: \\."
    - from: "README.md Environment Variables table"
      to: ".env.example"
      via: "documents the same four vars created in Plan 05-01"
      pattern: "ANTHROPIC_API_KEY"
---

<objective>
Wrap the working container (Plan 05-01) with the two things a solo owner needs to actually operate it unattended: documentation they can follow without reading source, and a CI pipeline that keeps ghcr.io/icariumtech/my-bar:latest in sync with main automatically.

Purpose: Plan 05-01 proved the stack works by hand; this plan makes that reproducible for the owner (README) and automatic for every future change (CI).
Output: A complete README.md and a `.github/workflows/docker-publish.yml` that tests, builds, and (on main only) publishes to GHCR.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-docker-containerization/05-CONTEXT.md
@.planning/research/PITFALLS.md
@Dockerfile
@compose.yml
@.env.example
@setup.sh
@start_server.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → GHCR | The CI workflow authenticates and pushes a public image; if the push step ran on untrusted input (a fork PR), an attacker could get a malicious image published as :latest |
| `pull_request` trigger → forked PRs | A PR from a fork triggers the `test`/build jobs against untrusted code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | GHCR image / CI publish pipeline | high | mitigate | Top-level workflow `permissions: contents: read`; `build-and-push` job additionally scopes only `packages: write` (never `write-all`); the `docker/login-action` and `push:` input on `docker/build-push-action` are both gated on `github.event_name == 'push' && github.ref == 'refs/heads/main'`, so a `pull_request` event (including from a fork, which GitHub already runs with a read-only `GITHUB_TOKEN`) can never publish |
| T-05-06 | Denial of Service | `restart: unless-stopped` + no alerting | low | accept | A crash-looping container restarts indefinitely without alerting; acceptable for a single-owner home deployment where README's Troubleshooting section (`docker compose logs -f`) is the expected debugging path — no additional alerting infrastructure is in scope (D-06) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: README — Quickstart, env vars, accessing the app, updating, troubleshooting</name>
  <files>README.md</files>
  <read_first>
    .env.example
    compose.yml
    Dockerfile
    setup.sh
    start_server.sh
    .planning/research/PITFALLS.md
    .planning/phases/05-docker-containerization/05-CONTEXT.md
  </read_first>
  <action>
    Replace README.md's placeholder title with a full document. One-paragraph project description first (home bar management with Patron/Bartender/Barback screens, inventory as source of truth). Then, in this order:

    "## Quickstart" — numbered copy-paste steps: git clone https://github.com/icariumtech/my-bar.git; cd my-bar; cp .env.example .env; edit .env and fill in ANTHROPIC_API_KEY (link https://console.anthropic.com/ for getting a key); docker compose pull && docker compose up -d (D-10 — first-time setup uses pull, not a local build, since ghcr.io/icariumtech/my-bar is public per D-01).

    "## Environment Variables" — a markdown table with columns Variable | Required | Default | Description, one row each for PORT (No, 3000, server listen port — must match compose.yml's 3000:3000 mapping), DB_PATH (No, ./data/my-bar.db, SQLite file path, persisted via the ./data bind mount), NODE_ENV (No, production, disables dev-only CORS), ANTHROPIC_API_KEY (Yes, none, server-side Claude API key, never sent to the browser) (D-10).

    "## Accessing the App" — once running, list http://<pi-ip>:3000/patron/, http://<pi-ip>:3000/bartender/, http://<pi-ip>:3000/barback/, noting kiosk devices (iPads, phones) must use the LAN IP printed by `hostname -I`, not localhost — mirrors start_server.sh's existing IP-detection messaging.

    "## Updating" — a section distinct from Quickstart (D-12): docker compose pull && docker compose up -d, with a one-line note that ./data persists untouched across the update.

    "## Troubleshooting" — docker compose logs -f for viewing logs, then two entries pulled directly from PITFALLS.md: better-sqlite3 native binding failures if a platform lacks a prebuilt binary (Pitfall 1 — rebuild the image so the build stage's python3/build-essential toolchain compiles it from source) and SQLite WAL corruption if the bind mount only covers the .db file instead of the whole data/ directory (Pitfall 2 — confirm compose.yml mounts ./data:/app/data, not a single file) (D-11).

    "## Local Development (without Docker)" — a short pointer to ./setup.sh and ./start_server.sh for contributors who want to run without Docker.
  </action>
  <verify>
    <automated>
grep -c '^## Quickstart' README.md
grep -c '^## Environment Variables' README.md
grep -c '^## Accessing the App' README.md
grep -c '^## Updating' README.md
grep -c '^## Troubleshooting' README.md
grep -c '^## Local Development' README.md
[ "$(grep -c 'docker compose pull && docker compose up -d' README.md)" -ge 2 ]
grep -c 'ANTHROPIC_API_KEY' README.md
grep -c 'console.anthropic.com' README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains ## Quickstart, ## Environment Variables, ## Accessing the App, ## Updating, ## Troubleshooting, and ## Local Development section headers
    - the literal command "docker compose pull && docker compose up -d" appears at least twice (Quickstart and Updating)
    - Environment Variables table lists all four vars from .env.example with Required/Default/Description
    - Troubleshooting references both the better-sqlite3 binding pitfall and the WAL bind-mount pitfall from PITFALLS.md
  </acceptance_criteria>
  <done>README.md lets the owner do first-time setup and later updates end to end without reading source code.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions — test-gated build and GHCR publish on main</name>
  <files>.github/workflows/docker-publish.yml</files>
  <read_first>
    package.json
    apps/server/package.json
    Dockerfile
    .planning/phases/05-docker-containerization/05-CONTEXT.md
  </read_first>
  <action>
    Create .github/workflows/docker-publish.yml. Trigger on push to branches: [main] and on pull_request targeting branches: [main], both with paths-ignore: ['README.md', '.planning/**'] (D-05 — skip CI entirely on doc-only commits). Add a top-level permissions: contents: read (least-privilege default) and a top-level concurrency block: group: docker-publish-${{ github.ref }}, cancel-in-progress: true — so back-to-back pushes to the same ref never race on which build finishes last and gets tagged latest.

    Job "test" on ubuntu-latest: actions/checkout@v4; pnpm/action-setup@v4 with version: 11.17.0 (must match the pnpm version pinned in Plan 05-01's Dockerfile — keep both in sync); actions/setup-node@v4 with node-version: '22' and cache: pnpm; a run step `pnpm install --frozen-lockfile`; a run step `pnpm -r build` (this compiles @my-bar/shared then every workspace package — each package's build script runs tsc, which is this project's typecheck, satisfying D-04's "and typecheck"); a run step `pnpm -r test`.

    Job "build-and-push": needs: test; runs on ubuntu-latest; job-level permissions: contents: read, packages: write (D-06 — scoped to exactly what publishing needs, never write-all). Steps: actions/checkout@v4; docker/setup-buildx-action@v3; a docker/login-action@v3 step named "Log in to GHCR", gated with if: github.event_name == 'push' && github.ref == 'refs/heads/main', logging into registry: ghcr.io with username: ${{ github.actor }} and password: ${{ secrets.GITHUB_TOKEN }}; a docker/build-push-action@v6 step named "Build and push" with context: ., push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }} (D-03 — pull requests build but never push, including PRs from forks), and tags: ghcr.io/icariumtech/my-bar:latest (D-02 — latest tag only, no semver or git-sha tags). Add no Slack/email/notification step of any kind — rely entirely on GitHub Actions' own default failure notification to the repo owner (D-06).
  </action>
  <verify>
    <automated>
test -f .github/workflows/docker-publish.yml
[ "$(grep -c 'paths-ignore' .github/workflows/docker-publish.yml)" -ge 2 ]
[ "$(grep -c 'packages: write' .github/workflows/docker-publish.yml)" -ge 1 ]
[ "$(grep -c 'ghcr.io/icariumtech/my-bar:latest' .github/workflows/docker-publish.yml)" -ge 1 ]
[ "$(grep -c 'pnpm -r test' .github/workflows/docker-publish.yml)" -ge 1 ]
[ "$(grep -c 'cancel-in-progress: true' .github/workflows/docker-publish.yml)" -ge 1 ]
[ "$(grep -c "github.event_name == 'push' && github.ref == 'refs/heads/main'" .github/workflows/docker-publish.yml)" -ge 2 ]
grep -c 'needs: test' .github/workflows/docker-publish.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/docker-publish.yml exists and triggers on push and pull_request to main with paths-ignore covering README.md and .planning/**
    - build-and-push job declares needs: test and only packages: write (plus contents: read) permissions
    - the push condition and the GHCR login step are both gated on github.event_name == 'push' && github.ref == 'refs/heads/main'
    - the only published tag is ghcr.io/icariumtech/my-bar:latest
    - a concurrency group with cancel-in-progress: true is present
  </acceptance_criteria>
  <done>Every push to main that changes non-doc files runs tests, then builds and publishes ghcr.io/icariumtech/my-bar:latest; pull requests (including from forks) build but never push.</done>
</task>

</tasks>

<verification>
Confirm README.md has all six required sections and the exact quickstart/update command appears at least twice. Confirm .github/workflows/docker-publish.yml exists, triggers correctly with paths-ignore, gates the GHCR push to main-only pushes, and never grants broader-than-necessary permissions.
</verification>

<success_criteria>
- DOCK-05 and DOCK-06 satisfied
- Owner can follow README.md alone for first-time setup and updates
- Every push to main (excluding doc-only commits) that passes tests publishes an updated ghcr.io/icariumtech/my-bar:latest image
- Pull requests build but never publish
</success_criteria>

## Artifacts this phase produces

- `README.md` — full setup/update/troubleshooting documentation (Quickstart, Environment Variables, Accessing the App, Updating, Troubleshooting, Local Development)
- `.github/workflows/docker-publish.yml` — `test` job (install/build/test) gating a `build-and-push` job that publishes `ghcr.io/icariumtech/my-bar:latest` on pushes to `main` only

<output>
Create `.planning/phases/05-docker-containerization/05-02-SUMMARY.md` when done
</output>
