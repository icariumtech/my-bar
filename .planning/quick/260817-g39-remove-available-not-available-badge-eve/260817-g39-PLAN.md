---
phase: quick
plan: 260817-g39
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/patron/src/components/TagRail.tsx
  - apps/patron/src/components/TagRail.test.tsx
  - apps/patron/src/components/RecipeBrowse.tsx
  - apps/patron/src/components/RecipeBrowse.test.tsx
  - apps/patron/src/components/RecipeCard.tsx
  - apps/patron/src/components/RecipeDetail.tsx
  - apps/patron/src/components/RecipeDetail.test.tsx
  - apps/patron/src/components/MakeableIndicator.tsx
  - apps/patron/src/components/MakeableIndicator.test.tsx
autonomous: true
requirements: [PATR-01, PATR-03]

must_haves:
  truths:
    - No "AVAILABLE"/"NOT AVAILABLE" badge text renders on any recipe row in the Patron browse list
    - No "AVAILABLE"/"NOT AVAILABLE" badge text renders on the Patron recipe detail screen (D-44's distinct missing-ingredients callout, which also says "Not Available", is unaffected and still renders for red recipes with something missing)
    - On first load, the Patron browse view shows only currently-makeable recipes (available-only default)
    - A guest can tap a toggle pinned to the bottom of the tag rail to reveal all recipes, including not-makeable ones (still dimmed via opacity-60 grayscale, D-43)
    - Tapping the toggle again returns to available-only
    - The tag filter and the availability toggle combine with AND, not OR — narrowing by tag while available-only is active only shows recipes matching both
    - Not-makeable recipes remain fully tappable (never disabled) whenever they are visible
  artifacts:
    - apps/patron/src/components/TagRail.tsx (new filterRecipesByAvailability export, new bottom-pinned toggle button, new showAvailableOnly/onToggleAvailableOnly props)
    - apps/patron/src/components/RecipeBrowse.tsx (new showAvailableOnly state, combined tag+availability filter wiring)
    - apps/patron/src/components/RecipeCard.tsx (MakeableIndicator import/usage removed)
    - apps/patron/src/components/RecipeDetail.tsx (MakeableIndicator import/usage removed, D-44 missing-line untouched)
    - apps/patron/src/components/MakeableIndicator.tsx and MakeableIndicator.test.tsx deleted (dead code once both call sites are gone)
  key_links:
    - RecipeBrowse's showAvailableOnly state -> TagRail's showAvailableOnly/onToggleAvailableOnly props -> toggle button's aria-pressed/onClick — must stay wired or the toggle silently does nothing
    - RecipeBrowse's filterRecipesByAvailability(filterRecipesByTag(allRecipes, selectedTagId), showAvailableOnly) composition — both filters must apply together (AND)
    - RecipeCard.tsx / RecipeDetail.tsx no longer importing the deleted MakeableIndicator.tsx — build must stay clean after deletion
---

<objective>
Remove the AVAILABLE/NOT AVAILABLE badge (MakeableIndicator) from the Patron browse card and detail screen everywhere it renders, delete the now-dead MakeableIndicator component, and add a new show-all/available-only toggle pinned to the bottom of the Patron TagRail — defaulting to available-only — whose filter combines with the existing tag filter (AND).

Purpose: The badge duplicates the D-43 dimming signal and clutters the header row; the owner wants a single explicit control that lets a patron choose between "what can I actually make right now" (the common case, default) and "show me everything."
Output: RecipeCard.tsx and RecipeDetail.tsx render no badge text; TagRail.tsx gains a bottom-pinned availability toggle; RecipeBrowse.tsx combines tag and availability filtering; MakeableIndicator.tsx/.test.tsx are deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/patron/src/components/TagRail.tsx
@apps/patron/src/components/TagRail.test.tsx
@apps/patron/src/components/RecipeBrowse.tsx
@apps/patron/src/components/RecipeBrowse.test.tsx
@apps/patron/src/components/RecipeCard.tsx
@apps/patron/src/components/RecipeDetail.tsx
@apps/patron/src/components/RecipeDetail.test.tsx
@apps/patron/src/components/MakeableIndicator.tsx
@apps/patron/src/index.css
@.planning/phases/03-patron-browse-experience/03-CONTEXT.md

This quick task supersedes 03-CONTEXT.md's D-45 ("not-makeable drinks remain fully visible... no hide/filter toggle") per a direct product decision confirmed with the user: the badge is removed everywhere, and a new available-only/show-all toggle is added to the Patron rail, defaulting to available-only. D-42 (2-state green=makeable collapse), D-43 (opacity-60 grayscale dimming), and D-44 (missing-ingredient detail line on RecipeDetail only) all remain unchanged and must not be touched — D-43's dimming becomes the sole remaining visual makeable/not-makeable signal once the badge is gone.

Baseline confirmed by planner: 6 test files / 28 tests currently pass via `pnpm --filter patron test`. `MakeableIndicator` is currently referenced only in RecipeCard.tsx, RecipeDetail.tsx, its own component file, and its own test file — no other usages anywhere in `apps/`.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add bottom-pinned availability toggle to TagRail and wire combined tag+availability filtering into RecipeBrowse</name>
  <files>apps/patron/src/components/TagRail.tsx, apps/patron/src/components/TagRail.test.tsx, apps/patron/src/components/RecipeBrowse.tsx, apps/patron/src/components/RecipeBrowse.test.tsx</files>
  <action>
    In TagRail.tsx: add and export a new pure function `filterRecipesByAvailability(recipes: Recipe[], showAvailableOnly: boolean): Recipe[]`, placed beside the existing `filterRecipesByTag` (Interface-First — pure logic ahead of the component, same file convention already used here). When `showAvailableOnly` is false, return the exact input array reference unchanged (mirrors `filterRecipesByTag`'s undefined-tagId contract). When true, return only recipes where `overallStatus === 'green'` (D-42's makeable value).

    Add two new required props to `TagRailProps`: `showAvailableOnly: boolean` and `onToggleAvailableOnly: () => void`. Wrap the existing `TAG_GROUP_META.map(...)` block in a new `<div className="flex flex-col items-center gap-lg w-full">` wrapper so it becomes a single child of the outer rail container, then add a new `<button>` as a later sibling of that wrapper (still inside the same outer `flex flex-col` rail container) with `mt-auto` in its className so it's pushed to the bottom of the full-height rail. The button: `type="button"`, `aria-label="Availability filter"`, `aria-pressed={showAvailableOnly}`, `onClick={onToggleAvailableOnly}`, sized `w-14 h-14` matching the existing group buttons, styled with the exact same two-state class pattern already used for `isExpanded` (`glow-orange bg-patron-accent/20 text-patron-accent` when `showAvailableOnly` is true, `glow-orange-subtle text-patron-accent` when false) plus `flex flex-col items-center justify-center gap-xs rounded-xl mt-auto transition-colors`. Inside the button render an `EyeOff` icon (size 22, `aria-hidden="true"`) when `showAvailableOnly` is true, an `Eye` icon (same size/aria) when false — import both from `lucide-react` alongside the existing `Martini, Sparkles, Leaf, Flame` imports — plus a `<span className="text-[10px] uppercase tracking-wide">` label reading `AVAILABLE` (true) or `ALL` (false).

    In RecipeBrowse.tsx: import `filterRecipesByAvailability` alongside the existing `filterRecipesByTag` import from `./TagRail.js`. Add `const [showAvailableOnly, setShowAvailableOnly] = useState(true)` beside the existing `selectedTagId`/`viewingId` state (CONFIRMED default — available-only on load). Compute `filteredRecipes` by applying both filters together: first `filterRecipesByTag(allRecipes, selectedTagId)`, then `filterRecipesByAvailability(...)` on that result — both must apply (AND), replacing the current single-filter line. Pass `showAvailableOnly={showAvailableOnly}` and `onToggleAvailableOnly={() => setShowAvailableOnly((prev) => !prev)}` as two new props on the `<TagRail>` element, alongside its existing `recipes`/`selectedTagId`/`onSelectTag` props (`recipes` stays `allRecipes`, unfiltered — D-36's active-tag computation must see every tag regardless of the availability toggle). Add a short note to the component's leading comment block that this supersedes 03-CONTEXT.md's D-45 ("no hide/filter toggle") per this quick task's confirmed product decision.

    In TagRail.test.tsx: add `showAvailableOnly={true}` and `onToggleAvailableOnly={vi.fn()}` to all four existing `render(<TagRail .../>)` calls (now-required props). Import `filterRecipesByAvailability` alongside the existing `TagRail, getActiveTagIds, filterRecipesByTag` import. Add a `describe('filterRecipesByAvailability', ...)` block mirroring the existing `filterRecipesByTag` block: one test asserting the same-array-reference return when `showAvailableOnly` is false, one test asserting only `overallStatus === 'green'` recipes are returned when true (use fixtures spanning green/yellow/red statuses). Add a `describe('availability toggle', ...)` block with three tests: (1) with `showAvailableOnly={true}` the rendered button (`screen.getByRole('button', { name: 'Availability filter' })`) has `aria-pressed="true"` and the text `AVAILABLE` is present; (2) with `showAvailableOnly={false}` the button has `aria-pressed="false"` and the text `ALL` is present; (3) clicking the button calls `onToggleAvailableOnly` exactly once.

    In RecipeBrowse.test.tsx: replace the existing "keeps not-makeable recipes visible and tappable in the grid (never hidden by filter/status)" test — its D-45 premise no longer holds by default now that the toggle starts in available-only mode — with two new tests using the existing `BASE_RECIPE`/`OTHER_RECIPE`/`WHISKEY`/`SWEET` fixtures: (1) a not-makeable recipe (`overallStatus: 'red'`) is hidden on initial render (available-only default) and reappears after clicking `screen.getByRole('button', { name: 'Availability filter' })`; (2) with the toggle switched to show-all, the existing tag filter (clicking the `Spirit` rail button then the `Whiskey` submenu button) still narrows the visible set down to only the tag-matching recipe — proving the two filters combine with AND rather than one overriding the other.
  </action>
  <verify>
    <automated>cd /home/gjohnson/src/my-bar && pnpm --filter patron test</automated>
  </verify>
  <done>TagRail renders a bottom-pinned (mt-auto) availability toggle button defaulting to aria-pressed="true" / "AVAILABLE" label; clicking it calls onToggleAvailableOnly; filterRecipesByAvailability is exported and returns only green recipes when true, the same array reference when false; RecipeBrowse combines filterRecipesByTag and filterRecipesByAvailability (AND); a not-makeable recipe is hidden by default and reappears after toggling to show-all, with the tag filter still narrowing correctly on top; pnpm --filter patron test passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the AVAILABLE/NOT AVAILABLE badge everywhere, delete now-dead MakeableIndicator, run final verification</name>
  <files>apps/patron/src/components/RecipeCard.tsx, apps/patron/src/components/RecipeDetail.tsx, apps/patron/src/components/RecipeDetail.test.tsx, apps/patron/src/components/MakeableIndicator.tsx, apps/patron/src/components/MakeableIndicator.test.tsx</files>
  <action>
    In RecipeCard.tsx: remove the MakeableIndicator import line. Replace the header `<div className="flex items-start justify-between gap-sm">` wrapper — which contained the name `<h3>` and the MakeableIndicator element — with just the bare `<h3 className="text-white font-semibold break-words">{recipe.name}</h3>` line (the flex/justify-between wrapper existed only to lay the badge out beside the name; with the badge gone there's nothing to justify against, so drop the wrapper along with the badge, not just the badge line). Leave the tags paragraph, the ingredients paragraph, and the "Tap to View" affordance completely unchanged. Append a short note to the file's leading comment block recording this badge-removal change (D-43 dimming is now the sole not-makeable visual signal in this view), without deleting the existing D-38/D-43/260813-ea3/260817-fkv provenance notes.

    In RecipeDetail.tsx: remove the MakeableIndicator import line and delete the MakeableIndicator element (the one directly above the `showMissing &&` block). Do not touch the `showMissing` paragraph itself (the "Not Available — {missing categories} missing." line) — that is D-44's distinct missing-ingredient callout, not the badge, and must keep rendering unchanged for red recipes with missing categories.

    Delete apps/patron/src/components/MakeableIndicator.tsx and apps/patron/src/components/MakeableIndicator.test.tsx entirely — confirmed dead code once the two call sites above are removed (planner's grep across apps/ found MakeableIndicator referenced only in these two component files plus their two test files).

    In RecipeDetail.test.tsx: in the "renders a missing-ingredients line with 'Not Available' and the missing category name for a red recipe" test, remove the now-redundant `screen.getAllByText(/Not Available/).length` assertion and its preceding comment about the badge (the "Dry Vermouth missing." assertion above it already fully covers this behavior). Rewrite the "renders the 'Not Available' badge but NO missing-ingredients section for a yellow recipe with nothing truly missing" test: rename its description to "renders no missing-ingredients section for a yellow recipe with nothing truly missing (D-44)", remove its badge-text assertion, and keep only the assertion that no "missing" text renders.

    Finally, run the full verification pair required to close out this quick task: the Patron test suite, then the Patron build.
  </action>
  <verify>
    <automated>cd /home/gjohnson/src/my-bar && pnpm --filter patron test && pnpm --filter patron build</automated>
  </verify>
  <done>No MakeableIndicator import or usage remains in RecipeCard.tsx or RecipeDetail.tsx; MakeableIndicator.tsx and MakeableIndicator.test.tsx are deleted; RecipeDetail's D-44 missing-ingredients line still renders unchanged for red recipes with missing categories; pnpm --filter patron test passes with the full suite green; pnpm --filter patron build succeeds with no TypeScript or Vite errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure client-side presentational/state change on the already-reviewed Patron browse surface — no new data flow, no new trust boundary crossed. Existing Patron API/WS boundaries are untouched. |

## STRIDE Threat Register

No new threats introduced. This plan removes a display-only component, deletes its dead code, and adds a client-side-only boolean toggle with no new network calls, no new inputs from an untrusted source, and no new npm dependency (lucide-react is already a Patron dependency).
</threat_model>

<verification>
1. Visual: no "AVAILABLE"/"NOT AVAILABLE" badge renders on any Patron browse row or on the detail screen; D-43 dimming (opacity-60 grayscale) remains the only visual not-makeable signal in the list.
2. Visual: the Patron TagRail shows a new icon+label toggle pinned to the bottom of the rail; on load it reads available-only (dimmed/hidden not-makeable recipes are absent from the list); tapping it reveals all recipes including dimmed not-makeable ones; tapping again returns to available-only.
3. Visual: with the toggle showing all recipes, selecting a tag from the rail still narrows the visible list to only recipes matching that tag AND makeable status where relevant.
4. `pnpm --filter patron test` — full suite green.
5. `pnpm --filter patron build` — clean TypeScript + Vite build.
</verification>

<success_criteria>
- RecipeCard.tsx and RecipeDetail.tsx render no MakeableIndicator badge text anywhere.
- MakeableIndicator.tsx and MakeableIndicator.test.tsx are deleted (confirmed dead code).
- D-43 dimming, D-44's missing-ingredients line, and D-42's tri-to-2-state collapse are all preserved unchanged.
- TagRail.tsx has a new bottom-pinned availability toggle defaulting to available-only, combining with the existing tag filter via AND.
- All existing Patron behavior not explicitly changed by this plan (loading/error/empty states, tag filtering, detail navigation, live-sync) is unaffected.
- No new npm dependencies added.
- apps/barback and apps/server untouched.
</success_criteria>

<output>
Create `.planning/quick/260817-g39-remove-available-not-available-badge-eve/260817-g39-SUMMARY.md` when done
</output>
