---
phase: quick
plan: 260817-o11
subsystem: api
tags: [fastify, drizzle, react-query, antd, tanstack-query]

# Dependency graph
requires:
  - phase: 02-recipes-makeable-engine
    provides: "recipeIngredients.ingredientId onDelete:'set null' FK (D-31), and the shipped DELETE /api/recipes/:id + useDeleteRecipe pattern this plan mirrors"
provides:
  - "DELETE /api/ingredients/:id route (204/404, no reference-count guard)"
  - "useDeleteIngredient() mutation hook in apps/barback/src/api/useIngredients.ts"
  - "Delete icon button on IngredientRow, mirroring RecipeRow's Modal.confirm UX"
affects: [barback-ingredients, recipes-makeable-status]

# Actuals (#2632)
actuals:
  tokens: 3226
  tasks: 2
  commits: 4

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "DELETE routes: direct SELECT-id existence check (not the joined loadRecipe/loadIngredient helper) then delete then 204, 404 on miss — established by recipes.ts, now mirrored by ingredients.ts"
    - "Frontend delete hooks bypass apiFetch() and call fetch() directly so a non-204 body's `error` field can be read into a named *Error class"

key-files:
  created: []
  modified:
    - apps/server/src/routes/ingredients.ts
    - apps/server/src/routes/ingredients.test.ts
    - apps/barback/src/api/useIngredients.ts
    - apps/barback/src/api/useIngredients.test.tsx
    - apps/barback/src/components/IngredientRow.tsx

key-decisions:
  - "No reference-count guard on ingredient delete (unlike categories/glassware) — recipeIngredients.ingredientId's onDelete:'set null' (schema.ts:75) is the DB's own safety net; a locked recipe line degrades to category-only instead of blocking the delete"
  - "Added a useIngredients.test.tsx hook test for useDeleteIngredient (success + dual invalidation, and non-204 error) even though the plan's task 2 action didn't explicitly call for a new test — kept the tdd=\"true\" RED/GREEN cycle honest and mirrors the file's existing G-02-9 hook-test precedent; no new IngredientRow/RecipeRow component test file was added since no such precedent exists for RecipeRow's Delete button either"

patterns-established:
  - "Ingredient/recipe delete mutations both invalidate ['ingredients']+['recipes'] (or just ['recipes']) in onSettled, never onSuccess-only"

requirements-completed: []

coverage:
  - id: D1
    description: "DELETE /api/ingredients/:id returns 204 on success, 404 on unknown/repeated-delete id, no reference-count guard"
    verification:
      - kind: integration
        ref: "apps/server/src/routes/ingredients.test.ts#DELETE /api/ingredients/:id"
        status: pass
    human_judgment: false
  - id: D2
    description: "Deleting an ingredient locked to a recipe line degrades that line to category-only (ingredientId set null) via the HTTP route, not just direct DB access"
    verification:
      - kind: integration
        ref: "apps/server/src/routes/ingredients.test.ts#DELETE /api/ingredients/:id > MATCH-05 via HTTP"
        status: pass
    human_judgment: false
  - id: D3
    description: "useDeleteIngredient hook: DELETE fetch, DeleteIngredientError on non-204, dual ['ingredients']+['recipes'] invalidation onSettled"
    verification:
      - kind: unit
        ref: "apps/barback/src/api/useIngredients.test.tsx#useDeleteIngredient invalidates both ['ingredients'] and ['recipes'] on settle (success)"
        status: pass
      - kind: unit
        ref: "apps/barback/src/api/useIngredients.test.tsx#useDeleteIngredient throws a DeleteIngredientError carrying the server message on non-204, and still invalidates on settle"
        status: pass
    human_judgment: false
  - id: D4
    description: "IngredientRow renders a Delete icon button in the trailing controls, outside the swipe-handler div, opening a named Modal.confirm dialog identical in shape to RecipeRow's"
    verification: []
    human_judgment: true
    rationale: "No component-level test precedent exists for either RecipeRow's or IngredientRow's Delete button (visual placement, swipe-handler isolation, Modal.confirm content) — verified by pnpm --filter barback build (type-checks clean) and full test suite passing with no regressions, but the actual on-screen tap/confirm/dismiss flow needs a human look."

duration: 3min
completed: 2026-08-17
status: complete
---

# Quick Task 260817-o11: Add Ingredient Delete Summary

**DELETE /api/ingredients/:id route + useDeleteIngredient hook + Delete icon button on IngredientRow, mirroring the shipped recipe-delete pattern exactly, with no reference-count guard since the schema's onDelete:'set null' already handles a locked recipe line gracefully.**

## Performance

- **Duration:** ~3 min (execution) — 2 test/impl cycles, TDD RED→GREEN each
- **Started:** 2026-08-17T17:22:33-05:00
- **Completed:** 2026-08-17T17:24:51-05:00
- **Tasks:** 2/2
- **Files modified:** 5

## Accomplishments
- `DELETE /api/ingredients/:id` — 404-check-then-delete-then-204, no reference-count guard, emits `app.io?.emit('inventory:changed')` matching the file's other mutation routes
- Four new backend tests: success+204 with a direct-query zero-rows check, 404 on unknown id, 204-then-404 on repeated delete, and a MATCH-05-via-HTTP case proving a recipe line locked to the deleted ingredient degrades to category-only (`ingredientId` null) rather than erroring
- `useDeleteIngredient()` hook mirroring `useDeleteRecipe`'s shape exactly: typed `DeleteIngredientError`, dual `['ingredients']`+`['recipes']` invalidation on settle
- Delete icon button on `IngredientRow`, placed in the trailing controls after Edit, outside the swipe-handler div, using the identical `Modal.confirm` Copywriting Contract as `RecipeRow` (named title, "This can't be undone.", danger styling, `isPending`-driven loading state)

## Task Commits

Each task was committed atomically (TDD RED → GREEN pairs):

1. **Task 1: DELETE /api/ingredients/:id — route + tests**
   - `test(quick-260817-o11)` - `90bed4a` (RED: 4 new failing tests)
   - `feat(quick-260817-o11)` - `97e3daf` (GREEN: route implementation, all 97 tests pass)
2. **Task 2: useDeleteIngredient hook + Delete button on IngredientRow**
   - `test(quick-260817-o11)` - `e3c8ce3` (RED: 2 new failing hook tests)
   - `feat(quick-260817-o11)` - `f4a051c` (GREEN: hook + button implementation, build clean, all 84 tests pass)

## Files Created/Modified
- `apps/server/src/routes/ingredients.ts` - Added DELETE `/:id` route
- `apps/server/src/routes/ingredients.test.ts` - Added `DELETE /api/ingredients/:id` describe block (4 tests)
- `apps/barback/src/api/useIngredients.ts` - Added `DeleteIngredientError` class + `useDeleteIngredient()` hook
- `apps/barback/src/api/useIngredients.test.tsx` - Added 2 hook tests for `useDeleteIngredient`
- `apps/barback/src/components/IngredientRow.tsx` - Added Delete icon button + `handleDelete()` using `Modal.confirm`

## Decisions Made
- No reference-count guard added to the DELETE route — confirmed via `recipeIngredients.ingredientId`'s `onDelete: 'set null'` (schema.ts:75), already exercised via direct DB delete in `recipes.test.ts:487-519` (MATCH-05); this plan additionally proves it reachable through the new HTTP route specifically.
- Added a hook-level test file update (`useIngredients.test.tsx`) for Task 2 even though the plan's `<action>` text for that task didn't explicitly instruct writing new tests — this follows the task's `tdd="true"` frontmatter attribute and the file's own established G-02-9 hook-test precedent (`useToggleStock`/`useUpdateIngredient` cross-entity-invalidation tests). No new component-level test file was added for `IngredientRow`'s Delete button, since no such precedent exists for `RecipeRow`'s equivalent button either — build type-checking and the full existing suite (no regressions) stand in as the automated proof, with the visual/interaction flow flagged for human UAT (coverage D4).

## Deviations from Plan

None - plan executed exactly as written. (The added hook test noted above is a strengthening of Task 2's TDD rigor, not a deviation from scope — it tests exactly the behavior the plan's `<behavior>` block already specified for the hook.)

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Ingredient delete now works end-to-end (route, hook, UI) — Barback owner can remove a bottle from inventory the same way they remove a recipe.
- Recommend a manual sanity pass (per the plan's `<verification>` item 4): start the stack, delete a bottle referenced by a recipe's locked-specific ingredient line, and confirm that recipe's detail view still shows a valid category-only status rather than an error.

---
*Phase: quick*
*Completed: 2026-08-17*

## Self-Check: PASSED

All 5 modified files found on disk; all 4 task commits (90bed4a, 97e3daf, e3c8ce3, f4a051c) found in git log.
