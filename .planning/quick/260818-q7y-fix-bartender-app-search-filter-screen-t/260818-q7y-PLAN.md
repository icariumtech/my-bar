---
phase: quick
plan: 260818-q7y
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/bartender/src/components/FullScreenScrollArea.tsx
  - apps/bartender/src/components/FullScreenScrollArea.test.tsx
  - apps/bartender/src/components/RecipeSearchFilter.tsx
  - apps/bartender/src/components/RecipeOrOrderDetail.tsx
autonomous: true
requirements: []

estimate:
  tokens: 26000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On RecipeSearchFilter (Recipes tab -> Search & Filter), the Clear and Apply buttons are always reachable above BottomTabBar — never rendered underneath/behind the fixed tab bar — matching how RecipeOrOrderDetail's Done button already clears the bar."
    - "Both of Bartender's full-screen sub-views (RecipeSearchFilter, RecipeOrOrderDetail) share one canonical layout wrapper for 'header + scrollable content clearing the fixed BottomTabBar', so a future full-screen view cannot reintroduce this bug by duplicating the header/main scaffold and forgetting the footer-outside-clearance mistake."
    - "No visual/behavioral regression: Back button, centered title, tag Collapse sections, Done button conditional rendering, and all existing RecipeSearchFilter/RecipeOrOrderDetail test assertions (by role/text, not DOM position) continue to pass unchanged."
  artifacts:
    - "apps/bartender/src/components/FullScreenScrollArea.tsx — shared component: min-height:100vh flex column, back-button header (onBack/title/titleStyle props matching apps/barback/src/components/FullScreenHeader.tsx's signature), and ONE scrollable <main> with paddingBottom: 'calc(16px + 48px + env(safe-area-inset-bottom))' wrapping `children` — the 260812-drh clearance convention, now enforced in one place instead of duplicated per-view."
    - "apps/bartender/src/components/FullScreenScrollArea.test.tsx — renders title, Back button calls onBack, children render and are reachable."
    - "apps/bartender/src/components/RecipeSearchFilter.tsx — uses FullScreenScrollArea; Clear/Apply buttons moved INSIDE children (end of the scrollable content), no more footer div rendered as a sibling after the old <main>."
    - "apps/bartender/src/components/RecipeOrOrderDetail.tsx — uses FullScreenScrollArea (titleStyle={{ fontSize: 28, fontWeight: 600 }} preserving its existing Display-role typography); Done button unchanged, still the last item inside children."
  key_links:
    - "RecipesTab 'filter' view -> RecipeSearchFilter -> FullScreenScrollArea's <main> (flex:1, overflow:auto, paddingBottom clears BottomTabBar) -> Clear/Apply buttons rendered as the last children -> always scrollable into view above the fixed BottomTabBar (zIndex:10, position:fixed, bottom:0)."
---

<objective>
Fix the Bartender app's RecipeSearchFilter full-screen view so its Clear/Apply footer buttons are always reachable above the fixed BottomTabBar, and extract the correct header+scrollable-clearance layout into one shared component (`FullScreenScrollArea`) that both of Bartender's full-screen sub-views use — so the bug class (a screen's bottom content rendering outside the tab bar's reserved clearance) cannot recur from copy-pasted, divergent header/main scaffolding.

Purpose: `BottomTabBar` is already `position: fixed` (correct, already matches the 260812-drh convention applied to Barback) and `RecipeOrOrderDetail.tsx` already reserves the correct `paddingBottom` clearance on its scrollable `<main>` for the bar's ~48px+safe-area height, with its action button (Done) living INSIDE that scrollable, padded region. `RecipeSearchFilter.tsx` instead renders its Clear/Apply row as a separate `<div>` AFTER `<main>` — outside the reserved-clearance scroll area — so those buttons sit at the natural bottom of the component's `minHeight: 100vh` column, directly underneath the fixed BottomTabBar (zIndex 10), unreachable/covered. This is the one place in the whole codebase (both apps) that diverges from the established "action buttons live inside the padded, scrollable `<main>`" convention (confirmed against `RecipeOrOrderDetail.tsx` and Barback's `AddEditIngredientView.tsx`/`AddEditRecipeView.tsx`, all of which put their Save/Done button inside `<main>`).
Output: RecipeSearchFilter's Clear/Apply buttons are always visible/tappable above the tab bar; a new `FullScreenScrollArea` component is the single source of truth for this layout shape across Bartender's full-screen views, used by both RecipeSearchFilter and RecipeOrOrderDetail.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@apps/bartender/src/components/BottomTabBar.tsx
@apps/bartender/src/components/RecipeSearchFilter.tsx
@apps/bartender/src/components/RecipeOrOrderDetail.tsx
@apps/bartender/src/components/RecipeSearchFilter.test.tsx
@apps/bartender/src/components/RecipeOrOrderDetail.test.tsx
@apps/bartender/src/App.tsx
@apps/barback/src/components/FullScreenHeader.tsx
@apps/barback/src/components/views/AddEditIngredientView.tsx

Reference pattern — the working convention (already proven in this codebase, see `apps/barback/src/components/views/AddEditIngredientView.tsx` lines 72-127 and `apps/bartender/src/components/RecipeOrOrderDetail.tsx` lines 38-166, and the 260812-drh quick-task SUMMARY that established it): a `min-height: 100vh` flex column containing a header, then exactly ONE `<main style={{ flex: 1, padding: 16, paddingBottom: 'calc(16px + 48px + env(safe-area-inset-bottom))', overflow: 'auto' }}>` that wraps ALL remaining content INCLUDING any bottom action button(s). `BottomTabBar` is `position: fixed; bottom: 0; zIndex: 10`, so anything NOT inside that padded, scrollable `<main>` renders at the literal bottom of the `100vh` column — directly under the fixed bar. `RecipeSearchFilter.tsx`'s current Clear/Apply `<div>` (lines 174-188 of the file as read) is exactly that mistake: a sibling after `<main>`, not inside it.

Reference pattern — `apps/barback/src/components/FullScreenHeader.tsx`: the existing shared-header extraction precedent this task's new component follows for its own header portion (`onBack`, `title`, optional `titleStyle` props; 3-column centering with two 48px flexShrink:0 spacer cells around a `flex:1, textAlign:center` `<h2>`). Bartender does not import from `apps/barback` (separate app bundle) — `FullScreenScrollArea` is a new, Bartender-local component, not a shared cross-app import.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared FullScreenScrollArea layout component</name>
  <files>apps/bartender/src/components/FullScreenScrollArea.tsx, apps/bartender/src/components/FullScreenScrollArea.test.tsx</files>
  <action>
    Create `FullScreenScrollArea.tsx` exporting a component with props `{ onBack: () => void; title: string; titleStyle?: CSSProperties; children: ReactNode }`. Render exactly the structure already proven correct in `RecipeOrOrderDetail.tsx`/Barback's `AddEditIngredientView.tsx`: an outer `<div style={{ display: 'flex', flexDirection: 'column', minHeight: '100vh' }}>` containing (1) a `<header>` identical in shape to `apps/barback/src/components/FullScreenHeader.tsx` — a `display:flex, alignItems:center, padding:16, borderBottom:'1px solid rgba(255,255,255,0.1)'` row with a 48px flexShrink:0 cell holding a circular primary `Button` (`icon={<ArrowLeftOutlined />}`, `aria-label="Back"`, `onClick={onBack}`, `style={{ width: 48, height: 48, minWidth: 48, padding: 0 }}`), a centered `<h2 className="text-white" style={{ flex: 1, textAlign: 'center', ...titleStyle }}>{title}</h2>`, and a matching empty 48px flexShrink:0 spacer cell; and (2) exactly one `<main style={{ flex: 1, padding: 16, paddingBottom: 'calc(16px + 48px + env(safe-area-inset-bottom))', overflow: 'auto' }}>{children}</main>`. Add a code comment above the component explaining that ALL screen content — including any bottom action button(s) — must be passed as `children` so it renders inside this reserved clearance, since anything rendered as a sibling of this component (not as its children) will sit underneath the fixed BottomTabBar, referencing this exact bug (quick 260818-q7y) as the reason.

    Then write `FullScreenScrollArea.test.tsx` (same `render`/`screen`/`fireEvent` pattern as `RecipeSearchFilter.test.tsx`): one test renders the component with a `title` prop and asserts `screen.getByText(title)` is present, then clicks the button with role `'button'`/name `'Back'` and asserts the `onBack` mock was called once; a second test renders arbitrary `children` (e.g. a `<button>Apply</button>`) and asserts `screen.getByRole('button', { name: 'Apply' })` is present — proving children render reachably inside the component.
  </action>
  <verify>
    <automated>cd /home/gjohnson/src/my-bar && pnpm --filter bartender test -- FullScreenScrollArea.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>FullScreenScrollArea.tsx exists exporting the described component; FullScreenScrollArea.test.tsx's two tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RecipeSearchFilter and RecipeOrOrderDetail onto the shared wrapper, fixing the Clear/Apply clearance bug</name>
  <files>apps/bartender/src/components/RecipeSearchFilter.tsx, apps/bartender/src/components/RecipeOrOrderDetail.tsx</files>
  <action>
    In `RecipeSearchFilter.tsx`: import `FullScreenScrollArea` from `'./FullScreenScrollArea.js'`. Replace the component's returned JSX (currently the outer `minHeight:100vh` div containing `<header>`, `<main>`, and the standalone Clear/Apply footer `<div>`) with `<FullScreenScrollArea onBack={onBack} title="Search &amp; Filter">...</FullScreenScrollArea>`, moving the existing `<main>`'s inner content (the `gap:16` column with the name `Input` and the tags `Collapse` section) to be `FullScreenScrollArea`'s children, and appending the Clear/Apply button row as the LAST item inside that same `gap:16` column (i.e., inside `children`, not as a sibling after it) — keep the two buttons' existing `flex:1, minHeight:48` styling and `handleClear`/`handleApply` handlers unchanged, just relocate the wrapping `<div style={{ display:'flex', gap:8 }}>` (drop its now-redundant `padding`/`borderTop`, since it's no longer a separate footer bar — it's simply the final row inside the scrollable content, consistent with how Barback's `AddEditIngredientView.tsx` places its Save button as the final item inside `<main>`'s content). Remove the now-unused `ArrowLeftOutlined` import and the inline `<header>`/`<main>` JSX this replaces.

    In `RecipeOrOrderDetail.tsx`: same replacement — import `FullScreenScrollArea`, replace the outer div/header/main with `<FullScreenScrollArea onBack={onBack} title={recipe.name} titleStyle={{ fontSize: 28, fontWeight: 600 }}>...</FullScreenScrollArea>` (the `titleStyle` preserves this view's existing Display-role typography override, matching its current inline `<h2>` style), passing the existing `<main>`'s inner `gap:16` content div as children unchanged (badge, status sentences, ingredients list, method, glassware, garnish, and the conditional Done button all stay in their current relative order — no repositioning needed here since Done already lives inside that content div). Remove the now-unused `ArrowLeftOutlined` import.

    Do not change `RecipeSearchFilter.test.tsx` or `RecipeOrOrderDetail.test.tsx` — both query by role/text, not DOM position, so they should pass unmodified against the new structure; if either fails, fix the component (not the test) to preserve the existing accessible names/roles (`aria-label="Back"`, heading text, `Apply`/`Clear`/`Done` button names).
  </action>
  <verify>
    <automated>cd /home/gjohnson/src/my-bar && pnpm --filter bartender build 2>&1 | tail -40 && pnpm --filter bartender test 2>&1 | tail -60</automated>
  </verify>
  <done>RecipeSearchFilter.tsx and RecipeOrOrderDetail.tsx both render through FullScreenScrollArea; RecipeSearchFilter's Clear/Apply row is the last item inside the scrollable, padding-cleared content (no more sibling footer div outside clearance); `pnpm --filter bartender build` typechecks clean and `pnpm --filter bartender test` passes in full (all 9+ existing test files, including RecipeSearchFilter.test.tsx and RecipeOrOrderDetail.test.tsx, unmodified and green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bartender kiosk UI (layout only) | Pure client-side CSS/layout refactor on a trusted-LAN, no-auth kiosk app — no new data flow, no new endpoint, no new trust boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-260818q7y-01 | Denial of Service (usability) | RecipeSearchFilter Clear/Apply buttons | low | mitigate | This IS the bug being fixed: buttons rendered outside the fixed BottomTabBar's reserved clearance are unreachable/covered, blocking the filter workflow entirely on any device where the tab bar overlaps them. Fix: relocate buttons inside the shared component's clearance-reserving `<main>` (Task 2). |
| T-quick-260818q7y-02 | Tampering | FullScreenScrollArea (new shared component) | low | accept | Pure presentational layout component (props: callback + strings + children) with no data access, no external input parsing, no new dependency — same risk profile as the existing FullScreenHeader.tsx precedent it mirrors. |

No new npm dependency, no new API route, no new client-side input parsing — this plan touches presentation/layout code only.
</threat_model>

<verification>
1. `pnpm --filter bartender test -- FullScreenScrollArea.test.tsx` — new component's 2 tests pass.
2. `pnpm --filter bartender build` — typechecks clean, production build succeeds.
3. `pnpm --filter bartender test` — full suite green (all existing test files, no regressions), including `RecipeSearchFilter.test.tsx` and `RecipeOrOrderDetail.test.tsx` unmodified.
4. Manual sanity (optional, not blocking): `pnpm dev`, open Bartender's Recipes tab -> Search & Filter icon -> scroll the tag list to the bottom -> confirm Clear and Apply buttons are fully visible and tappable above the BottomTabBar, not hidden underneath it.
</verification>

<success_criteria>
- RecipeSearchFilter's Clear/Apply buttons are always reachable above the fixed BottomTabBar (the reported bug is fixed).
- Both Bartender full-screen sub-views (RecipeSearchFilter, RecipeOrOrderDetail) share one `FullScreenScrollArea` component for the header+scrollable-clearance layout — eliminating the duplicated, divergence-prone inline scaffolding that caused the bug.
- `pnpm --filter bartender build` and `pnpm --filter bartender test` both pass with zero regressions.
</success_criteria>

<output>
Create `.planning/quick/260818-q7y-fix-bartender-app-search-filter-screen-t/260818-q7y-SUMMARY.md` when done
</output>
